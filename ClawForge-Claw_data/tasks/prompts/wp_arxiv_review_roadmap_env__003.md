嘿，Jayden，最近团队在集中攻关 Tool-Augmented Reasoning 方向，我需要你帮我整理一份资料。

我把数据库的一个快照放在了 `db_dumps/` 目录下，里面有个论文清单 `papers.json`，保存着我们内部论文库的部分数据。你知道的，这个库偶尔会有重复记录、过期条目甚至一些格式不完整的脏数据。我需要你先把那些有明显问题的记录剔除掉——比如同一个 ID 出现了两次、年份写成了未来年份、或者缺少必要字段的，都别要了。

然后，基于清洗后的数据，把所有 Tool-Augmented Reasoning 方向的论文提炼出来，写一份简洁的 Markdown 综述，列出它们的标题、年份和关键结论（摘要里摘几句就行）。另外，帮我画一张 Mermaid 路线图，按时间线把这些论文串起来，展示这个方向的发展脉络。

最终产物放在工作区根目录下吧：综述命名为 `review.md`，路线图命名为 `roadmap.md`。我需要直接能看的内容，别给我脚本或额外说明。两天内搞定，谢啦！
