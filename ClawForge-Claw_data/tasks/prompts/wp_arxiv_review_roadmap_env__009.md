> 凌晨3点，我的收件箱又炸了。实验室的几个师弟师妹追着要这篇综述，但 group 的论文索引乱成一锅粥。我凌晨盯着屏幕，眼睛都快瞎了。

> 我把最新拉下来的论文数据丢在了 `data/papers/papers.json` 里，里面一堆论文，还有几个附件记录在 `data/attachments.json`（但那些图表暂时不急用）。我只要 **tool‑augmented reasoning** 这个方向的东西，近五年（2020 年起）的进展最要紧。有些论文的摘要字段是空的，那种参考意义不大，帮我直接滤掉，别污染最终结果。

> 帮我把筛选过的关键论文整理成一份综述，放到 `results/review.md`。另外画一张发展路线图（Mermaid 流程图），放在 `results/roadmap.mmd`，用节点展示这些论文的演进脉络（按时间先后用箭头串起来）。每篇论文后面记得附上它在原始数据里的 paper_id（例如 `paper_001`），方便我今晚直接去查底稿。

> 只给我干净的、能用的东西。拜托了。
