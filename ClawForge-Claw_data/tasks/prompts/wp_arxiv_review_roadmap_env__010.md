嘿，我最近在整理“工具增强推理”这个方向的材料，但被论文池里的各种方向搞晕了。我把所有论文数据都放在 `data/papers/papers.json` 里了，还有一些附件（`data/attachments.json`）可能有些补充描述，不过附件里混杂了其他项目的笔记，你看着用吧。

我需要一份干净、完整的小综述，以及一张能一眼看清这个子领域论文演进脉络的路线图。具体来说：

- 写一份 Markdown 文件，就叫 `review.md`，把 **tool_augmented_reasoning** 方向的所有论文梳理一下：标题、年份、核心贡献、以及它们之间的引用关系。不需要写太长，但该提到的重要论文和它们的传承要点都得有。
- 再画一张 Mermaid 图，保存为 `roadmap.mmd`，用时间线或流程图的方式展示这些论文的演进顺序（按年份），并且把引用依赖用箭头标出来。图的格式要规范，方便我直接粘贴到文档里。

里面有不少其他方向的噪声数据，我只要那个特定方向的，千万别混进去。论文的标识符都是 `paper_` 加三位数字，方向在 `direction` 字段里。附件里可能有一些旧版本或无关记录，不用管它们。你的输出就这两个文件，搞定后我直接拿去用。
