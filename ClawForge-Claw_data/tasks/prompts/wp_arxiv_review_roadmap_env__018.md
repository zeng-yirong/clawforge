嘿，我最近在整理“efficient_vision”方向的最新进展，准备写一篇内部的综述和路线图。

论文数据都放在 `papers/` 里了，不过每篇的摘要我单独扔在了 `data/attachments.json` 对应的附件文件中，因为主表里摘要那一栏我还没来得及填全。麻烦你从附件里把每篇论文的详细摘要提取出来，整合到一起。

最终要给我两份东西：
- 一份综述文档，命名为 `review.md`，放在新创建的 `output/` 目录下。里面需要涵盖所有 `efficient_vision` 方向的论文，并且把从附件中找到的摘要完整放进对应的描述里。
- 一份 Mermaid 格式的路线图，命名为 `roadmap.mermaid`，也放在 `output/` 里。画一个 timeline，按年份展示这些论文的里程碑，同一年份按论文标题字母顺序排好。

注意附件里可能混杂了其他方向的内容，别搞混了。我只要 `efficient_vision` 方向的论文。加油！
