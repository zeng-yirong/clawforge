嘿，AI助手。我们小组正在做“tool-augmented reasoning”方向的文献调研，为了下周组会准备材料。我把收集到的ArXiv论文元数据放在了 `papers/papers.json` 里，相关的附件（摘要、图表等）存放在 `attachments/` 目录下，附件清单在 `attachments/attachments.json`。

请帮我整理一份文献综述，放在 `outputs/review.md` 里。我希望用表格的形式列出每篇论文的ID、标题、年份、关键词和摘要，并且按照出版年份从早到晚排列。注意，我们只关心方向为“tool_augmented_reasoning”的论文，其他方向的不要混进来。另外，我个人发现有些论文的元数据可能有点问题（比如方向字段的拼写不太准确），你处理时留个心眼，别把错误的数据也算进去。

同时，请根据这些论文的演进关系，用Mermaid语法画一个时间线路线图，放在 `outputs/roadmap.mermaid` 里。路线图中的每个节点请标注为“年份 - 论文ID”的格式，并按时间顺序用箭头连接。如果能利用附件中的补充信息来丰富综述内容就更好了。

拜托了，尽快搞定！
