嘿 Jack，我的研究小组下周要交一份关于「工具增强推理」（tool_augmented_reasoning）的综述初稿。我昨天把服务器上的论文摘要快照拖到了 `data/papers/papers.json` 里，但是数据里什么方向的都有，还有一堆过时的。  

我只需要最近三年（2021、2022、2023）发表在工具增强推理方向的论文。你帮我按引用次数（citations 字段）从高到低排好序，每篇论文保留 `paper_id`、`title` 和 `citations` 三个字段，直接写成一个 JSON 数组，放到 `output/selected_papers.json`。我等着拿这个列表去写正文，别搞错了格式。
