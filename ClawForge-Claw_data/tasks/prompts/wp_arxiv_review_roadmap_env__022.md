# 研究助手留言

嘿，我是实验室的研究员小张。最近我们在整理“高效视觉”方向的研究，我手头有一份模拟的ArXiv摘要快照，放在了工作区的 `data/` 目录下。里面包含了论文列表和一些附件。我想让你帮我快速产出两份文件：

1. **review.md** — 放在 `ops/` 目录下，写一份简洁的综述。重点列出所有与“高效视觉”相关的论文（根据方向字段识别），每篇一行，格式为 `- paper_id: title`。不需要其他多余内容。
2. **roadmap.mmd** — 也放在 `ops/` 目录下，用 Mermaid 语法画一个路线图，展示这些论文之间的引用关系。节点用 `论文ID[标题]` 的形式，边用 `引用论文ID --> 被引论文ID` 表示。只包含那些有明确引用关系的边。

注意：数据里有些方向写得不标准，你只取方向字段精确为 `efficient_vision` 的论文。附件不用管。完成后把两个文件放在 `ops/` 下即可。谢谢！
