嘿，刚把最近ArXiv上关于视觉深度学习的一批论文抓下来了，数据放在 `data/papers/` 目录下，不过那个文件夹里混了几个旧备份，最新的完整数据在 `papers.json` 里。另外 `data/attachments/` 里还有些论文的扩展材料，但不是必需的。我下周组会要汇报 **efficient_vision** 方向的进展，你帮我快速梳理一份综述和未来路线图吧。结果放到 `output/` 文件夹下，弄成一个 `review.json`，里面要有：研究方向是什么、你挑出了哪些论文（按发表年份排好）、还有你觉得应该分哪几个阶段推进。附件里的东西你看着办，能帮忙就帮忙，别耽误正事就行。拜托了！
