嘿，兄弟！最近在赶一篇 tool-augmented reasoning 方向的调研报告，手头数据乱七八糟的。papers/papers.json 里是搜集到的论文元数据，attachments/ 下还有我随手写的几篇笔记。帮我整两份东西出来：

1. **review.md**：把属于 tool-augmented reasoning 方向的论文全扒出来，按发表年份从早到晚排，要是同年就按论文 ID 字母序。每篇一行表格，列出自论文 ID、标题、年份和从 abstract 里提炼的一句核心贡献（别太长，30 字内）。

2. **roadmap.mmd**：用 Mermaid 的 graph TD 画个引用关系图。每个节点标上论文 ID 和年份（比如 `paper_id(2024)`），边就是 citation_ids 里的引用指向。只画真实存在的引用（两篇都在列表里才算），孤立节点也得画出来。

搞完直接放工作区根目录，我等着用。谢了！
