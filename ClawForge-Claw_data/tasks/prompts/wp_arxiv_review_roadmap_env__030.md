嘿，小A，周末赶工的事还记得吧？老板催着要「高效视觉」方向的文献综述，明天演示就得用。我把研究室之前的论文缓存丢在 `papers/papers.json` 里了，里面塞了不少其他方向的旧文章，还有些重复项和缺字段的脏记录，你帮我筛一筛。

我要的东西很简单：

1. 把 **efficient_vision** 方向的论文全部挑出来，按发表年份升序排好。
2. 写一份 Markdown 综述，放在 `reviews/review.md` 里。不需要长篇大论，但要列出每篇论文的 **年份、论文 ID 和标题**，顺便提一句核心贡献方向（从摘要里抓个关键词就行）。
3. 再画一张 Mermaid 时间线路线图，放在 `roadmaps/timeline.mmd` 里。每个节点用论文 ID 做标识，标签写标题前 20 个字符加上年份，节点按时间顺序从左到右排，用箭头连起来。

注意：附件文件夹 `attachments/` 里有些额外 PDF 摘要，不过核心数据都在 `papers/papers.json` 了，你优先用那个。另外别漏了 `cache/` 目录里的那份旧快照，里面可能也有有用记录——不过别搞混了，我只要 **efficient_vision** 且 **年份在 2019 到 2023 之间** 的论文，超出范围或方向不对的统统扔掉。

搞完直接放工作区，我回头跑个脚本就收工。谢啦！
