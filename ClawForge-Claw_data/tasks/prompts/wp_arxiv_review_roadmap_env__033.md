小A，我这周要赶一篇关于 efficient vision 的综述，手头有从缓存导出的数据在 `data/` 目录下，包括论文列表和附件索引。你帮我根据那些论文，挑出属于 efficient vision 方向的，按年份排个序，写一份 markdown 综述，再画个引用关系图（mermaid 格式），都放在 `output/` 里。我只要准确的信息，别混入其他方向的干扰项。哦对了，那些缺失年份或重复 ID 的脏记录直接忽略就好。谢谢！
