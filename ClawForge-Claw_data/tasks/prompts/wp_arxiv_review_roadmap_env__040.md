嘿，小王，下周组会上我要做一个关于 Efficient Vision 方向的进展报告，但手头这些数据乱得一塌糊涂。我之前把所有论文信息都存到 `data/papers/papers.json` 里了，还有一些附件在 `data/attachments.json` 里（不过附件可以先不管）。你帮我把所有属于 Efficient Vision 方向的论文全部挑出来，按发表年份从早到晚排好，然后写一份综述和一张路线图。

综述就放在 `reviews/review.md` 里，用表格列出每篇论文的 ID、年份和标题，再写一小段摘要精华。路线图用 Mermaid 格式放在 `reviews/roadmap.mmd` 里，每个论文作为一个节点，按年份顺序连起来，节点里标注年份和标题。今晚就要，别漏了也别多塞其他方向的论文。
