嘿，我是隔壁实验室的小王。老板昨天又发脾气了，说我们组在「efficient_vision」方向的论文整理得乱七八糟，下半年汇报要用。我把之前同事留下的所有数据都扔在 `data/papers/papers.json` 里了，但里面掺了好多其他方向的论文，还有几个文件夹里的旧备份，你可别读错了。我只需要那个方向的所有论文，按发表年份从早到晚排好，然后把论文 ID 列个清单单独存一下，我回头手动核一遍。另外，麻烦你顺便写一份简单的综述（Markdown 格式）和一张可视化的路线图（Mermaid 格式），都放在 `docs/` 文件夹下。综述里记得提一下研究方向的名字，路线图里把论文节点串起来就行。我信得过你，搞完直接发我，别让我老板再催了。
