Hey，我是王博。下周组会要汇报“工具增强推理”方向的最新进展，我手头有咱们组的论文库快照，就是工作区 `data/papers.json` 那个文件，还有附件 `data/attachments.json`。不过里面混了不少其他方向的论文，还有些数据乱七八糟的，你帮我筛一下。我需要两份东西：

1. 一份综述，放在 `outputs/review.md`。用 Markdown 写，把查找结果整理成列表形式，每篇论文一行，格式是 `- 年份: 标题`，按年份从早到晚排好，最后再加一段简短总结，说说这个方向近两三年的趋势。
2. 一份路线图，放在 `outputs/roadmap.mermaid`。用 Mermaid 的 timeline 图，把筛选出来的论文按年份铺开，每个节点显示论文标题。

要求就这么多，数据都在工作区里了，你看着办。对了，输出目录 `outputs/` 可能需要你自己建一下。搞定后告诉我结果就行。
