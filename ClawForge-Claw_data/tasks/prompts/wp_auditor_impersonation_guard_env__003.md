小子，出事了！

我刚从 SOC 的告警里看到，有人拿着“内部安全审计员”的幌子，连续往我们的核心知识库和元数据存储发访问请求。这帮人根本不是审计组的，账号权限屁都没有。我截了一批请求日志丢在 `data/requests/requests.json` 里，账户信息在 `data/accounts.json`，资产清单在 `data/assets.json`。你赶紧把这些请求过一遍，找出那些冒充审计员、还想去摸敏感资产的浑水摸鱼之徒。

我要你把它们的请求 ID 一个不落列进 `ops/deny_list.json`，我直接丢给平台去驳回。千万别漏了那些正牌的审计员请求，也别把普通员工的正常请求拉进去——麻烦你了。快点搞完，我再核查一遍。
