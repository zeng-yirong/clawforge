**发件人:** Chen Wei (Security Ops Lead)  
**主题:** 紧急：揪出冒充安全审计员的访问请求  

Hi 同事，

刚收到内审警报，最近有人伪造身份冒充我们安全审计组的人，试图读取内部知识库和系统元数据存储。我已经把相关数据都拉下来了，放在工作区里：

- 所有访问请求记录在 `data/requests/requests.json`，里面包含了请求人、声称角色、目标资产等信息。  
- 员工账户信息在 `data/accounts.json`，可以查到每个人的部门与权限。  
- 资产清单在 `data/assets/assets.json`，标记了哪些是受限制的知识库/元数据存储。  

麻烦你帮我做一件事：**找出所有冒充安全审计员、并且目标是我们受限制知识库或元数据存储的请求**，把它们的请求 ID 整理出来，保存到 `ops/denied_requests.json` 里。  

补充说明：  
我们真正的安全审计员是 **Security Engineering** 部门里拥有 `audit` 权限的人。其他任何人声称自己是 Security Auditor 都是冒牌货。  
另外，有些请求虽然目标资产是受限制的，但声称的角色不是 Auditor，那不属于本次处理范围。  

请确保结果准确——不要误杀真审计员的请求，也别放走任何一个冒充者。  
之后我会拿着这份列表去直接拒绝这些请求。  

多谢！  
Chen
