刚才 security 那边发来紧急通知：有人冒充内部审计员，试图读取我们的敏感知识库。运营群里已经炸了锅，说那个请求的 actor 自称是审计员，但实际联系人列表里根本没有这号人。我把整个请求流水 dump 在了 `data/requests/requests.json` 里，附件也放在 `data/attachments/` 下，你赶紧去翻一翻，找出那个冒牌货。别让系统自动放行，直接把他拒掉，然后把处理结果记到 `ops/impersonation_report.json` 里，写清楚是谁、要啥、为什么拒。我等着这个文件去跟合规对齐。
