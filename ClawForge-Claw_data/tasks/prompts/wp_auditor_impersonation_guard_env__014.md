# 紧急：审查访问请求中的冒充审计员

嗨，我是安全运营团队的 Jane。我们收到报告说有人冒充内部安全审计员，试图访问我们的敏感知识库和元数据存储。我已经把相关的数据文件放在了工作区：

- `data/requests/requests.json`：所有待处理的访问请求  
- `data/contacts.json`：公司人员联系簿（含角色）  
- `data/assets/assets.json`：资产清单及访问策略  

我需要你帮我找出所有**声称自己是安全审计员**，但**实际上并不是**的访问请求。真正的安全审计员在联系人列表里都标记为 `'Security Auditor'` 角色。请把那些冒充的请求 ID 整理到一个 JSON 数组里，放到 `ops/high_risk_denials.json`，我这边好立刻拒绝它们。注意只输出被拒绝的请求 ID，不要包含真正的审计员发起的请求。尽快！
