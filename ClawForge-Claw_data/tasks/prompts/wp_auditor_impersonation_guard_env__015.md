嘿，我是运维小周。刚刚系统弹出了一堆访问请求，全是一个叫“Security Audit Team”的人发来的，想读取我们内部的“Knowledge Base”和“Metadata Store”。但昨天真正的审计组长还跟我说他们全体休假了，这肯定有问题！我已经把请求记录都整理在 `data/requests/requests.json` 里了，每个请求都附带了一份身份证明文件，放在 `data/attachments/` 下，文件名就是 `attachment_<请求ID>.json`。真正的审计员信息在 `data/contacts.json` 里，他们每个人都有一个唯一的“valid_signature”。你帮我挨个核对一下，把那些签名对不上号的冒牌货揪出来，列一个拒绝清单，放到 `ops/deny_list.json` 里，我直接去拒掉他们。注意清单里只要请求ID就行了，按字母顺序排好，别漏了也别多写。
