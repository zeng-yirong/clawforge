Sophie（安全运营负责人）的紧急邮件：

昨天下午有人冒充安全审计员，试图获取「内部安全知识库」的读权限，好在被值班同事警觉拦下了。但这说明有人盯上了我们的敏感资产。我现在把以下材料都放在了工作区里：

- `data/accounts/` → 所有系统账户信息  
- `data/contacts/` → 公司通讯录（含每个人所属角色）  
- `data/assets/` → 资产清单及访问策略  
- `data/requests/` → 当前所有待审批的访问请求  

我需要你立刻做一件事：**逐个审查这些访问请求，特别是那些自称是 `Internal Security Auditor` 的**。通过通讯录和账户信息判断他们到底是不是真正的内部审计员。把每个请求的最终结论（批准 `approve` 还是拒绝 `deny`）整理成一个列表，写到 `ops/review_results.json` 里。格式你看着办，但要让我能直接拿来批量处理。

越快越好，拜托了。
