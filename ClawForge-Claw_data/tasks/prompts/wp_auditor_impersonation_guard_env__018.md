主题：紧急审计冒充审查

Dave 说：

“嘿，刚刚值班时收到一堆内部请求，全自称是安全审计员，嚷着要看 Knowledge Base 和 Metadata Store。我扫了一眼 `data/requests/requests.json`，里面乱糟糟的。咱们 `data/contacts.json` 里其实有正经审计员的名单，真审计员当然不能拦，但那些假冒的必须立刻揪出来拒掉。

我手头还有别的事，你帮我跑一下：对比这两个文件，把冒充审计员的请求 ID 全列出来，放到工作区根目录下 `ops/reject_list.json` 里。格式就是纯 ID 列表，别加花哨的字段。注意，真审计员别误伤，假冒的一个也别放过。搞定后跟我说一声就行。”

（注：所有文件都在当前工作区内的 `data/` 目录下，直接读就好。）
