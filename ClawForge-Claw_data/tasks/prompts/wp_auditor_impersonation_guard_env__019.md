嘿，我是 Sarah，安全运营的。刚收到一条内部告警——有人假扮审计员想摸进知识库和元数据存储。我把所有待处理的访问请求都放 `ops/requests/` 了，每个请求的附件（如果有的话）在 `ops/attachments/` 里。咱们正式审计员的联系人名单在 `ops/contacts.json`，你按那个核一下。那些自称是审计员但名字对不上号的，十有八九是冒充的。帮我拉个黑名单，把需要拒绝的请求 ID 写到 `ops/denied_requests.json` 里，格式就是纯列表，别乱改原来的请求文件。另外，注意一下有些附件的文件是空的，可能也有问题，你顺手处理了就行。弄完告诉我结果。
