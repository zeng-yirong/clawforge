嘿，值班的兄弟，出事了！

今天一早安全运营就收到风，有人冒充内部安全审计员，试图读取我们那两个最敏感的知识库和元数据存储。我手头刚拿到一份全量的员工账号和权限清单，还有最近两天所有的访问请求记录。我现在得去处理一个紧急事件，没空一个个核对。

你帮我干这个活：所有访问请求里，凡是自称“内部安全审计员”或者“审计员”之类的家伙，你都得查一下他们到底是不是真有审计权限。是真正的审计员就留着他，冒牌货全部揪出来。我只要一个清单，列清楚要拒绝的请求ID（就是那个 request_id），放到 `ops/denied_requests.json` 里。我回头拿这个清单直接批量驳回。

数据都在工作区里：权限清单在 `data/accounts.json`，每个访问请求记录在 `data/requests/requests.json`，另外还有附件目录 `data/attachments/` 可能有点干扰信息，但你得拿账号权限当准绳。快点搞完，别让假审计员得手了。
