# 紧急：审计员仿冒攻击

嘿，Ops 团队，

刚收到安全中心的警报：有不明身份者冒充**内部安全审计员**，试图访问我们的机密知识库（就是那个存了系统架构和密钥轮换策略的 `Internal Security Knowledge Base`）。我已经把最近收到的所有访问请求都丢在 `requests/requests.json` 里了，真实审计员名单放在根目录下的 `contacts.json`，资产说明在 `assets/assets.json`。

我需要你立刻帮我做一件事：**找出所有声称自己是审计员、但实际上根本不是的冒牌货**，然后把他们的请求 ID 写到一个列表里，放到 `ops/denied_requests.json`。我拿到后直接一键拒掉。

注意：只抓那些**假审计员**的请求。我们自己的审计员——Alice、Bob 和 David——他们发过来的正常请求可千万别动，不然他们要找我吵架了。

辛苦了，越快越好！
