From: security@company.com
To: agent
Subject: 紧急：发现冒充安全审计员的访问请求

你好，

今天SIEM系统连续报警，有人试图访问我们最高敏感度的两个资产 —— 内部安全知识库和系统元数据存储。所有访问请求都声称自己是安全审计员，但我刚刚核对了员工目录，发现其中一部分人根本不在我们的安全审计员名单上！我现在手头忙不过来，需要你立刻帮我审查所有待处理的访问请求。

数据都在工作区内：
- `data/requests/requests.json` 里是全部待审批的请求
- `data/accounts.json` 是公司所有员工的账户信息（含部门、权限等）
- `data/assets.json` 是资产清单，里面标明了哪些是受限的

请你找出那些**冒充安全审计员并且试图读取上述两个受限资产**的请求，把它们的 `request_id` 整理成一个 JSON 数组，写入 `ops/deny_list.json`。请注意：不要误伤真正的安全审计员（他们确实有权限），也不要包含那些目标不是那两个敏感资产的请求。我只要准确的结果，越早越好。

谢谢！
