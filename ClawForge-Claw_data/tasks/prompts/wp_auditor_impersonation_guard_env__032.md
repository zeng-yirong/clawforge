嘿，值班的小伙伴——

刚才 SOC 自动触发了高级别告警：有人连续提交内部安全审计员身份的访问请求，目标直指 `Internal Security Knowledge Base` 和 `System Metadata Memory Store`。我查了一下，这几个请求的 actor 名称在域控里要么找不到人，要么压根不是审计团队的人。肯定是冒充的，想趁夜偷数据。

我已经把所有相关快照丢到 `data/` 下了，包括账户清单、联系人名录、资产注册表和全部待审批的请求。你帮我仔细过一遍，把那些 **冒充内部安全审计员、试图读取受限知识库或元存储** 的恶意请求找出来，把它们的请求 ID 写到 `ops/denied_requests.json` 里，格式放一个 JSON 数组就行，例如 `["req_001","req_002"]`。我拿到这个列表后直接批量拒掉，不用再走审批流程。

注意：合法审计员的请求不要误伤，我要确保只有真正的冒牌货被拿下。收到请速办，天亮前必须搞定。
