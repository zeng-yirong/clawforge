嗨，我是安全运营组的 Alex。今早 SOC 收到警报：有人伪装成内部安全审计员，试图调阅我们的核心知识库和元数据存储。我怀疑这不是第一次了——过去一周可能已经混进来好几条这样的请求。

我把所有相关文件都放在了工作区里：
- `data/requests/requests.json`：过去 7 天的全部访问申请，含渠道、请求人声称身份、目标资产等。
- `data/accounts/accounts.json`：全公司账号名录，包含部门、权限、唯一邮箱。
- `data/assets/assets.json`：所有数据资产的基本信息，包括分类等级和读取策略。
- `data/contacts/contacts.json`：外部通讯录，记录了供应商、合作方和审计人员的官方联系方式。

我的需求很简单：帮我揪出那些**冒充“内部安全审计员”** 的恶意请求。真正的审计员在账号库和通讯录里都能找到对应记录，而冒牌货要么查无此人，要么邮箱对不上，甚至权限都不匹配。请你基于这些文件做一次交叉核查，然后把需要立即拒绝的请求列出来，保存到 `ops/denied_requests.json`。我只关心那些声称自己是内部安全审计员、但身份经不起验证的申请——其他类型的异常暂时不用管。

格式方面，我希望每条拒绝记录包含 `request_id` 和 `reason`（简要说明为什么是冒充）。最终清单里不应该混入任何正常的或伪装成其他角色的请求。尽快给我结果，我好直接执行驳回操作。
