我这边刚收到安全告警，有人企图冒充内部安全审计员来读取我们的核心资产。我把所有请求记录都放在 `data/requests/requests.json` 里了，账户信息在 `data/accounts.json`，正式审计员名单在 `data/contacts.json`。帮我揪出那些假借审计员名义的冒牌货，把他们的请求 ID 整理到 `ops/blocked.json` 里，我立刻执行拒绝操作。注意别把真正的审计员拦错了，他们也在联系人列表里。动作快，已经有人提交了多个请求，我们得抢在对方得手前拦截。
