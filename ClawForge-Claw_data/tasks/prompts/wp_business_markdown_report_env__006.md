唉，小王啊，月底了财务那边又在催季度汇报。我们手头三个部门的业务台账一直没人整合，每次都要手动翻csv，太折腾了。

客户、产品和运营这三个 ledger 都在 `data/ledgers/` 下面，每个月的指标代码和数值都有。我需要一份简洁的 Markdown 报告，放在 `reports/` 文件夹里，文件名叫 `monthly_summary.md`。

报告里要把 **2024年12月** 每个部门的指标汇总成一个表格，另外再加一个“环比变化”的表格——就是把12月的数值和11月的比，算出每个指标的变化百分比。注意：12月的数据要完整，11月的数据也要有才能算环比。那些过期的备份文件（比如带旧月份后缀的）别管它，只看最新的三个 csv。

哦对了，客户台账里 metric_code 包含 `revenue` 和 `active_customers`，产品台账有 `units_sold` 和 `avg_price`，运营台账有 `uptime_pct` 和 `ticket_resolved`。这些就是我们需要关注的核心指标，其他杂七杂八的代码可以忽略，别让它们混进去。

你弄完直接放那，我回头自己看，抓紧啊！
