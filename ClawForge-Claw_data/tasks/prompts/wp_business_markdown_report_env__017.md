老王今天有点火大，一大早跑过来拍桌子：“昨晚系统崩了，账本数据乱成一锅粥！我把三个CSV都塞到 `data/ledgers/` 里了，你赶紧看看。里面混了不少重复行，还有几行数值写成了'abc'这种鬼东西。我要你帮我把2025年Q1的数据理清楚，每个指标（metric_code）的总值算出来，报告写在 `reports/q1_report.md` 里。表格里给我列清楚：指标名称、总值、还有状态——哪些是正常算的，哪些我因为重复或者无效数据帮你过滤掉了。别整那些没用的关联文件，就干这一件事。我十点开会要用，快！”
