嘿，我是车载空调系统的维护工程师。今天早上发现几个车机的空调预设乱套了：有些车明明选了“制冷”却吹出热风，还有些风扇转速忽高忽低。我怀疑是预设数据库出了问题。这是从一辆测试车的控制单元里 dump 出来的原始空调预设快照，就放在 `data/ac_presets.json` 里，里面记录了所有预设的配置。

我需要你帮我做件事：这个文件里混入了不少测试用的无效预设和重复条目，但真正的有效预设都在同一层。请你把那些**风扇转速设定为“high”**的有效预设对应的预设 ID 全部提取出来，整理成一个干净的 JSON 列表，保存到 `ops/valid_high_fan_presets.json` 里。之后我直接用这个列表去更新车载系统。别搞错，我只想要准确的 ID，一个都不能少，也别多塞没用的东西。

拜托了，今晚要出车，谢啦！
