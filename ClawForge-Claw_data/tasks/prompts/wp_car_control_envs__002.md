嘿，我是车主小王，昨天跑长途的时候空调风扇突然不转了，后来重启了一下勉强能用，但总感觉不对劲。我翻出上次保养时维修师傅留的便签（就在工作区的 `maintenance_log.txt` 里），上面记着一条关键提示。另外车外温度传感器（`sensors/env_temp.txt`）显示的温度也给了我线索。我记得车载系统里有个预设库 `data/ac_presets.json`，里面应该有一个预设正好适合现在的情况。你能帮我从那堆预设里把匹配的那个找出来吗？把它的“预设编号”和“风扇速度”两个关键信息提取出来，写到 `ops/fan_setting.json` 里就行，格式就像预设里的字段一样：`preset_id` 和 `fan_speed`。拜托了，修好了请你喝奶茶！
