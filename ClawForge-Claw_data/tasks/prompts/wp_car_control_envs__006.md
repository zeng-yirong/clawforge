嘿，运维的小伙伴，

昨晚我跑了一轮空调风扇的回归测试，发现运动模式下风扇转速完全不对——按道理它应该自动调节，但实测时一直在高速猛吹。我翻了一下 `data/ac_presets.json` 和 `ops/modifications.csv`，感觉是有人之前粗心调错了参数，把运动模式的风扇速度从原来的值改成了别的，但改完后没校正回来。

我现在手头有其他事，你帮我理一下：
- 运动模式（preset_id = "sport"）那个空调预设，它的风扇速度 **原本** 应该是多少？
- 找到后，把结果写成 `ops/fan_target.json`，格式像这样：
  ```json
  {
    "preset_id": "sport",
    "correct_fan_speed": "<你算出的正确值>"
  }
  
别忘了，只动 `ops/fan_target.json`，别改原始文件。拜托了！  
—— 测试工程师小陈
