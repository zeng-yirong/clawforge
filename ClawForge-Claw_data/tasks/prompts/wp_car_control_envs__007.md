嘿，小王，空调除雾模块又出妖蛾子了！用户反映前挡风玻璃老是起雾，我怀疑我们预设库里那些开启了除雾功能的配置有问题。你帮我捋一捋：去 `data/ac_presets.json` 里，把所有 `ac_enabled` 已经打开、并且 `defog_enabled` 也勾上了的预设拎出来。然后算一下这些预设的 `default_temperature` 平均值，取个整数（四舍五入就好）。接着根据这个平均值判断一下风扇该怎么吹——如果平均温度超过 25°C，风扇就用 `high` 档；否则用 `auto`。最后把这几个东西整理成一个 JSON，放到 `ops/defog_recommendation.json` 里。格式你自己看着办，但我要能一眼看到平均温度、推荐风扇档位、还有你筛选出来的预设 ID 列表。弄好了直接丢给我，我拿去做实车标定。
