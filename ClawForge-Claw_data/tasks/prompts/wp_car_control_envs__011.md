小明早上火急火燎地发来消息：

“昨晚我测试空调风扇时，发现有好几条命令发出去没反应，我以为服务器挂了呢！后来一查，是部分权限校验没过的错误命令混在里面了。我把所有操作记录都丢在 `logs/command_history.log` 里了，每条记录都写了时间、操作和结果。你帮我看看哪些 `ac-fan` 命令是真的成功执行了的，把它们的风扇速度值都找出来，算个平均值（四舍五入到整数），然后写到 `ops/fan_avg.json` 里，格式就放个 `average` 字段就行。其他命令你直接跳过，别管。我等着这个数去修配置。”

“着急，拜托！”
