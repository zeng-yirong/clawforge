你好啊，我是车队运维的老张。紧急情况！最近好几台车的空调风扇在节能模式下转速异常，用户投诉噪音大。我翻了一下维修手册，节能模式的风扇默认应该是“auto”，但是系统里好像写的是“high”。我现在需要确切的默认值来让后台做批量复位。

麻烦你帮我去 `data/` 里那张空调预设表里，找到那个名字就叫“节能”的预设，把它默认的风扇速度值抄下来。然后把这个值写到 `ops/reset_fan.json` 里，格式就写 `{"fan_speed": "xxx"}`，xxx就是你找到的值。快点搞定，我好发车间通告。
