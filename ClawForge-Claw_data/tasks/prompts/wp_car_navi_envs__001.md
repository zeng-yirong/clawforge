张师傅刚发来消息，说今天下午要跑一趟长途，从仓库出发先找地方吃午饭，然后把车开去快充站补电，最后在附近找家经济型酒店过夜。他让我提前把沿途三个补给点的清单整理出来，直接扔到 `trip_plan.json` 里，他上车就能导入导航。  

地图数据都在 `data/` 下面，不过小刘昨天更新地图时没清理干净，新旧版本混在一起了。你注意别用那个旧版 `offline_maps.json` 里的区域，新版是 `2024.06.01` 的版本。另外我们只管**市中心区域**里面的 POI，其他区域的别管。张师傅对补给点有要求：午饭一定要找餐厅（food），充电站功率不能低于 **150 kW**（快充），酒店价格不能超过 **200 元/小时**（经济型）。这三个点按照 **先吃饭、再充电、最后住宿** 的顺序排好就行。  

动作快点，他下午两点就要出发了。文件格式你自己看着办，反正导航能读就行。
