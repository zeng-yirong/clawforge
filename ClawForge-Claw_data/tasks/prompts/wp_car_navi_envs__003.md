🚨 紧急通知：导航系统运维

我是值班调度员小王。凌晨3点刚接到监控告警：多个活跃导航会话的路线段出现严重拥堵（拥堵等级≥3），用户可能会被困在路上。我们需要为这些受影响的会话添加一个最近的充电站作为临时绕行 waypoint，方便用户充电或换路线。

相关数据我已经拖到工作区里了：
- `data/sessions/` 下是所有导航会话的快照（JSON格式），每个文件包含会话状态、当前位置、路线段列表和已有 waypoints。
- `traffic/traffic_data.json` 包含各道路段的实时拥堵等级。
- `pois/pois.json` 包含所有兴趣点（充电站、餐厅、医院等）的位置和类别。

请你立刻处理：
1. 找出所有需要处理（状态为 active 且路线包含拥堵等级≥3的路段）的会话。
2. 对每个找到的会话，从 `pois/pois.json` 中找出 **类别为 charging 且距离该会话当前位置最近** 的 POI，将其 poi_id 追加到该会话的 waypoints 列表末尾。
3. 将更新后的会话信息整理成一个 JSON 对象输出到 `output/updated_sessions.json`，键为会话ID，值为更新后的会话对象（只需保证 waypoints 字段正确，其他字段可保留原样，但必须含 session_id）。

时间紧迫，别弄错！只输出受影响的会话，不要添加多余的东西。完成后告诉我一声。
