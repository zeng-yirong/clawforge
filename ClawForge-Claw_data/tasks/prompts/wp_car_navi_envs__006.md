哎，明天要开车去郊区扩展区域自驾游，路上得补电和吃饭。我把最新地图和POI数据都放在 `data/` 目录下了——注意里面有些旧备份文件，别搞混了。帮我做件事：找到郊区扩展区域对应的区域 ID，然后从 POI 数据里挑出这个区域中充电功率不低于 50 千瓦的充电站，以及所有餐厅，按从东到西（经度从大到小）的顺序排好，结果存到 `plan/trip_plan.json` 里。我明天就按这个路线跑。多谢！
