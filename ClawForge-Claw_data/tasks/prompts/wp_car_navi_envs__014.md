嘿，我是导航路书组的阿杰。刚才产品经理扔给我一个紧急工单：有位VIP用户手写了一份景点行程，说想按这个顺序导航。我扫描了一份放在工作区根目录的 `plan.txt` 里。

麻烦的是，我们公司的POI数据隔三差五就更新，数据目录 `data/` 下堆了一堆历史版本。刚才我粗看了一眼，至少有三个文件，有些还是坏的——你知道的，老版本里地点ID对不上新版，要是用了旧数据，导航直接偏到沟里去。

你帮我把 `plan.txt` 里的景点名称，按顺序从**最新的、可用的**POI数据里找出对应的地点标识，整理成一个列表。我待会直接灌给路径规划模块用。结果放在 `ops/trip_waypoints.json`，格式你看着办，只要我能一眼看出顺序就行。

时间紧，别给我搞复杂了，行就行，不行就标记出来，但别乱塞旧数据。
