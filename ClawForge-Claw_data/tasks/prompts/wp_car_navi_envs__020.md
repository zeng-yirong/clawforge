嘿，调度员小王紧急求助！今天下午要跑一趟从市中心到机场的长途，中途必须补两次电、吃一顿饭，顺序是：先充电站A，然后餐厅，最后充电站B。咱们的POI数据库在 `data/pois.json` 里，里面记录挺全的，但有些老数据没清理干净，还有个别坐标飘到外太空去了，你帮我仔细筛一下。

具体要求：
- 只要营业中的（字段 `open` 是 `true`）。
- 有些POI有重复记录（同样的 `poi_id` 出现多次），只取最新版本（`version` 最大的那个）。
- 坐标明显不合理的（比如经度 > 180 或纬度 > 90）直接扔掉。
- 最后按我说的顺序：充电站 → 餐厅 → 充电站，整理成一个列表，每个元素包含 `poi_id` 和 `name`，存到 `route/waypoints.json` 里。

拜托了！我等着这个文件去更新导航计划呢。
