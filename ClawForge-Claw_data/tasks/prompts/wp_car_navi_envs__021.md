老张：
昨天刚上线的充电规划模块又翻车了——用户从市中心出发去天津，导航居然推荐了一堆慢充桩和远郊桩，差点半路抛锚。我把最新的地图快照和 trip scenario 配置丢在 data/ 下了，你帮忙处理一下。

具体是 scenario \`navi_021\`，起点大概是市中心那片。你从 POI 数据里把周围 50 公里内、充电功率不低于 60kW 的充电站都筛出来，按离起点距离由近到远排好序，每个充电站后面附带计算好的距离（单位公里，四舍五入保留两位小数），整理成一条条记录扔到 ops/ 目录下，文件名叫 \`navi_021_chargers.json\`，我直接导入系统用。

注意：只选充电站（category 是 charging），别把餐馆、停车场混进来。功率不足的、太远的统统不要。顺序搞错了我还要手动重排，你上点心。
