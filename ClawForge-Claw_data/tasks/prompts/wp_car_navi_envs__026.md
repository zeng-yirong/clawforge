嘿，我是导航测试员小李，刚才用户报了个需求：他们想在北京天安门附近找几个充电站，要求充电功率至少 150kW。我在工作区放了请求文件 `trips/charge_plan_request.txt`，里面写了起点坐标和其他条件。

另外，咱们系统刚导出的充电站数据放在 `data/pois/beijing_chargers.json` 里，不过这个文件里混了不少其他类型的站点（比如餐厅、景点）和一些功率不达标的充电站，需要你筛选一下。

麻烦你帮我挑出符合条件的充电站，按离起点的距离从小到大排个序，取前 3 个，把它们的 ID 写到 `plans/waypoints.json` 里。格式就是一个 JSON 数组，比如 `["id1","id2","id3"]` 这样。谢啦！
