哎，昨天我跑了一趟长途，用导航系统规划路线，结果到了半路发现途径点顺序完全乱掉了！我明明是按顺序搜索了几个地点打算依次停靠的，但系统好像把我最后一次搜索的放到了第一个。真是头疼。

我用的 session ID 是 **navi-20250301-abc**。所有的搜索记录应该都写在 `logs/search_history.log` 里了，每一条都有时间戳和当时搜索的 POI 编号。你帮我按我实际搜索的时间顺序，把我搜过的 POI 理出来，写成一份途径点清单，放到 `ops/waypoints_order.json` 文件里。格式就简单一点：一个 JSON 对象，里面有一个 `waypoints` 数组，按顺序列出 POI 的 ID 就行。

注意啊，别把其他 session 的搜索混进来，也别把无效的或者重复的记录算上——我每个地点只搜了一次。这件事挺急的，明天出车前我得用这个文件重新导入导航系统。拜托了！
