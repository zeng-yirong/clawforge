Hi 分析师，昨晚系统警报一直在响——有几个大客户的活跃度崩了，我怀疑他们在准备跑路。我把原始数据丢在 `data/` 下面了：客户清单、活动日志、还有市场新闻样本。你帮我快速过一遍：

1. 从活动日志里揪出那些风险等级为“高”的客户；
2. 看他们属于哪个行业，然后去新闻样本里找对应行业的头条；
3. 针对每个高危客户写一封挽留邮件，邮件里要带上客户名称、引用的新闻标题，还有一段有温度的话。

结果存到 `ops/cache/retention_mail_cache.json`，格式用 JSON 数组，每个邮件对象包含 `customer_id`、`email`、`headline` 和 `body` 四个字段。我回头直接调用来发送。越快越好，先谢了！
