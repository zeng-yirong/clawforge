嘿，我是市场部的李明。昨晚的客户流失预警把我从床上拽起来了——好几个大客户最近一个月都没碰我们平台，使用量直线掉，连工单反馈都是负面语气。老板让我赶紧出一份定向挽留邮件草稿，我先把你需要的东西说一下。

你进工作区后，去翻 `data/logs/activity_logs.json`，把那些 **风险等级高风险、最近30天以上没活跃、使用趋势下降、工单情绪负面** 的客户筛出来。然后到 `data/news/news_samples.json` 里找跟他们行业相关的新闻——注意，我要那种能反映行业困境的负面消息（tone 是 pain_point 的），这样写邮件才有说服力。每个高风险客户只挑一条最匹配的新闻。

客户的邮箱在 `data/accounts.json` 里，按 account_id 对应客户ID。邮件主题统一写成 `"Re: " + 新闻标题`。把最终结果整理成一个 JSON 数组，放到 `cache/retention_mail.json` 里。数组里每个元素至少包含：
- 客户ID（customer_id）
- 收件邮箱（email_to）
- 新闻ID（news_id）
- 邮件主题（subject）

别把其他无关数据掺进去，我只要精准的。搞定了告诉我一声。
