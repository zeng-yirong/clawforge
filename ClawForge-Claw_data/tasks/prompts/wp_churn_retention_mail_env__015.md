嘿，运维那边又拉警报了！客户流失模型跑了个高危名单出来，但数据散落在几个旧文件里，我没空手动拼。你帮我把这根骨头啃了吧。

客户档案在 `data/customers/` 下的那个 JSON，活动日志在 `data/logs/` 里，行业新闻样本在 `data/news/` 里。我需要你找出那些 **真正危险** 的客户——风险高、快一个月没动静、使用趋势往下掉、最近工单情绪还很差的那种。然后从新闻样本里挑一条 **和这个客户行业对口的、听起来像机会而不是抱怨** 的新闻，用它写一段挽留邮件，存到 `ops/retention_cache.json`。

邮件里要把客户名字、那条新闻的标题和你的挽留话都写上，格式你看着办，反正我回头要直接读出来就能用。别整些花里胡哨的，我只要准确的信息，一个客户一条新闻一封邮件，多了没用。干完通知我就行。
