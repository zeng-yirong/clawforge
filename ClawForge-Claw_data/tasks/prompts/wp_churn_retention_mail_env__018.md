> **紧急工单 #1847**  
> 来自：运营总监 王工  
> 主题：客户流失预警 – 帮我准备一封挽留邮件

老王：
  
  最近系统一直报警，好几个大客户的活跃度直线下降。我把最近的快照数据拉到 `data/logs/activity_logs.json` 里了，客户基本信息在 `data/customers/customers.json`，新闻库在 `data/news/news_samples.json`。  
  你抓紧捋一捋：哪个客户最危险？然后从新闻库里挑一条跟他行业相关、能用来劝他留下来的好消息，帮我写一封简短的邮件草稿。  
  邮件直接丢到 `ops/retention_email_cache.json` 里，格式你定，但得让人一眼就能知道发给谁、标题是什么、正文重点在哪。  
  别给我一堆分析过程，我只要最终能用的成果。拜托了！
