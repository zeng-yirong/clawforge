我这边刚收到一批活动日志和行业新闻样本，情况不太妙。几个大客户的活跃度在持续下滑，支持工单的情绪也很负面。我们得抢在它们完全沉默之前做点什么。

我整理好的原始数据放在 `data/` 下面：客户资料在 `data/customers/customers.json`，活动日志在 `data/logs/activity_logs.json`，行业新闻样本在 `data/news/news_samples.json`。另外 `templates/` 里有一个我常用的留存邮件模板，可以直接套用。

我的需求很简单：从日志里揪出真正高危的客户（风险高、活跃趋势下降、工单情绪负面），然后从新闻里找一条最贴合的行业机会类报道，基于模板生成一封定向挽留邮件草稿。最后把草稿放进 `ops/retention_draft.json` 里，我明天审核后手动发送。

时间紧，别给我中间步骤，我只要最终结果文件。邮件内容要诚意满满，别漏了客户名字和新闻标题。
