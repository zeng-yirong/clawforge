Hey, 运营部刚拉了个预警，说有几个大客户活跃度在往下掉。我把数据丢在 `data/logs/activity_logs.json` 了，里面记着客户的风险等级和最近活跃天数。另外 `data/news/news_samples.json` 里有一些行业新闻样本，是我们公关那边收集的。

你帮我看看那些风险等级高而且超过30天没怎么动的客户，他们所在的行业最近有没有什么利好新闻。把匹配出来的结果整理一下存到 `ops/retention_pairs.json` 里。每条记录里写上客户的ID、名字、所属行业，还有那条新闻的标题和摘要就行。数据可能有点脏，你看着处理，别把重复或者不相关的搞进去。

急用，搞完喊我。
