> 运营小张凌晨发来一条消息：

哎，我刚跑完这个月的活跃度看板，心凉了半截。几个老客户最近几乎不登录了，工单反馈也全是负面情绪。再不拉一把，下季度又要掉几个大单。

我把最新的客户和活动数据都放在 `data/customers/customers.json` 和 `data/logs/activity_logs.json` 里了。另外，市场部整理了一些行业新闻样本在 `data/news/news_samples.json`，据说有些行业的正面消息能帮我们暖场。

你帮我从活动日志里找出**风险最高的那些客户**（就是那种使用频率断崖式下跌、工单吐槽不断的），然后结合他们所在的行业，挑一条**最积极、最有希望的新闻**出来。给每个危急客户写一封挽留邮件草稿，邮件里要带客户名、对应新闻标题，语气诚恳一点，别太模板化。

整理好之后存到 `ops/retention_cache.json`。格式你看着办，但得让我能一眼看懂哪个客户配哪条新闻、邮件正文写了啥。我回头要复制出来手动调整。

尽量快，周会上要用。
