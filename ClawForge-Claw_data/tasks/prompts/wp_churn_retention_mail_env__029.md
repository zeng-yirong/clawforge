嘿，我是客户成功部门的 Sam。最近系统监控显示有几个大客户活跃度掉得厉害，工单那边的语气也不太妙。我手头有上周的活动快照和新闻简报，都在 `data/logs/` 和 `data/news/` 里。客户资料在 `data/customers/`，不过里面可能混了些测试账号和历史垃圾，你过滤一下。

我的需求很直接：找出当前真正需要紧急挽留的客户（就是活动日志里打了高风险的），然后从新闻里挑一条他们行业相关的、有积极信号的消息——那种能用来谈未来机会的，别找负面新闻。最后用这些素材帮我写一封简短的挽留邮件草稿，放进 `ops/email_cache.json` 里，我直接复制粘贴就能发。邮件里要带上客户名字和那条新闻的标题，语气诚恳一点。

哦对了，`ops/` 下可能有个旧的缓存文件，别管它，直接覆盖成新的就行。我只要准确的结果，别多事。

快去弄，今晚之前我要跑数据。
