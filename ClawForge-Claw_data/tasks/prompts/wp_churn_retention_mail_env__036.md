> 我这边是客户成功团队的小李，刚被运营总监催了——这个月几个大客户的活跃度都在往下掉，再不干预可能就要流失了。  
>  
> 我把数据团队整理的活动日志和客户信息扔在 `data/` 里了，你看看。日志里标了风险等级和最近活跃天数，我们关注那些风险高、而且最近超过30天没怎么动的客户，他们才是真正需要紧急挽留的。  
>  
> 另外，新闻组那边给了些行业素材，也放在 `data/news/` 里。我想为每个高危客户挑一条他们所在行业里讲“机会”的新闻（别找负面的），然后写一封简短的留存邮件草稿，包括邮件主题和正文，里面至少要提到客户名称和那条新闻的标题。  
>  
> 所有结果整理成一个 JSON 文件放到 `ops/retention_campaign.json` 里，每条记录包含客户 ID、客户名称、新闻 ID、新闻标题、邮件主题和正文。我后续要根据这个触发自动化发送。  
>  
> 谢了！尽快给我结果。
