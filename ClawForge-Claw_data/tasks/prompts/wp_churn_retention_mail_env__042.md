嘿，我是客户成功部的Jamie。我们最近一批活动日志真是乱得可以，但有几个大客户明显在流失边缘——最后活跃都是两个月前，工单态度也很负面。我把原始日志放 `data/activity_logs.json` 了，客户资料在 `data/customers/customers.json`，还有最近行业新闻样本在 `data/news/news_samples.json`。

我需要你在工作区里帮我把那几个高风险客户的挽留邮件草稿准备好，每个客户参考一条匹配他们行业的新闻（新闻样本里的头条和摘要直接拿来用），邮件主题和正文都写清楚，收件人邮箱从客户资料里拿。最后把所有草稿按我们团队的惯例存成一个缓存文件，路径用 `cache/retention_mail_cache.json`。我回头直接跑脚本发出去，所以格式要规整，别漏人，也别把低风险的混进来。

赶紧弄，我下午就要用。
