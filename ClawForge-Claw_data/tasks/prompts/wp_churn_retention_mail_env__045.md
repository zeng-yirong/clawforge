嗨，我是客户成功团队的 Alice。最近我们流失预警系统响个不停，我手头有几份数据文件，想请你帮忙快速出一份留存邮件草稿，我先缓存起来，回头让运营同事再润色。

客户档案在 `data/customers/customers.json` 里，每个客户有行业、等级、负责人等信息。活动日志在 `data/logs/activity_logs.json`，记录了每个客户的最近活跃天数、风险等级、使用趋势和工单情绪。另外，市场部整理了一批行业新闻样本在 `data/news/news_samples.json`，每条新闻标注了所属行业、语气（机会还是痛点）和摘要。

我关注的是那些真正高危的客户——风险等级高，而且超过 30 天没动静了。对于这样的客户，我想挑一条他们行业里 **机会型（opportunity）** 的新闻作为邮件引子，写一封简短但有针对性的留存邮件草稿。邮件里至少得包含客户名称和那条新闻的标题，这样运营一看就知道针对谁、说什么。

最后把草稿放到 `ops/retention_email_cache.json` 里，我直接拿去用。拜托了！
