嘿，运营团队这边刚拉了一波活动日志，我扫了一眼，感觉有几个大客户状态不太妙。你帮我看一下 `data/logs/activity_logs.json`，把那些风险高、好久没动过、用量一路往下掉、而且最近工单态度很差的客户找出来。这批人再不管就要流失了。

然后去 `data/news/news_samples.json` 里捞点他们行业相关的“机会”类新闻，挑那种能让人看到曙光的。针对每个高危客户写一封挽留邮件，主题和正文都拟好，放到 `ops/retention_emails.json` 里，我直接拿去推。格式你懂的，之前咱们做过类似的，别搞得太复杂，但信息要全。
