嘿，我是 Daniel，Cloud FinOps 的老大。刚收到财务 VP 的急电：要我们 6 月的业务集群成本明细，明天一早就要挂到 dash 上。我把所有原始数据都丢在工作区了——`data/` 下面有集群清单、资源账单、还有定价目录。你需要的就是把这些东西拼起来，给我一份**只包含业务集群（business clusters）的月度成本报告**。

注意，定价目录里有两个版本，一个 3 月的归档版，一个 6 月的现行版——别拿过时的算，不然 CFO 会发飙。资源账单里可能有些“垃圾条目”，比如数量为零、负值、甚至缺字段的，你眼睛放亮一点，别让它们污染结果。每个业务集群的 total_cost 按当前定价计算，保留两位小数。

搞完之后，把报告写成 `cost_report_2026_06.json`，放在工作区根目录下。我要的格式：一个 JSON 对象，里头有 `report_month`（“2026-06”）、`clusters`（数组，每个元素带 `cluster_id`、`cluster_name`、`total_cost`）。就这么多，别画蛇添足加无用字段。

拜托，今晚我得睡得着觉。🙏
