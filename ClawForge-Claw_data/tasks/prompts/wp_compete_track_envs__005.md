嘿，我是市场部的 Jason。我们正在制定下季度的竞争策略，需要一份精确的竞品分析。我把实习生昨天 dump 的数据放在了 `data/competitors/` 里，但他好像手滑了——里面既有最新的快照，又有旧备份和测试数据，还有几个非 JSON 的杂文件。我没空一个一个看，你帮我从这些文件里捞出真正有用的竞品。

具体是这样的：我需要找出那些 **体量足够大且增长迅猛** 的竞争对手——市值（market_cap）超过 500 亿，而且增长率（growth_rate）高于 20%（增长率字段存的是百分比整数，比如 25 表示 25%）。把符合条件的竞品挑出来，每个竞品记录它的名字、市值、增长率、市场份额和营收。最后把它们的平均市场份额和平均营收也算出来，跟前面的列表一起放到 `reports/competitive_analysis.json` 里。平均值保留两位小数就行。

目录结构你不用动，只要产出那个 JSON 文件就好。赶紧搞，今晚要提交给 VP 看。
