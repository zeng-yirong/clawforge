嘿，我是市场分析组的 Kevin。昨晚法务推过来一份紧急通知——那个新出的《美国 AI 透明度法案》（US AI Transparency Act）据说下个月就要开始执行，合规部让我们先把受直接影响最严重的竞品列出来，他们要提前准备应对方案。

我把所有政策文件丢在 `data/policies/` 了，竞品的详细资料在 `data/competitors/` 里。政策文件里有“影响范围”的备注（impact 字段），明确写了哪些竞品会被直接点名；你只需要管那些 **当前已经生效** 的政策（status 是 active），别碰还在提案阶段的。另外，营收低于 5000 万的竞品暂时不考虑，影响太小不值得优先处理。

找到那个目标竞品后，把它在文件里的唯一编号和全名记下来，放到 `ops/priority_target.json` 里。格式就按标准的 JSON 写，字段名叫 `competitor_id` 和 `name`。快弄好，我十分钟后要用这个去跟 VP 汇报。
