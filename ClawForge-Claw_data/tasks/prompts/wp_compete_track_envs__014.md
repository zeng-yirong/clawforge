Hey 伙计，市场部那边又甩任务过来了。他们想做一轮定向营销，目标是一批高价值潜在用户。具体定义是这样的：**Premium 等级、通过 Referral 渠道加入、并且所属公司的增长率超过 20%** 的用户。

我把原始数据扔在 `data/users/` 和 `data/competitors/` 下面了，都是标准的 JSON 格式。里面有些字段可能用不上，但你需要的基本都在。帮我找出所有符合上面条件的用户，整理成一份清单，每条记录包含用户的 ID、姓名、公司名称和公司增长率，按用户 ID 升序排好。最后把结果放到 `ops/target_leads.json` 里，我直接拿给市场部用。

对了，数据里可能有些过时的备份文件夹，别管它们，只看 `data/users/` 和 `data/competitors/` 下的 JSON 文件就行。谢了！
