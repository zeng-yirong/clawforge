From: VP of Competitive Intelligence
To: Competitive Analysis Team
Subject: Quick Insight on SmartSaaS for Tomorrow's Board Meeting

Hi team,

We need a last-minute insight on SmartSaaS for the board presentation tomorrow. 

First, check the `data/competitors/` folder – I know we have multiple snapshots of their profile. Pick the most up‑to‑date one (the version number or date should help you figure that out). 

Then, dig into `data/users/` and pull all users that belong to SmartSaaS. I’ve noticed some old entries there that are marked as churned – please ignore those so we only count the active accounts.

I need three numbers from you:
- Total active users for SmartSaaS.
- Average acquisition cost for those active users.
- The market share they publicly reported (you’ll find it in the competitor file you selected).

Combine everything into a single JSON file at `reports/smartsaas_insight.json`. The structure should be simple – just keys for `competitor_id`, `total_users`, `avg_acquisition_cost`, and `market_share`. No extra fields, no confusion.

Get this to me by 3pm. Thanks!
