嘿，我是安全运营的老王。昨天下午两点多，监控发现有人试图通过外部邮件系统直接索取我们的未发布架构图，我赶紧截下了当时的请求日志，扔在 `data/requests/requests.json` 里。

你帮我看一下，这些请求有的是内部同事的正经流程，有的是伪装成合作伙伴的钓鱼试探。我们的未发布技术计划资产 ID 是 `tech-plan-v2`，分类是 `technical_plan`，只有走完 OA 审批的内部人员才能碰。凡是外部发起的、目标又是这个资产的请求，一律可疑，必须拦截。

你把需要拦截的请求 ID 整理出来，写进 `ops/block_targets.json`，格式就是一个 JSON 数组，里面只放那些该拦的 ID。别把自家人的正常请求也加进去，我可不想背锅。快搞，我等着交给防火墙团队。
