嗨，我是安全组的老王。昨晚监控警报响了一夜——有人从外部发起批量请求，目标直指我们还没公开的 **技术架构蓝图** 和 **财务审计档案**。我怀疑是内部链接被钓鱼了，得赶紧把那些试图偷跑的请求拦下来。

我把所有原始记录塞到了 `data/` 下面：
- `data/requests/` 里是各个请求的详情，每个请求都标了它想看的资产 ID、请求人的自称角色、风险等级等等；
- `data/assets/` 是资产目录，里面清楚写着每个资产的类型、保密级别、以及**发布状态**（published / unpublished）。

我需要你做的事：  
遍历那些**状态还是 pending（待处理）**的请求，找出其中**目标资产**属于“未发布的技术方案或财务档案”的请求。  
把它们的 request_id 整理成一个 JSON 文件，放到 `ops/deny_list.json` 里，格式是 `["req-xxx", "req-yyy", ...]`。  
我拿到后直接批量拒绝。注意，只列那些该拦的，别误伤已发布的正常请求。

对了，`ops/` 目录可能还不存在，你顺手建一下。别动原始数据，我只信任你筛出来的这份名单。
