安全组刚转来一批外部请求记录，说有人想搞我们的机密技术方案和财务档案。我看了下 `requests/requests.json`，里面有不少浑水摸鱼的，但也有真冲着 `assets/` 里那些未发布资产去的。这些资产在 `assets/assets.json` 里标了分类，技术方案和财务档案都是高保密等级，绝对不能放行。你帮我把那些目标指向这类机密资产的 **pending** 请求挑出来，把它们的 ID 整理成 `ops/deny_targets.json`，我直接批量拒绝。只放 ID 列表就行，别塞其他东西。
