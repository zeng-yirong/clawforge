嘿，我是安全组的 Alice。昨晚又有几波外部请求撞到我们的保密文档上了，IT 那边已经给我甩了一堆日志。

我把原始请求和资产清单都放在了 `data/` 下。你帮我筛一下：那些标记为“confidential”且访问策略是“restricted”的文件，原则上只有该资产的所属团队才能碰。如果请求者声称的角色不在这个所属团队里，那就是违规访问，必须拦下来。

你不需要管那些公开文件，也不用纠结低风险告警。直接找出所有需要拒绝的请求，把它们的 `request_id` 整理成一个列表，写到 `ops/denied_requests.json` 里。我只要准确的 ID，别把内部同事正常的工作请求也误杀了。

干完喊我一声，我这边等着去防火墙落地规则。
