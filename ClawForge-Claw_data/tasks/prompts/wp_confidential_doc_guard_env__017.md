我是 Sasha，SOC 值班。刚才 SIEM 炸了——大量外部请求正在疯狂扫描我们的两个核心机密资产：**未发布的技术架构蓝图**和**还没公开的财务档案包**。我手快把系统快照全拽到工作区了，资产清单、请求日志、甚至附件都在，你直接看。

现在，把所有真正威胁到这两个宝贝的请求挑出来，生成一份拒单清单，存到 `ops/blocked_requests.json`。格式就按标准来：一个数组，每项包含 `request_id` 和 `reason`，reason 写清楚为什么拦它。别拖，数据泄露就在眼前！

注意：内部同事的正常访问别误杀，已经拦过的也别重复处理。快去。
