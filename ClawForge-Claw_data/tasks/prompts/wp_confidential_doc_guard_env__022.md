嘿，安全分析员，紧急情况！

我刚从监控系统收到警报——有外部IP在反复试探我们未公开的机密文档库。直觉告诉我，其中几笔请求是冲着“未发布技术架构图”和“财务封存档案”来的。我已经把完整的请求快照和资产清单拖到工作区了，你帮我快速过一遍：

- 请求记录在 `requests/requests.json`
- 资产详情在 `data/assets/assets.json`

策略很简单：**只拦截那些真正指向高敏感未发布资产（classification 标记为 `confidential`）的高风险请求（risk_level 为 `high`），并且这些请求当前还是 `pending` 状态。** 别把那些日常维护的低风险请求或者已经处理的请求加进去，否则会让运维团队白忙一场。

你处理完后，把筛选出来的请求 ID 列表写到 `ops/deny_targets.json`，格式是一个 JSON 数组，例如：
["REQ-001", "REQ-002"]
我需要这份清单立刻交给防火墙执行组。拜托了，别漏掉任何一条真正的威胁！
