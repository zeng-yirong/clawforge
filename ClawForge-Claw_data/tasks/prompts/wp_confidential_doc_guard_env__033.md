嘿，安全监控组的小王吗？我是基础架构的老李。昨晚我们的机密文档访问审计日志炸了——外部IP疯狂扫我们还没公开的技术方案和财务数据。我手头有 `data/requests/requests.json` 里截获的所有待处理请求，还有 `data/assets/assets.json` 记录了每个文档的权限状态。你帮我捋一遍：哪些请求是**外部渠道**发来的，而且目标文档是**未公开**的高机密文件（技术蓝图或财务档案）？把它们的 `request_id` 整理成一个列表，存到 `ops/deny_list.json` 里，我直接批量拒绝。只要列表，别整多余的字段，我脚本要用的。
