嗨，我是 Jacqui，安全运维组的。最近日志里冒出一堆让人头疼的访问请求，好多都在打我们还没公开的技术蓝图和财务档案的主意。我已经把完整的请求记录、资产清单、还有每个请求的附件都整理好放在 `data/` 下面了。

我需要你帮我把那些**必须被拒绝的外部请求**挑出来。规则很简单：只有那些真正冲着未发布的技术计划或财务档案来的、而且是外部身份发起的请求才需要拦截。内部同事的正常访问可别误伤。

把筛选出来的请求 ID 放到 `ops/deny_list.json` 里，格式给我一个对象，里面 `requests` 字段是个数组就行。比如 `{"requests": ["req_xxx", "req_yyy"]}`。拜托了，这周审计要拿这个做重点，别搞错。
