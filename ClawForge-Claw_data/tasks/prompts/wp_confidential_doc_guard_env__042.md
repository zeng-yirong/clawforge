我是安全运营中心的负责人。今天凌晨，我们的威胁监测系统拉响了一级警报——有可疑的外部请求试图绕过身份校验，直接访问我们尚未公开发布的核心技术架构图和财务档案包。我已经把近期所有请求记录以及对应的附件内容都放在了 `data/` 目录下，方便你交叉分析。

**业务背景**：公司有两大类高敏感资产——“Finance Archive Bundle”（财务归档包）和“Unpublished Architecture Plan”（未发布架构计划），分别归财务运营部和架构办公室管理。所有外部对这两类资产的访问请求都必须经过严格审核，特别是当请求者声称的角色与资产拥有者部门不一致时，更需要警惕。

**我现在需要你帮忙做的事**：请你根据 `data/requests/requests.json` 里的请求详情，结合 `data/attachments/` 下的附件原文，以及 `data/accounts.json` 和 `data/assets/assets.json` 提供的权限与资产信息，判断哪些请求应该被立即拒绝。这类请求通常隐藏着欺诈、凭证窃取或未授权访问的企图。请把需要拒绝的请求ID（`request_id`）整理成一个列表，写入 `ops/deny_targets.json` 文件中。

**注意事项**：别把所有看着可疑的都毙了——可能有一些是内部同事做的合规审计或例行维护，它们的附件或请求描述会证明自己是清白的。我只希望把真正的恶意请求拦截下来。行动要快，但判断要准。
