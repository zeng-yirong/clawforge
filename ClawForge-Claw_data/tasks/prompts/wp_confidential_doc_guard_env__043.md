嘿，运维值班的同事，我是安全组的 Lena。昨天下午开始，有好几个自称外部审计的家伙通过工单系统发了一堆请求，说要调阅我们两个重要的内部资料——一个是未公开发布的技术架构方案，另一个是还没审计完的财务归档。我看他们填的部门和理由都怪怪的，附件里还夹着一些很可疑的链接描述。我觉得这八成是钓鱼或者数据窃取尝试。

我已经把工单系统的请求快照放在 `data/requests/` 下面了，每个请求的目标资产信息在 `data/assets/assets.json` 里，请求所带的附件清单在 `data/attachments.json`，附件实际内容放在了 `attachments/` 目录。你帮我把这些请求逐个分析一下，找出那些真正威胁到我们未公开资产（技术方案和财务档案）的请求，把它们的请求 ID 整理到一个清单文件里，放到 `ops/deny_list.json`。我拿到之后会立刻在系统里拒绝掉。别漏掉，也别把正常请求误杀进去。拜托了！
