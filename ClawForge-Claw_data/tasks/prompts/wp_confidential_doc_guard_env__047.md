Subject: 🚨 紧急：外部请求窃密预警 – 请立即生成拒绝列表

嘿，伙计，

昨晚 SOC 警报响了三次，全部指向我们在 `data/assets/` 里标记为最高机密的资产——就是那两份还没公开的架构规划图和财务归档包。有人通过不同渠道提交了访问请求，伪装成合规审计，但意图很明显。

我已经把完整的请求记录扔在 `data/requests/requests.json` 里了，资产元数据也在 `data/assets/assets.json`。你帮我筛一遍：把所有**目标直指最高机密资产、并且至今还没被处理（状态仍是 pending）** 的请求揪出来。我需要你生成一个清单，丢到 `ops/deny_list.json` 里，格式就是一个纯数组，里面放这些请求的 ID。我会直接拿这个列表下发到防火墙阻断规则。

注意：别把已经审批通过或已拒绝的历史记录加进去，也别把那些目标不是最高机密的请求掺进来——那些可能是正常业务。我只要真正的危险项。快一点，防火墙那边等着呢！
