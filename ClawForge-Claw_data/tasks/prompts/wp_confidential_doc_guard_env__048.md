紧急通知：安全中心刚刚截获了一批来自外部的异常请求，对方似乎盯上了我们尚未公开的核心技术方案和财务档案。所有请求记录已经汇总在 `data/requests/requests.json` 里，每个请求都指向了一个具体资产。资产详情（包括发布状态、密级、类型）都在 `data/assets/assets.json` 中。我需要你立刻审查这些请求，把那些真正瞄准“未发布且密级为机密”的技术方案或财务档案的请求全部挑出来，将它们的 `request_id` 整理成一个列表，写到 `ops/deny_list.json` 里。我这边等着批量拦截，动作要快，别漏掉威胁，也别冤枉正常的访问。其他的日志文件或附件你可能用不上，但都在工作区里，自己斟酌。
