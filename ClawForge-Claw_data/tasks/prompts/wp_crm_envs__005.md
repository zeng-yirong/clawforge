嘿，有个急事！TechCorp 的客户经理刚来找我，说他们公司的 CTO 和 VP Engineering 这些核心联系人在系统里一个 VIP 标识都没有，而且还被丢在 inactive 文件夹里。市场部下周要开合作会，必须马上修复。

我把所有资料都丢在 `data/` 下了——联系人、公司信息、标签定义都在里面。你帮我筛一遍，把 TechCorp 名下那些本该标成 VIP 并挪到业务文件夹的联系人找出来，整理成一份修改清单，放进 `ops/contact_updates.json` 里。清单用数组，每个条目写清楚联系人的 ID、最终该放哪个文件夹、以及更新后的标签列表。我需要一眼就能看明白每个要改什么。

拜托了，尽快搞定！
