嘿，我是你的CRM经理老张。今天（2024年6月15号）早上我扫了一眼生日日历，发现接下来一个月好几个老客户的生日要到了，但我实在抽不出手一个个去查。你帮我把这件事料理了？

**要求很简单：**
- 只处理 **business** 文件夹下的联系人（personal和inactive的别动，他们暂时不管）。
- 给**每个在6月15号到7月15号之间过生日的business联系人**做两件事：
  1. 如果这个联系人的tags列表里还没有“birthday”标签，就加上它（标签定义文件里已经有这个标签了，直接引用就行）。
  2. 如果这个联系人的生日提醒还没建，就创建一条新的生日提醒——提醒日期就是生日当天，提前1天通知，每年重复。
- 别重复操作：已经有标签/提醒的就不动它，但不要漏掉该加的。
- 所有数据都在 `data/` 目录下：
  - `data/contacts.json` – 联系人主表
  - `data/tags/tag_definitions.json` – 标签定义
  - `data/reminders/reminders.json` – 现有提醒记录
  - `data/current_date.txt` – 上面“今天”的日期，来自系统，以它的内容为准。
- 最后，把你做的所有变更总结成一份报告，放到 `output/upcoming_birthdays.json` 里，列出每个命中联系人的 `contact_id`、`full_name`、生日、以及这次是否新增了标签/提醒。

动作快点，中午前我要拿这个去发祝福邮件。直接改 `data/` 下的文件，不要另起炉灶。报告格式你自己定，但字段要清晰，我能一眼看出谁被处理了。
