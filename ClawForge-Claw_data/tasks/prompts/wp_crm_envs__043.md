小张，紧急求助！老板昨晚突然要求给每个联系人都打上行业标签，说什么要搞精准营销。我刚才翻了系统，发现 `data/contacts.json` 里的联系人很多还没有行业标签，但有些已经有了——我查了一下 `data/companies.json` 知道每个公司的行业。我实在不想手动一个个比对，太容易漏了。

你帮我跑一下：把那些**还没有行业标签**的联系人找出来，根据他们所属的公司行业，生成一个记录文件放在项目根目录，叫 `industry_tags_applied.json`。里面每条记录就写 `contact_id` 和 `tag_name`（注意 tag_name 直接从公司行业字段抄过来就行）。千万别把已经有的再重复加进去，也别管那些找不到公司的联系人——它们可能是旧数据，跳过就行。

拜托了，我上午要交差！结果文件放根目录就行。
