嘿，我是运营部的Mike。我们现在的客户标签完全是摆设，跟实际消费脱节两个月了！我刚把最新的消费和活动数据放到 `data/logs/` 下面，客户档案在 `data/customers/customers.json`。你帮我根据这些最新信息重新算一下每个客户的层级标签，整理成一个清晰的 JSON 文件放在 `data/labels/tier_labels.json` 里。规则很简单：

- 季度消费 3 万以上并且最近 30 天内活跃的 → Gold
- 季度消费 1 万以上并且最近 90 天内活跃的 → Silver
- 其他的 → Bronze
- 如果某个客户完全没有消费记录，就标成 Unclassified

注意有些客户可能只有消费记录没有活跃记录，或者反过来，你按自己的判断处理就行。小心那些旧的备份文件，别用错数据。弄好了跟我说一声。
