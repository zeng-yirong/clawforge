Hey there!

我是客户成功团队的 Alice。我们现在的客户分层标签还是半年前的呢，完全跟不上业务变化了。我刚刚把最新的季度消费数据和活跃度日志整理好了，就放在工作区的 `data/logs/` 下面。

麻烦你根据我们公司一贯的分层规则重新计算每个客户的标签，并输出到 `data/updated_labels.json` 中。规则很简单：
- 季度消费 >= 8万 且 最近30天内活跃的客户，打上 **VIP** 标签。
- 季度消费 >= 3万 且 最近60天内活跃的客户，打上 **High** 标签。
- 超过365天没有活跃记录的客户，标记为 **Churned**。
- 其余客户统一打 **Standard** 标签。

注意：所有数据文件都是 JSON 格式，但可能存在多个版本，请务必使用最新的、不带后缀的那份文件。另外，如果某个客户在消费日志和活跃日志中都有记录，以日志为准；如果某个客户没有记录，则忽略它。

输出时每条记录应包含 `customer_id` 和 `label` 字段。多谢啦！
