嘿，我是市场部的Jane。我们刚更新了客户分层策略，需要给高价值客户打上“priority_support”标签。我把资料都扔在工作区了：

- 客户档案在 `data/customers/customers.json`
- 最新一季度的消费记录和活跃报告都在 `data/logs/live/` 下，一个消费文件、一个活动文件。
- `data/logs/` 里还有之前存档的旧数据，别搞混了。

新策略很简单：季度消费超过5万美元、最近30天内有活跃记录且风险低的客户，就该享受优先支持。你帮我跑一下，把符合条件的客户ID和要加的标签写进 `ops/tier_updates.json` 里就行。

注意，我只想要最新数据的结果，别拿旧报告里的数字糊弄我。还有，每个客户的标签是追加，不是覆盖，但我只需要你输出要新增的那一个标签就够了。搞定跟我说一声。
