Alice（运营经理）发来一条紧急工单：

> 小李，昨天审计部又抱怨客户分层标签跟实际行为对不上了，CEO 要求今天必须出正确版本。我把最新一季度的消费快照和活动日志都拉到 `data/logs/` 下了，客户档案在 `data/customers/` 里。分层的规则文档我之前整理好放 `rules/` 目录下了，你按那个规则重新跑一遍，结果写到 `reports/tier_labels.json`。  
>  
> 注意这些原始数据有点乱，有些记录明显是脏的（比如说消费金额是负数、活跃天数是负数、或者客户 ID 不在档案里的），你做之前先洗干净，别把脏数据算进去。结果里每个客户一条记录，带上客户 ID、客户名、新标签，方便我直接导入系统。  
>  
> 辛苦，尽快搞完。
