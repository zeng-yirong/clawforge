小王，我刚整理了一下我们的客户分层数据，发现目前线上打的一批标签还是去年的，跟实际情况差得离谱！你看——

仓库 `raw_data/` 里有最新的季度消费报表和活动日志，还有一份规则文档，是业务那边刚给的。我手头太乱，没时间自己算。你帮我跑一遍，把更新后的客户分层标签写到 `ops/latest_tier_labels.json` 里。

注意：数据文件里可能混了些历史残留的脏记录，比如重复的行、过期的客户、字段不完整的条目，你处理的时候留个心眼，别让脏数据污染了结果。规则文档里怎么定，你就怎么算。

最后只要一个文件，结构清楚就行，我这边直接拿去同步。辛苦了！
