嘿，AI 助手！我是客户运营部的 Chris。刚拿到 Q3 的消费和活动数据，放在 `data/` 目录下了，文件名是 `consumption_logs.json` 和 `activity_logs.json`。麻烦你帮忙跑一下新的客户分层打标。

事情是这样的：我们最近更新了分层规则，但数据有点脏——好像有些记录有问题，比如支出是负数、活跃天数为负数，还有重复出现的客户 ID。另外，有些客户只出现在其中一个文件里。我们只对两个文件都存在的有效客户打标。

新规则分三档：
- **VIP**：季度消费超过 50,000 美金，且最近 30 天内有登录（last_active_days ≤ 30），且风险等级为 low（risk_level 为 "low"）。
- **重要（Important）**：季度消费在 20,000 到 50,000 美金之间（含），且最近 60 天内有登录（last_active_days ≤ 60）。
- **普通（Standard）**：其余所有有效客户。

对于重复的客户 ID，以两个文件中各自最后一次出现的记录为准（即取数组顺序中最后的那个）。无效数据（quarter_spend_usd ≤ 0 或 last_active_days < 0）对应的客户直接忽略。

处理完后把结果写到 `output/customer_tiers.json`，每条记录包含客户 ID、计算出的标签（tier）和原因（reason）——原因简单说明一下为什么是这个标签就行。

先谢了！数据在 `data/` 下，我等你的结果。
