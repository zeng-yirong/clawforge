老李，Q2 的数据总算齐了！我在 `data/logs/` 里放了消费明细和活动日志，客户主数据在 `data/customers/` 下。运营那边发了新的分层标准，说附件里写得清清楚楚——我直接存到了 `data/attachments.json` 里，你点开就能找到。

帮我把所有客户的标签按最新规则刷一遍，最后把结果搁到 `result/customer_labels.json` 里。我这边急着出报告，辛苦啦！
