嘿，分析师，我是运营的小王。最近市场部反馈客户分层太粗糙了，流失预警完全跟不上。他们刚丢了一套新规则在 `rules/segmentation_rules.json`，你赶紧看看。

数据都在 `data/logs/` 下面，消费和活动日志都有。有几个客户的消费字段写着 "N/A"，应该是录入错误，按0处理吧。另外 `rules/` 里有个旧版本的文件别管它。

搞完把每个客户的新标签写成 `ops/label_update.json`，我直接导入系统。格式你看着办，能让我一眼对得上客户ID和标签就行。辛苦了！
