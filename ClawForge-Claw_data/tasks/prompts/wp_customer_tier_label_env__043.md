你好！我是市场部的 Sarah。我们前段时间制定的客户分层标签好像没同步更新，导致现在营销活动瞄准错了人群，浪费了不少预算。

我把最新的客户消费数据和活跃度日志都放在了 `data/logs/` 下，客户基本信息在 `data/customers/customers.json`，还有之前定好的分层规则 `data/segmentation_rules.json` 也一起给你。规则里清楚写着怎么根据消费金额、活跃天数和风险等级给客户分档。

我需要你：**根据规则重新给每位客户贴上正确的层级（tier）和标签（labels），然后把更新后的全部结果集中放到 `ops/updated_labels.json` 里**。注意，那些没有对应消费或活跃记录的客户，就保持原样别动；但只要有完整数据的，必须按规则算准。

这个文件我后面要直接导入系统，格式一定要对——每条记录都要包含 `customer_id`、`tier`、`labels` 这三个字段，其他字段我不需要。拜托了，周末前搞定就行。
