Hey, 我是运营的 Linda。这两天我们看了季度报表，发现客户分层有点滞后——有些消费大户活动频率反而很低，而几个低消费客户却异常活跃。我想根据最新的客户消费数据和活动记录，重新给客户打标签。

规则是这样的：
- 如果客户季度消费超过 8000 美元，而且最近 60 天内活跃过，同时风险等级为低，那么他们应该升级到“gold”层级。
- 如果客户季度消费低于 3000 美元，或者超过 180 天没有活动，又或者风险等级为高，那么他们应该降级到“bronze”层级。
- 其他客户保持现有层级不变。

客户档案在 `data/customers/customers.json`，消费数据在 `data/logs/consumption_logs.json`，活动记录在 `data/logs/activity_logs.json`。请把每个客户的 ID 和新的层级写到 `ops/customer_tier_update.json`，格式类似 `[{"customer_id":"...","new_tier":"..."}]`。要包含所有现有客户，别漏了。

辛苦了，尽快给我结果。
