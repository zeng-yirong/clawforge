嘿，我是 Keiko，市场情报。我们在赶 Q2 技术方案追踪报告，急需所有涉及 **HelioSync Edge Inference Fabric** 的文档线索。我把原始数据丢在 `data/` 下了 —— 报告在 `reports/`，演示文稿在 `presentations/`，媒体样本在 `media_samples/`。你帮我翻一翻，把每个明确指向这个解决方案的文档挑出来：看 `solution_aliases` 字段里有没有包含 *HelioSync Edge Inference Fabric* 的。对了，有些旧版我已经标了 `obsolete` 标签（在 `tags` 里），那些不要，咱们只看最新的有效版本。  

每个文档的 ID（`report_id`、`presentation_id` 或 `sample_id`）和它的 `summary` 摘出来，整理成一个 JSON 列表，放到 `ops/clue_list.json` 里。列表里每个对象两个字段：`id`（字符串）和 `clue`（字符串）。弄好叫我，今晚就要用。
