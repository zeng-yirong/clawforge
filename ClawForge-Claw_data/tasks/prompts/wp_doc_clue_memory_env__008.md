Hey, morning! We're at a critical junction for the HelioSync Edge Inference Fabric – the Q2 signal trace is due tomorrow. I've pulled the latest document snapshots into the workspace: all reports, presentations, media samples, and the matching notes.  

Here's the deal: we need a consolidated clue list for every piece of content that references **HelioSync Edge Inference Fabric**. I don't want the raw files; I want a clean, structured JSON that captures:  
- Which **document type** it came from (report, presentation, or media_sample)  
- Its **unique document identifier**  
- The **clue bullets** that the matching notes associate with that solution  

You'll find the matching guidelines in `data/attachments/solution_matching_notes.md` – it breaks down clues per solution. Only pull the clues that belong to HelioSync Edge Inference Fabric.  

Drop the final list in `ops/clue_list.json`. Make sure it's valid JSON and every entry has exactly those three fields. No extra fluff.  

Oh, and double-check that you're not picking up content for other solutions – we've been testing multiple technology tracks in the same library. I need clean signal, not noise. Get it done and I'll feed it straight into the briefing deck.
