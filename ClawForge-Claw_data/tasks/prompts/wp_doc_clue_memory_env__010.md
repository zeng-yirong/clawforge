嘿，芮亚那边刚刚发来紧急消息：竞争对手明天就要发布基于“HelioSync Edge Inference Fabric”的新品。我们得赶在今天下班前把所有内部相关线索整理出来，给战略组做对标分析。

之前几轮调研散落在不同文件里——`data/reports/` 下的行业快报、`data/presentations/` 里的技术演示、还有几个 `data/media_samples/` 里的媒体转录。芮亚的团队在 `data/attachments/` 里留了一份匹配说明文件，叫 `solution_matching_notes.md`，你可以先翻翻，理解清楚我们要追的是哪个确切的名字。

注意，内部文件里出现过几个长得像的变体，比如“HelioSync Edge”或“HelioSync Edge Inference Platform”，但这次只要完全匹配“HelioSync Edge Inference Fabric”的记录。把每个匹配文档的 ID、标题、以及它是来自报告、演示还是媒体样本，整理成一个 JSON 数组，放到 `ops/target_clues.json` 里。别拖，下班前我要交出去。
