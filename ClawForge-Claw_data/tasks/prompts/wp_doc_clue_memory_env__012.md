Aisha 刚刚把 Q2 的技术信号追踪包扔进了 `data/` 目录。她说：

“嘿，帮把手！市场部催着要一份明确的线索汇总——关于那个‘HelioSync Edge Inference Fabric’方案，到底哪些文档真的提到了它？我把最新的报告、演示稿和媒体样本都按原有结构放在 `data/` 下了，你翻一遍，把所有明确出现这个全名的文档都找出来。

每个文档我需要四样东西：
- 它的唯一编号（就是 ID）
- 标题
- 类型（报告/演示/媒体）
- 一句核心引用——从文档里摘一句最能说明这个方案价值或用途的话

整理成 `ops/clue_list.json` 放好，格式简单点，一个数组就行。我回头直接读。”

P.S. 别管那些文件夹里其他零碎文件，只盯 `data/` 下这三类 JSON。
