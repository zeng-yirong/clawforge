嘿，我是Rhea，市场情报组的。Q2我们在主推**HelioSync Edge Inference Fabric**这个方案，之前大家零散收集了一些材料，都丢在工作区的 `data/reports/`、`data/presentations/` 和 `data/media_samples/` 下面了，另外 `data/attachments.json` 里有一份匹配指南可以帮你确认判定标准。

我需要你把这三堆材料翻一遍，找出所有**明确提及“HelioSync Edge Inference Fabric”（注意大小写）**的文档。每找到一份，就记下它的文档ID（比如 `report_id`、`presentation_id`、`sample_id` 这种标识）和一句能证明它提到了该方案的原文引用。把结果整理成一个JSON数组，每个元素包含 `document_id` 和 `clue` 两个字段，存到 `clues/clue_list.json` 里。我下午的汇报要用，拜托啦！
