嘿，是你在值班吗？太好了，我这边快炸了。产品组上周刚发布了那个“QuantumEdge Neural Accelerator”方案，市场部催着要一份快速情报汇总，但我手头的资料全乱套了。IT把最近一轮的报告、演示稿和媒体转载都丢在 `reports/`、`presentations/` 和 `media_samples/` 三个文件夹里，有些文件命名还不规范，甚至还有历史版本混在里面。我没时间一个一个翻，你帮我把所有明确提到“QuantumEdge Neural Accelerator”这个方案的文档找出来——注意是完整的名字，不要那种拼写错误或者只写一半的。每个文档记下它的ID（就是那个 `_id` 字段）、类型（是报告、演示还是媒体样本），还有文档里跟这个方案相关的关键描述句子，摘一两句就行。全部整理好放进 `ops/collected_clues.json`，格式就用简单的 JSON 列表，每个元素带 `doc_id`、`doc_type` 和 `clue`。做完告诉我一声，我直接拿去给老板交差。
