Hey AI, 我这边有个紧急的竞品追踪任务。我们市场情报团队正在盯“HelioSync Edge Inference Fabric”这个技术方案，但相关的资料散落在好几个地方——数据目录 `data/` 下面有 reports、presentations 和 media_samples 三个子文件夹，每个里面都有对应的 JSON 文件。我需要你帮我找出所有**明确**提到了这个完整技术名称的文档，并记录下它们的标识和核心摘要。注意，有些文档里只写了“HelioSync Edge”或者别的近似词，那些不算，我只要精准命中“HelioSync Edge Inference Fabric”的。麻烦你整理成一个整洁的 JSON 列表，每个元素包含两个字段：`id` 代表文档标识，`clue` 代表该文档的核心摘要（直接抄原文的 summary 就行）。最后把这个列表存到 `ops/final_clues.json` 里。时间紧，别被那些一眼看上去像但实际不匹配的干扰项迷惑了。谢谢！
