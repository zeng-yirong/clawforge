嗨，我是市场洞察组的Rhea。我们刚接到战略部的紧急需求：他们想快速掌握“HelioSync Edge Inference Fabric”这一技术方案的行业动态，下周就要汇报用。我记得我们内部的调研报告、合作伙伴演示和之前录的几期媒体采访里都提到过它，但散落在不同的文档库里。

你能不能帮我从 `data/` 下面把相关的文档翻出来？具体来说：
- 去 `data/reports/` 看看那些行业报告，凡是提到这个方案的，把报告ID和摘要里最能说明问题的一句话记下来。
- 再去 `data/presentations/` 查演示文稿，同样按方案匹配，记下演示ID和关键摘要。
- 最后翻一翻 `data/media_samples/` 里的采访稿，把涉及该方案的样本ID和摘要提取出来。

我需要把它们按报告、演示、媒体三类归整到一个文件里，存到 `ops/clue_list.json`。摘要直接抄原文就行，但最好把多余的空格和换行清理干净，免得领导说我们不专业。

注意：有些材料可能名字很像但压根儿不是我们要的方案（比如只写了“HelioSync Edge”或者“HelioSync Fabric”），别搞混了。另外，那些重复录入或者明显过时的版本可以先忽略，我们只要最新的有效记录。

辛苦你了，明早之前搞定就行。
