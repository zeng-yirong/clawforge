Rhea 在 Slack 上给你发了一条消息：

“嘿，帮我看一下那个 HelioSync Edge Inference Fabric 的追踪。我把所有报告、演示卡片和媒体采样的元数据都扔在 `data/` 底下了——reports、presentations、media_samples 各自一个子文件夹，里面各有一个 JSON。附件里还有一份匹配指南，应该有点用。我需要你把所有明确指向这个技术方案的文档线索整理出来。每个目标文档里应该都夹着一个格式化的线索子弹，像 `CLUE_BULLET: xxx` 这种。请按文档 ID 分组，把所有子弹收集起来，放到 `ops/clue_list.json` 里。我只要那些真正匹配的——别把近似名称的也算进来，不然下游会乱套。搞定了踢我一下，谢啦！”

> 提示：工作区里的数据已经就绪，你只需要产出 `ops/clue_list.json`。
