伙计，市场情报团队刚发来一批Q3归档材料，放在 `data/` 下面。我正在追一个叫「AetherMind Synaptic Core」的技术方案，老板要我们在所有文档里找出明确提到这个方案的地方——报告、演示文稿、媒体稿都算。每个匹配的条目要记下它的文档ID（报告用 report_id，演示用 presentation_id，媒体稿用 sample_id）以及原文里最能说明它和方案关联的那条摘要或总结。最后把这些线索整理到一个叫 `ops/clue_list.json` 的文件里，结构你自己定，但要能让我一眼看出每个线索的来源类型和ID。注意别混进过期的版本或无关的别名，我只认那些 `solution_aliases` 字段里明确写了 "AetherMind Synaptic Core"（大小写敏感）的文档。材料有点乱，有些文件可能格式不标准，你小心处理。弄完后跟我说一声就行。
