嘿，我刚把Q2的技术追踪资料包解压到了工作区的 `data/` 下。下周要见客户，他们特别关心我们怎么跟踪“HelioSync Edge Inference Fabric”这个方案。我需要你把这个技术的所有相关线索整理出来——包括正式报告、演示文稿和媒体样本，只要文档里明确提到了这个技术或者它的旧称“HelioSync AI”都算。对了，有一些早期内部草稿（状态标记为 draft）可别混进去，我只认最终发布版。

每个匹配的文档正文里都有一行以 `# Clue:` 开头的关键线索，把它摘出来。把所有找到的线索汇总成一个 JSON 列表，写入 `ops/clue_list.json`。具体的字段结构可以参考 `data/attachments/solution_matching_notes.md` 里的说明，那里我放了格式示例。

时间紧迫，弄完喊我一声就行。
