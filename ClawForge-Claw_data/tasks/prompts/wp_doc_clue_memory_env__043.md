Rhea (Signal Research Lead) here – quick one. I’m stitching together the Q2 2026 tech‑signal trace for our new product family, and the core phrase I’m hunting is **HelioSync Edge Inference Fabric**.  

Could you dig through the library dumps under `data/`? I need every report, presentation, and media sample that explicitly lists that exact solution alias in their metadata. For each hit, jot down the document’s ID, its title, a short summary (the `summary` field is fine), and what type it is (report / presentation / media_sample).  

Toss the compiled list into a file at `ops/clue_list.json` so I can feed it straight into the temporary record pipeline. I don’t need attachments or contacts – just the three document types I mentioned.  

Watch out: some docs mention a similar name but with typos or casing; I only want the exact "HelioSync Edge Inference Fabric" – case‑sensitive match against the `solution_aliases` array.  

Thanks!  

P.S. The `ops/` folder already exists; just drop the JSON there.
