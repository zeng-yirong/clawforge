> **From:** Keiko Han <keiko.han@northstar.example.com>  
> **Subject:** HelioSync Edge Inference Fabric – clue trace needed  
>  
> Hey,  
>  
> Our partner marketing team is building a competitive intelligence brief around *HelioSync Edge Inference Fabric*. I’ve pulled together the latest reports, presentations, and media samples we have on file. They’re sitting under `data/reports/`, `data/presentations/`, and `data/media_samples/` — each collection has its own JSON index.  
>  
> I need you to comb through those documents and identify every single one that mentions our target solution (full name: `HelioSync Edge Inference Fabric`). For each match, grab the document’s ID and the plain‑text summary you see in its index entry. Then bundle everything into a clean clue list.  
>  
> Please put the result in `ops/clue_list.json` as a JSON object that lists the solution name and an array of clues — each clue should show where it came from (report, presentation, or media_sample) and the ID plus summary.  
>  
> Don’t overthink it — I just need the accurate set of leads. No need to dig into full file content; the index summaries are enough.  
>  
> Thanks,  
> Keiko
