晚上好！我是Rhea，市场情报组的。

刚才客户成功团队发来紧急请求：有一家关键客户正在评估 **HelioSync Edge Inference Fabric**，需要我们立刻提供所有现有的线索文档——包括相关报告、演示文稿、媒体样本——以及每份文档里最有价值的引用点。他们说越快越好，最好明天一早就能给到对方方案架构师。

我手头的工作区已经放好了所有原始数据，就是 `data/` 下面那几个目录：`reports/`、`presentations/`、`media_samples/`。另外，`data/attachments/` 里有一份我们之前整理过的 **Solution Matching Notes**，里面写了怎么判断一篇文档是否和某个方案匹配，以及最后汇总的格式要求。麻烦你先读一下那份指南，然后按它的指引帮我把符合条件的文档找出来，把结果存成一个 `clue_list.json` 放到工作区根目录。

注意：数据里可能混了一些旧版本和无关的东西，请严格按指南里的规则来筛。我需要的就是一份干净、准确的线索清单，直接能发给客户。辛苦你了！

对了，如果你看到任何 `data/obsolete/`、`backup/` 之类的东西，忽略就好，那些是历史存档，不在这次的范围里。
