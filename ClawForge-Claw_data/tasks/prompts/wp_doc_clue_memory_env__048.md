嘿，我刚刚从客户那边回来，他们想部署 **HelioSync Edge Inference Fabric**，但我们之前的资料里到底有没有具体的落地案例？我翻了一下我们工作区里的几个数据源：

- `data/reports/reports.json`  
- `data/presentations/presentations.json`  
- `data/media_samples/media_samples.json`  

还有 `data/attachments.json` 里有一个 `solution_matching_notes.md`，里面应该提到了这个方案的全称别名。

我需要你帮我把所有提到 **HelioSync Edge Inference Fabric** 的文档都找出来。每个匹配文档里应该都有一行以 `CLUE:` 开头的关键线索（就在文本内容里），请你把这些线索连同对应的文档 ID 一起整理出来，放到 `ops/clue_list.json` 里。格式你定，只要我能一眼看出哪个文档对应哪个线索就行。我等着拿这个去给客户汇报，拜托啦！
