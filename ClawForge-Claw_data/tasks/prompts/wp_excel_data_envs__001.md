Subject: 销售数据整理请求

Hi 数据分析助手，

我们销售系统最新的原始数据我已经放在工作区的 `data/raw_sales.csv` 里了。因为之前手动合并各分支的报表，我发现里面有不少完全一模一样的重复行，还有一些交易的金额是负数或直接空着，这明显是录入错误。我需要你帮我清理一下：

1. 先把那些完全重复的行去掉，只保留一份。
2. 把金额有问题（负数或空白）的整行删除。
3. 对于剩下的有效交易，我想知道每个产品类别的平均折扣后价格。折扣后价格的计算是：原价乘以 (1 - 折扣百分比/100)。折扣列的数值是整数。
4. 最后请把每个类别的平均值存成一个 JSON 文件，直接放在工作区根目录，文件名叫 `products_avg.json`。格式类似：
   ```json
   {"Electronics": 790.0, "Clothing": 63.75, ...}
   
数据量不大，麻烦你尽快处理，谢谢！

— 销售部老李
