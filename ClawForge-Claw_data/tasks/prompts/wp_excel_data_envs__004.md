Hi，我是销售部的李华。

刚把上季度的原始销售数据导出来了，放在 `raw_data/sales.csv` 里，另外还有一份产品价目表 `raw_data/products.csv`。我粗略扫了一眼，发现有些订单行完全重复了，还有些行的金额是空的。麻烦你对照价目表把缺失的金额补上，去掉重复的条目，然后按产品统计一下平均每个订单卖了多少。结果放在 `results/averages.json` 里，我下午开会要用。多谢！
