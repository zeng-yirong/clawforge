Hi，我是销售部的张伟。最近要准备季度报告，但原始数据里有很多重复记录，还有些客户名字缺失了。我这边有 `data/sales_raw.csv` 和 `data/accounts.csv`，你能帮我清洗一下数据吗？具体来说：去掉完全重复的行，把缺失的客户名字从 `accounts.csv` 里补上，如果销售额缺失就当作0。然后根据清洗后的数据，按 `region` 和月份做汇总，统计每个地区每个月的总销售额和总订单数，生成一个汇总表。另外，再算一下每个月的平均订单金额（总销售额除以订单数），单独存成一个文件。麻烦把清洗后的数据保存为 `output/cleaned_sales.csv`，汇总表保存为 `output/region_monthly_summary.csv`，平均订单金额保存为 `output/monthly_avg_order.json`。谢谢！
