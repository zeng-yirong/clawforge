Hey, quick help needed. I left this quarter's raw sales dump in `data/raw_sales_2024.csv` – you know, the usual mess. Some rows are exact duplicates (probably a copy-paste error), and a few have bogus amounts (negative or blank). I need you to clean it up and then figure out the average order value for each product. Only consider rows that are valid (no negative or blank amounts, no full duplicates). Put the result in `output/average_order_values.json` with product IDs as keys and the averages as values. I trust you to get it right – just the clean numbers, please.
