嘿，我是销售部的张总。早上实习生发来一份销售数据，放在 `data/raw_sales_2024.csv` 里，我大致扫了一眼，问题不少——有几行明显是重复的，还有空行、负数金额甚至数量为零的垃圾记录。旁边还有个 `data/sales_backup_old.csv`，那是去年备份的，别碰它。

我需要你帮我清洗这份数据，然后回答几个关键问题，把结果写到一个叫 `ops/report.json` 的文件里，格式如下：

{
  "total_revenue": 总营收（金额之和）,
  "average_order_value": 平均每单金额,
  "unique_customers": 不同客户数,
  "top_product": "销售额最高的产品名称"
}
**注意**：只基于 `data/raw_sales_2024.csv` 处理，去掉任何有问题的行（比如空行、金额<=0、数量<=0、完全重复的行），然后计算。别问我要不要保留备份，就按我上面说的干。结果尽快给我。
