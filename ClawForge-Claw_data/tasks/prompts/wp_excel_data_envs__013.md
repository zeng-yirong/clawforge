Hey team,

刚从销售系统导出了一批原始数据，放在 `raw_data/sales_raw.csv` 里。我扫了一眼，发现不少问题：有重复行、几行产品的分类字段是空的，还有金额为负或0的脏记录（这明显不对）。我赶着给区域经理看按地区汇总的销售情况，你帮我快速清理一下：

- 把完全相同的重复行去掉
- 根据 `data/product_categories.csv` 把缺失的产品分类补上
- 剔除金额 <= 0 的无效订单
- 最后按区域（region）统计销售额，并算出总销售额、订单总数和平均订单金额

结果按下面格式整理到 `ops/report.json`，我开会直接展示。别忘了在报告里注明是否处理过缺失分类。

拜托了，周一前要。
