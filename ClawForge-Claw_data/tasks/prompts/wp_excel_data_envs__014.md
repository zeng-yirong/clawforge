嗨，数据分析师你好！

我是销售部的李明。我们上个月的销售原始数据非常混乱，我把它放在了 `data/sales_raw.csv` 里。我看了一下，里面有不少问题：  
- 有的行完全重复，看起来是录入系统时卡顿导致的。  
- 有些订单的地区（region）是空的，根本没法归类。  
- 还有几条记录里销售额或数量是负数，明显是错误数据。  

麻烦你帮我处理一下：  
1. 把数据清洗干净（去掉重复行、剔除地区缺失或金额/数量非正的记录）。  
2. 然后按地区（region）汇总一下**净销售额**。净销售额的计算公式是：`销售额 × 数量 × (1 - 折扣百分比 / 100)`。  
3. 把最终结果输出到 `report/region_summary.csv`，文件格式：两列，第一列是 `region`，第二列是 `net_sales`，每条记录一个地区，数值保留两位小数。  

谢谢你！期待你的结果。
