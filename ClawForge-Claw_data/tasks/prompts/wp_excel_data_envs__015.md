老张（销售部）匆匆跑来：“昨晚我导了一份销售明细，放在 `data/raw_data/sales_raw.csv` 里。但是数据有点脏——我怀疑有人不小心复制粘贴了重复行，还发现好几条记录的金额字段是空的。你帮我拾掇拾掇，然后告诉我每个产品类别（category）的平均订单金额是多少。结果放到 `ops/average_order.json` 里就行，格式就写成 `{"类别": 平均值}` 这种。那些重复的行别记两次，金额空的行也别算进去，我要的是干净的数据。麻烦你了！”
