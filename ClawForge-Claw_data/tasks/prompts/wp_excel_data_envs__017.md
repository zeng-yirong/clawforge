嘿，小李，有空吗？我这边刚收到销售团队发来的上个月原始订单数据，就放在工作区的 `data/sales_raw.csv` 里。我粗看了一眼，发现有些交易编号重复了，而且好几行金额还是空的——这帮人录入的时候太粗心了。你帮我处理一下，把数据弄干净。

重复的记录就按日期保留最新的那条吧，毕竟后面更新的可能才是最终版本。那些缺失的销售金额，你用同一种产品的平均销售额（算的时候只算非空的）补上，别让老板看到一堆空值。

处理完之后，我需要一个按区域（region）汇总的表格，看看每个区域的平均订单金额是多少。另外，清理后的完整数据也单独存一份，方便我后续核验。结果都放在 `output/` 文件夹里，清理后的数据叫 `sales_clean.csv`，汇总结果叫 `regional_summary.json`。

哦对了，别动其他文件，那里面有些旧备份和客户信息我还在用。辛苦了！
