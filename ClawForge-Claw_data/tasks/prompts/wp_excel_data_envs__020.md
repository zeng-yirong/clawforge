嘿，急！刚拿到Q3的销售数据，但文件里一堆重复和缺失，简直没法看。我已经把原始数据放在 `data/raw/sales_raw.csv` 了，帮我把这堆烂摊子收拾利索：

- 那些完全一模一样的重复行（所有字段都相同）删掉，留一条就行。
- 销售额（sales_amount）为空的行，改成 0 元，别让它空着。
- 我怀疑一笔交易 ID 为 `TXN-000999` 的记录有问题，你在清理完前面的脏数据后，把它从数据集里摘出来，单独存一份。

最终的成果物我要两个：
1. 把清理完（去重、补缺、移除可疑记录）的数据，按区域（region）汇总总销售额，写到 `reports/region_summary.json` 里，格式比如 `{"East": 580, "West": 450, "North": 370}`。
2. 那条被我怀疑的交易记录，把它的完整字段保存到 `reports/suspicious.json` 中。

动作快点，这些报表我下午开会要用！
