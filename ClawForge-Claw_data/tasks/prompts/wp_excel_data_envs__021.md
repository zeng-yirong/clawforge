嗨，我是销售部的小王。这周汇总的销售原始数据简直一团糟！我在 `data/raw_data/sales_raw.csv` 里放的销售记录有大量重复行（完全一样的记录出现了好几次），而且客户名字也缺了不少。好在总部的客户档案是干净的，就在 `data/accounts.csv` 里，有客户 ID 和显示名称的对应关系。老大让我赶紧整理出一份干净的数据，然后按客户算一算每个人的平均订单金额，他要看看哪些客户贡献稳定。你帮我把清洗后的数据写到 `cleaned_sales.csv` 里（去掉那些完全重复的行，并且把缺失的客户名称补对），同时把每个客户的平均订单金额、订单总数和总销售额汇总到 `customer_average_order.json` 里，用客户 ID 当 key。拜托了，明天晨会就要用！
