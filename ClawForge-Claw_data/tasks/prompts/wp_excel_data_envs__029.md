嗨，小李，

我刚从销售系统导出了上个月的交易数据，放在了 `data/sales_raw.csv` 里。不过数据一团糟——我瞄了一眼，发现同一个交易 ID 出现了好几次，肯定是导入时重复了；还有些订单金额居然是负数，明显是误录；另外不少销售员的姓名是空的，没法直接出报表。

你帮我彻底清理一下，规则你自己应该懂的：重复的交易就按日期留最新的那条，负数的垃圾记录直接扔掉，没名字的销售员统一标成“未分配”。然后把清洗后的数据写成 `cleaned_sales.csv`，字段保持原样；另外再给我一个 `summary.json`，里面写上清洗后总共多少笔、总销售额是多少、平均每笔订单金额是多少。我下午汇报要用，谢啦！

老张
2025-03-21
