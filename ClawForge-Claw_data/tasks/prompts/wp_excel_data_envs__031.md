嘿，数据分析组的小张，早上好！

昨天销售部的老王又来找我抱怨了，说我们季度报表里的数据乱七八糟的。我让IT把原始销售数据导出放在了 `data/sales_raw.csv` 里，你帮我看看。

几个问题：
1. 好像有完全重复的交易记录混在里面，只留一份就好，别让报表里出现双倍的数字。
2. 还有几行缺了销售额或缺了产品名，这样算出来肯定不准。缺的销售额你就按0处理吧，产品名写成 "Unknown" 就行，至少别让公式报错。
3. 处理完之后，我想看看每个产品类别的总销售额，按从高到低排个序，结果放到 `analysis/summary.json` 里，格式我就用最常见的 `{"category": total}` 就行。
4. 清理后的完整明细也保存一份，叫 `analysis/cleaned_sales.csv`，方便以后查账。

你弄好后把两个文件放在 `analysis/` 目录下就行，我这边直接拿去用。其他无关的文件不用管，可能在 `data/` 目录下还有备份啥的，但那个是旧的，你别搞混了。

先谢啦！有问题随时找我。
