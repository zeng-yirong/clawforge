小张，帮我看一下销售数据。昨天系统导出的时候出了点问题，我先把最新的文件放到了 `data/sales_data.csv` 里，其他的旧备份和测试文件也在那个目录下，你千万别搞混。  

这里面有好几笔重复的订单（同样的交易号、产品、数量、金额、折扣全一样），还有一些订单的折扣字段是空着的，应该是系统漏填了，你就按没有折扣处理。  

你帮我算出每个月的实际收入（收入 = 销售额 × 数量 ×（1 - 折扣百分比/100），空折扣当0%），然后按月份汇总，结果存到 `output/monthly_revenue.json` 里，格式用 `{"月份": 金额}`，金额保留两位小数就行。我着急要用，拜托了！  

对了，只处理 `sales_data.csv` 这个文件，其他文件别管。
