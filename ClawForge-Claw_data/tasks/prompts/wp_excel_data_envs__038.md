嗨，我是销售部的张明。最近财务那边快疯了——对账发现很多订单被重复提报，系统里的交易流水乱成一团。我手头有一份原始数据，就在你工作区的 `data/raw_data/sales_raw.csv` 里，里面每一行是一条订单记录，`transaction_id` 是唯一订单号，但奇怪的是有些 ID 出现了好几次。麻烦你帮我把所有**重复的订单 ID**（也就是 `transaction_id` 出现次数大于 1 的那些）找出来，整理好放到 `ops/duplicates.json` 里。格式就按这个来：`{"duplicate_ids": ["ID1", "ID2", ...]}`，ID 按照字母顺序排好，我直接丢给 IT 去清洗。千万别漏了哪个，要不然又得重新折腾一遍。多谢！
