牛哥，你那边能不能帮我处理一下销售数据？我这边急着跟总部汇报季度业绩，结果从系统导出来的原始数据乱七八糟的——有重复的订单，还有几行金额直接是空的。我把数据丢在 `data/sales_raw.csv` 里了，你看一眼就明白了。

我需要一份按月份聚合的销售报表，包含每个月的总销售额、订单总数和平均每单金额。报表要清晰、准确，别给我一堆垃圾数字。清理规则你自己拿捏，但结果必须靠谱。报表放到 `reports/` 目录下，文件名就叫 `monthly_summary.json`，我直接用你的结果做PPT。

三天后我要拿去讲，别掉链子啊。
