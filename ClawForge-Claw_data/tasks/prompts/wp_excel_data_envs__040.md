嘿，小张，销售部刚扔过来一堆原始数据，乱得跟麻团似的！你到 `data/` 目录下翻翻，有个叫 `sales_raw.csv` 的主文件，里面订单记录有重复的整行、还有产品名称空着的。旁边有个 `product_mapping.csv` 是小李之前手动维护的型号对照表，能补上缺的名字。

你先帮我把重复的行彻底清掉，一条都不留；再对照映射表把空的产品名称填好。做完这些，按每个产品算一下平均销售额，四舍五入到两位小数，弄成一个整洁的 CSV 扔到 `report/average_order.csv` 里。我要的列：product_id、product_name、avg_sales_amount。别漏了，也别多塞没用的东西。

对了，注意别把那些历史存档的旧文件（像 `old_sales.csv` 之类）给误改了，我只要那份最新的主文件。搞利索点，下午开会要用。
