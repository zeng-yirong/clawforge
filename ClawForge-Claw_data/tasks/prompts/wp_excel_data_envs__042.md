Hi 数据分析团队，

我是销售部的Jason。最近各区提交的销售数据都堆在 `raw_data/` 文件夹里了，我大致看了一下，问题不少：

- 有些订单重复出现了（比如同一个订单号在多个文件里出现）。
- 还有几行的订货数量<=0，这种肯定是无效记录，不能要。
- 部分客户的姓名是空的，不过客户ID都在，你们可以去根目录的 `accounts.csv` 里查一下补全，如果查不到就标成 "Unknown"。

我需要你们帮我整理出一份干净的完整数据表，放在 `analysis/` 目录下，CSV 格式，把无效的去掉、重复的只留一条、缺失的客户名填好。另外，再按产品汇总一下总销售额和总销量，也放到 `analysis/` 下，JSON 格式，方便我直接导入 BI 系统。

拜托了，周三前要出结果，谢谢！
