嘿，小王，刚销售部那边又闹腾了，说上个月的渠道数据乱得没法看。我让小李从系统拉了一份快照放在 `data/raw_sales/` 下面，但你看文件名，有 `v1`、`v2` 好几个版本，还有几个带 `backup` 的破文件。他们自己人往里面手动补过一些记录，搞得一堆重复的、缺字段的，甚至还有产品 ID 打错的。

咱也别管哪个版本是“最终版”了，你帮我把真正的有效数据理清楚，然后给我算一份 **每个渠道（channel）的平均每笔交易额（average order amount）**。这活儿我就两点要求：

1. **干净** — 重复的行只留一条；`sales_amount` 或 `quantity` 为空或者不是数字的整行扔掉；`transaction_id` 格式不对（比如不是 TXN- 开头加6位数字）也别留着碍眼。
2. **准** — 平均每笔交易额 = 该渠道下所有有效交易的 `sales_amount` 总和 / 有效交易笔数。结果保留两位小数，四舍五入。

弄完把它存成 `ops/channel_avg.json`，结构就简单点：
{
  "channel_avg": {
    "渠道名1": 1234.56,
    "渠道名2": 789.01
  }
}
别整那些花里胡哨的，干完喊我一声就行。
