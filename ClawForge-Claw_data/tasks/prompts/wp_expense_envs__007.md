嘿，我是财务部的老张。上周华东区销售团队一次上海出差，报销单把财务系统冲垮了，我现在得靠你先做一轮人工审核——用你本地的数据就行。

我让实习生把原始信息整理到 `data/trip_info.json` 里了，里面有出差编号、员工等级、目的地和天数。公司标准级别政策（就那个 `standard` 的）放在 `data/travel_policies.json`，不过旧版本也堆在里面，你注意别拿错。消费记录在 `data/consumption_records.json`，但那个文件混杂着好几个 trip 的单子，我这个是 `trip_alpha` 的，别把别人的混进来算。

你帮我把实际开销跟政策限额对一下，找出超支最多的那个费用类别，算出来具体超了多少，然后写到 `ops/overbudget.json` 里。格式你看着办，但要让我一眼看懂是哪个类别超了多少。搞完喊我一声，我等着核验后走审批。
