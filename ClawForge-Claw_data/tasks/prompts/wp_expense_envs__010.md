嘿，我是财务部的老李。上周张三去深圳出差，回来交的报销单被系统拦截了，提示预算超支。我人工核过发票，都是真实消费，但系统就是不给过。领导让我查清楚到底是哪些费用类别花超了，好跟审批人解释。

所有原始数据都在 `data/` 目录下：员工信息、出差申请单、消费明细、现行差旅政策。你顺着这些材料，帮我把张三这次出差（trip_info 里标明了是哪一次）的预算和实际对一对，找出所有超支的类别，整理成一个 JSON 文件放到 `ops/` 下，文件名就叫 `budget_analysis.json`。我只要超支的那几项，每项写明实际花了多少、预算多少、超了多少，别掺杂其他没超支的，多谢。
