哈喽，我是财务部的小刘。上周市场部小李去北京出了趟短差，回来报销单填得我头大——光住宿就比政策贵了小两百，还有几笔餐饮费日期对不上。我把公司旅差政策文件丢在了 `data/` 下面，按员工级别分文件夹存的（有一份旧版本别用，注意文件名带 `v2` 的那个才是最新的）。小李的实际消费记录在 `records/` 目录下，按 trip ID 分文件。麻烦你帮我算一下：**按小李的级别（senior）**，这次北京出差（2天）里，哪些费用类别超了预算，每项超了多少，一共超了多少。把结果整理成一份 JSON 放到 `report/overspend.json` 里，结构大概是：按类别列出超支金额，再加一个总计字段。审计下周就来翻旧账了，越快越好！

> 忘了说：trip ID 是 `trip_LX_202310`，别找错了。
