财务部的小张刚才在钉钉上急吼吼地找我，说小明（工号EMP007）上周去北京出差三天（6月1号到3号）的报销单有问题。他把住宿、餐饮、交通的发票全塞在一个信封里，自己也没算清楚。我手头有公司最新的差旅政策文件，在 `data/travel_policies.json` 里，小明的消费记录在 `data/consumption_records.json` 里。麻烦你帮我把那些超出预算的明细揪出来，按类别整理成一个列表，放到 `ops/overbudget.json` 里。我只想要小明的数据，别把其他同事的混进来。直接给我能用的结果，越精确越好。
