辛辛苦苦跑了一趟上海，回来后财务那边炸了锅——说我报销单严重超支，要我自己查清楚再报。我明明按公司规矩来的啊！

我把出差信息扔在 `ops/trip_info.json` 里了，公司政策在 `data/travel_policies.json`，我自己的消费流水在 `data/consumption_records.json` 里面都存着。你帮我比一比，看看到底是哪个项目超了，超了多少。结果写成一份报告放到 `ops/analysis_report.json`，字段你自己看着办，但至少要把超支的项目列清楚，预算多少、实际花了多少、超了多少，还有总共的预算和实际总数。别把其他人的账算到我头上，就只分析我这一趟的。

对了，别动原始文件，我后面还要用。
