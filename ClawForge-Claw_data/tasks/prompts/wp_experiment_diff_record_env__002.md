嘿，我这边刚跑完两批 A/B 实验，batch_001 和 batch_002，数据就扔在 `data/experiments/experiment_results.csv` 里。  
我怀疑有个组的准确率波动特别大，但手头事情太多，没空一个一个算。你帮我看看，把两个批次都出现的组揪出来，算一下准确率的变化（用 batch_002 减去 batch_001），然后找出变化最大的那个组，把它和具体变化量记下来，放到 `ops/diff_result.json` 里。  
哦对了，有些组可能只在一个批次里出现过，那些就不管了，别把它们混进来。文件格式你自己定，只要我能一眼看到组名和变化值就行。谢啦！
