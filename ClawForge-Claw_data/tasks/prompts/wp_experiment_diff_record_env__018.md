嘿，刚跑完第二批实验，数据我已经扔进 `data/experiments/` 了。第一批和第二批的结果混在一个文件里，还有几个旧副本干扰。我需要你帮我算清楚：每个组在两个批次之间的核心指标到底差了多少？注意，有些行数据有问题——比如 accuracy 不在 0~1 之间、latency 是负数的，它们肯定不是有效测试，别算进去。最后把结果整理成一份干净的结构，放到 `ops/diff_record.json` 里，我拿它填报告。别给我多余的东西，我只要准确的数值。
