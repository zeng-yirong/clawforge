嗨，小王，下周要给产品团队汇报 A/B 实验进展。我周五把最近两轮实验的原始数据放到了 `experiments/batch_results.csv` 里，你帮我快速整理一下。

第一轮是 3 月 1 日跑的（batch_id = batch_20250301），第二轮是 3 月 15 日跑的（batch_id = batch_20250315）。每个批次都有三个实验组：control、variant_a、variant_b。记录里包含 accuracy、latency_ms、cost_usd 这些指标。

老板最关心准确率提升，但强调响应时间（latency）不能比原来的组恶化超过 10%。麻烦你算一算每个组的指标变化（第二轮减第一轮），然后挑出符合条件的最佳组——也就是准确率提升最大，同时延迟增长不超过 10% 的那个。把对比结果和最终推荐放到 `ops/diff_report.json` 里就行，结构你看着办，但要有每个组的差异值和最后选定的组 ID。

辛苦，周一开会前给我。
