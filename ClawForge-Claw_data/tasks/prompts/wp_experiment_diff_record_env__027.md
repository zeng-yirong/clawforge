嗨，我是实验平台的分析师艾伦。今天凌晨我跑了五个批次的对比实验，数据就扔在 `experiments/experiment_results.csv` 里了。你可能会看到旁边有一些乱七八糟的备份文件，别管它们，那都是历史遗留物。

事情是这样的——我需要一份清晰的差异报告，用来跟产品团队扯皮。报告的规则很简单：按批次出现的先后顺序，把每个组（group_id）的核心指标跟前一批次同一个组的变化量算出来。换句话说，每个批次都是拿它自己的数据跟它紧挨着的上一个批次比。第一个批次嘛，变化量就是零。

具体指标包括准确率（accuracy）、延迟（latency_ms）和成本（cost_usd），变化量用差值表示（当前减前序）。格式我习惯用 JSON，每个条目包含批次号、组名以及三个差值，按批次顺序排列，同一个批次内按组名 A、B、C 排列。把最终结果存到 `ops/diff_record.json` 里。

哦对了，那个 CSV 文件里可能夹杂着一些格式异常的行（空行、注释、列数不对之类的），你自己看着清理一下，别让垃圾数据混进去。我相信你的判断。

搞定了喊我一声，我直接拿去用。
