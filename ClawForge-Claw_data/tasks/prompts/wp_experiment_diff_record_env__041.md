陈博，实验组刚跑完两轮 benchmark，我把原始数据扔到 `experiments/experiment_results.csv` 里了。v1 和 v2 两个版本，每个版本都有 A、B、C 三个组的多次运行记录。不过听说采集程序有点抽风，个别行重复了，你帮我清一下，每个 (batch, group) 组合只留一条数据就行（重复行内容一样，随便保留一条）。

我要的是每个组的性能变化：准确率提升了多少？延迟降低了还是升高了？成本呢？正数代表变好（准确率提高、延迟下降、成本下降），负数反之。把这些差异整理成一个 JSON 文件，放到 `ops/diff_record.json`，我直接导入仪表盘。其他 batch（比如那个 `batch_old`）不用管，别把无关数据掺进来。

对了，别忘了 ops 目录可能还没建，你自己创建一下。弄好了告诉我一声。
