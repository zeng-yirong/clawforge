嘿，我刚跑完两批实验，结果都塞在 `data/experiments/experiment_results.csv` 里了。你帮我看看哪些 group 在准确率、延迟或成本上出现了明显变化——我个人认为准确率变化超过 0.05、延迟变化超过 10 毫秒、成本变化超过 0.1 美元就算有动静。把有变化的组和具体变化值记下来，放到 `ops/diff_record.json` 里就行，格式你看着办，反正我能看懂。哦对了，数据可能有点脏，有些行可能缺字段，你自己看着处理吧。
