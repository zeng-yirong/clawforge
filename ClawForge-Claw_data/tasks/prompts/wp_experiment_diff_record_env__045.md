嘿，小王，昨天我们团队跑了两轮实验，第一轮批号是 batch_001，第二轮是 batch_002，结果都存在 `data/experiments/experiment_results.csv` 里。但我翻了翻那个文件，里面好像还混了些之前测试的旧批次和乱七八糟的格式，你处理的时候留个心眼，别把脏数据算进去。

我现在急需一个清晰的差异记录，每组的三个核心指标——准确率、延迟和成本——第二批相对于第一批的变化量（也就是 batch_002 的值减去 batch_001 的值），按组 ID 列出来。整理成 JSON 文件放到 `ops/diff_record.json` 里，结构你自己定，但必须让我一眼能看清每个组的 delta。我下午开会要拿着这个给老板看，所以务必准确。谢了！
