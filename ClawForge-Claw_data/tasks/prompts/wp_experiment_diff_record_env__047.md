# 实验差异分析

嘿，我是数据科学家小明。昨天我们跑了新一轮实验 `batch_002`，和之前的 `batch_001` 对比一下效果。实验结果原始数据我放在 `data/experiments/experiment_results.csv` 了，里面记录了每个分组的准确率、延迟和成本。

我担心新版本在准确率上反而退步了。你帮我仔细看看，把 `batch_001` 和 `batch_002` 对应的每个分组（`control` 和 `variant`）在准确率上的差异（`batch_002 - batch_001`）算出来，然后写到一个叫 `ops/diff_record.json` 的文件里。我只要准确率的差异，其他指标暂时不用管。

要求结果精确到小数点后四位，格式清晰，我回头直接拿给老板看。拜托了！
