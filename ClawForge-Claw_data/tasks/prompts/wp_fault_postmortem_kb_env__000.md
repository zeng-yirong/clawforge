嘿，SRE 老兄！凌晨那场骚动你肯定还记得——支付服务突然雪崩，主库 IO 直接打满，报警器炸了一夜。我连夜把现场数据都整理到 `db_dumps/` 和 `attachments/` 里了。现在领导要一份正式的事后分析报告归档到知识库，我实在没空从头写。

你帮我跑一趟：到 `data/faults/fault_cases.json` 里找到昨晚工单系统里登记的那条记录——就是故障 ID 带 “007” 的那个倒霉蛋。把它关联的附件内容读出来，里面包含了事故根源、修复步骤这些干货。然后把这些信息揉成一份标准的事后分析报告，扔到 `ops/` 目录下，文件名叫 `postmortem_report.json`。格式你就按运维知识库的惯例来：得有出问题的是哪个服务、严重程度、根因是什么、怎么修的，还有附件的原始文本摘要。别漏字段，也别瞎编，我只认准附件里写的东西。

快点搞，我一会要直接拿来归档和复盘。
