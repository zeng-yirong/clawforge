**Subject:** 紧急！服务雪崩事后分析报告需要你出手

老兄，凌晨3点的告警终于消停了。刚才主库IO持续100%，一堆服务雪崩式挂掉。我把现场信息都拉到工作区了：

- 故障记录在 `data/faults/fault_cases.json`，有三个案例但我只关心那个最严重的。
- 相关的堆栈和调用链附件我放在 `data/attachments/` 下面，对应关系你翻一下 `data/attachments.json` 就能知道哪个附件属于哪个故障。
- 已有的知识库条目在 `data/knowledge_entries.json`，里面有一些历史修复方案，看看能不能对上。

你帮我整一份标准的事后分析报告，扔到 `ops/postmortem_report.json`。报告里需要包含：
- 这次事故的故障ID
- 根因（从附件里揪出来的准确描述）
- 修复方案（同样从附件里拿）
- 受影响的服务列表（从故障记录里提取）
- 关联的知识库条目ID（如果有的话）

注意：别把另外两个无关的故障混进来，我要的只是导致这次IO爆满的那个。报告务必简洁、准确，别加多余信息。
