凌晨3点，值班手机把我震醒了——主库 IO 飙到了 100%，业务侧已经出现大量写入超时告警。我赶紧把 `db_dumps/` 里的 InnoDB 快照和慢查询日志拖了下来，放在工作区对应的附件目录里。从慢查询日志看，有一条长时间未提交的事务，一直持有表级锁，阻塞了后面两百多个写请求。你顺着这些堆积的锁信息，把那个罪魁祸首的事务 ID 找出来，写进 `ops/kill_target.json`，我准备直接强杀它。我只要准确的 ID，其他多余信息一概不要。快点，每多一秒就多一批交易失败。
