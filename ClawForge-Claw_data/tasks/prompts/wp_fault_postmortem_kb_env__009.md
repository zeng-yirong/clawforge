凌晨3点，电话炸了。payment-svc 全线飘红，用户支付失败率飙升到 40%。我紧急拉了一份故障快照丢在 `data/faults/` 下面，里面有几个 case，还有一个附件目录 `data/attachments/` 存着当时的日志片段。那个最严重的故障（严重等级最高的那个）就是我要的。你帮我把根因、修复方案、还有牵连的上游服务都理清楚，按标准的 postmortem 格式扔到 `ops/` 里，文件名就叫 `postmortem.json`。注意附件里的日志才是真实情况，别被 case 自带的提示带偏了。
