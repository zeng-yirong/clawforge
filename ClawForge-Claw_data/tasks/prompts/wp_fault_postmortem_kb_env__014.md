🧨 紧急！昨晚支付服务又死锁了，主库 CPU 跑满，事务堆积了一堆。我把故障快照扔在了 `faults/` 下面，附件在 `attachments/` 里。

根据监控，那个 severity 标成 critical 的案例肯定是元凶。它的附件里有当时截取的日志片段，你帮我从里面把导致死锁的那个事务 ID 挖出来，然后写一份事后分析报告，放到 `postmortems/` 目录下，文件名就用故障的 ID 命名，方便归档到知识库。

报告里要包含三个东西：故障 ID、事务 ID、根因简述。事务 ID 一定要从附件里找，别瞎编。我这边等着把根因入库，出了问题你负责啊。
