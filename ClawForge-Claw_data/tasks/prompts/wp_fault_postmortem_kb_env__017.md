哥们儿，昨晚支付服务又死锁了，服务直接卡死，我盯着监控都急疯了。我临时把故障堆栈和调用链倒出来了，放在 `data/faults/` 下面；对应的日志碎片我扔在 `attachments/` 目录里，文件名跟故障ID对应。其他的告警（订单超时、库存OOM）我已经确认是误报或者别的问题，你不用管。你帮我把真正导致死锁的那个事务ID找出来，塞到 `ops/kill_target.json` 里，我准备直接强杀。就一个ID，别给我整别的乱七八糟的。快点！
