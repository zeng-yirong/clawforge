凌晨3点，主库IO飙到100%！我手忙脚乱把故障现场的快照拖下来了，所有东西都在 `data/` 里。

`data/faults/` 下有个故障案例集合，里面有个严重级别是 critical 的服务叫 payment-service，它把整个调用链都堵死了。我顺藤摸瓜找到了它的堆栈和调用链附件（也在 `data/attachments/` 里），但没时间细看。

你帮我把这件事搞完：生成一份事后分析报告，扔到 `ops/postmortem.json` 里。报告里必须写明这个故障的 ID、服务名、严重级别、根因、修复方案，还有你读了哪些附件（标题和路径）。我要的就是准确、完整，直接拿去归档。快点，这边还等着强杀呢！
