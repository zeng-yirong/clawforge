Hey，刚把昨晚的故障复盘材料拖下来了。最头疼的是那个 **订单服务超时** 的 fault，ID 是 `fault_003`，我看了下堆栈和调用链，感觉是数据库那边扛不住了。附件里有个慢查询日志（在 `data/attachments/slow_query.log`），里面应该能佐证。我需要你整合一下这些信息，写一份简洁的故障根因分析报告，放到 `ops/postmortem.json` 里，明天全组复盘要用。

报告里务必写明这个 fault 的 ID、严重级别、你判断的根因、以及怎么修。别啰嗦，关键信息到位就行。对了，`data/faults/` 下有个故障列表，里面有每个 case 的线索，你自己翻。搞完叫我一声。
