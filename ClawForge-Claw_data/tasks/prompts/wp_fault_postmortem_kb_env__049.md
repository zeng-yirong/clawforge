小王：

昨晚 payment-service 又跪了，P1 级事故，幸好是预发环境。我把现场快照撂在 `faults/fault_cases.json` 里了，故障详情、堆栈那些都在。附件日志放在 `attachments/` 文件夹下，你瞅一眼，有个事务 ID 一直在阻塞，应该就是祸根。

知识库的历史修复方案在 `kb/knowledge_entries.json`，你翻翻有没有匹配这个事务 ID 的修复记录。

帮我整一份干净的事后分析报告，放到 `ops/` 目录下，文件名用故障 ID 标出来。报告里把那个事务 ID 和对应的修复方案写清楚就行，明早我跟老大对齐。别整废话，ID 要准，方案得是知识库里扒出来的。

谢了。
