嘿，我是Calvin，技术分析组的。昨天我们从数据库导出了一堆零散的数据包，扔在了 `data/` 下面——有股票池、新闻推送、还有季报历史。今天凌晨我把 `analysts.json` 和 `stocks.json` 也补进去了，但你知道的，这些导出文件里面掺了不少其他板块的垃圾记录，还有一些过期的旧季报和老新闻充数。

我需要你帮我盯一下 **TechVentures Inc（代码 TECH）**。最近他们那个 AI 合伙新闻闹得沸沸扬扬，我想知道按最新季报的超预期幅度和基本面增速，结合当时的市场情绪，我该不该再推一把。

你从 `data/` 里把 TECH 相关的最新消息和最近一个完整季报翻出来，算一个合理的目标价，然后给个明确的动作建议——买还是持有。老规矩，结果放到 `briefs/tech_summary.json` 里，我回头直接贴到内部看板上。

别把其他股票的垃圾带进去，也别用去年那篇老新闻凑数。干净、准确，就一个 JSON 文件。
