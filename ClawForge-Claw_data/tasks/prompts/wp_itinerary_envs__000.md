Hi 团队的小伙伴，

我这边刚从几个交通供应商那拿到了最新的线路数据，放在工作区的 `data/` 目录下了，有三个文件：`flights.csv`、`trains.csv`、`buses.csv`。不过这些数据有点乱——有些是去年的旧报价（你注意看日期），还有 `trains.csv` 里有些价格是用美元标的（按汇率 1 USD = 7 CNY 换算就行）。我现在急需你帮忙找出从北京（BJS）到上海（SHA）最省钱的方式（综合所有交通方式，只看价格，不用考虑时间），然后整理成一个结果文件放到 `ops/` 目录下，文件名就叫 `best_option.json`。内容你自己决定，但至少得让我一眼看到起点、终点、交通工具类型、具体班次编号和总花费（人民币）。顺便提一句，`data/` 里还有个我随手写的 `notes.txt`，跟正经数据没关系，别被干扰了。

辛苦啦！今晚就要用。
