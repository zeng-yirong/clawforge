嘿，我是运营小张。刚接到VIP客户李总的紧急行程需求——他今天下午要从北京出发，先到南京接上一位合作伙伴，然后一起去上海参加晚宴。李总反复强调：**必须是最短总耗时**，钱不是问题，但时间一秒都不能多花。

我把交通数据扔在工作区了，有 `data/` 和 `archive/` 两个目录。`archive/` 里是之前备份的老版本，可能有些信息过时了，你挑最新最准的用。有几个常见毛病：有些路线标注了“已取消”或“已废弃”，千万别用；同一个路线可能重复出现，记得留最新那个。

我需要你：
1. 从所有有效数据中找出 **北京→南京** 和 **南京→上海** 两个路段各自最快的交通选项。
2. 把这两个路段拼成完整行程，算出 **总耗时（小时）**。
3. 把结果写进 `ops/best_route.json`，格式我不管，你自己看着办，只要让我一眼能看懂路线标识和总耗时就行。

注意：我只认最新、有效、未取消的交通记录。别给我整出什么中途去广州的奇葩路线。数据都在工作区里面，动手吧！
