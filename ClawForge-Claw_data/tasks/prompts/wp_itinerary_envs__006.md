收到你的邮件了，下周要去上海出差，时间很紧，必须尽快确定方案。  
我把手头的数据都扔到了 `data/` 下面，里面有不同交通方式的汇总，还有一些历史备份（那些过时的别管）。  
你帮我把从北京出发到上海最快的那条路线和具体交通方式找出来，整理成一个简洁的 JSON 文件放到 `ops/best_route.json` 里。  
文件里写上推荐的路由ID和交通类型，这样我直接就能订票了。  
拜托，越快越好！
