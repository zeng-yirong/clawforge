**紧急：数据库性能问题 - 需要立即处理**

凌晨 03:17，物流核心系统的数据库 CPU 飙到 100%，IO 等待严重。我已经把问题时间段的诊断快照丢在 `db_dumps/` 下了：

- `lock_info.csv` – 表级锁快照
- `active_transactions.json` – 活跃事务列表
- `slow_queries.log` – 最近 5 分钟的慢查询全量记录

监控显示大量会话在等待 `inventory` 表上的锁。我初步怀疑某个长事务占着写锁不释放，后面几百个操作全部排队。

帮我把这个害群之马的 **事务 ID** 找出来，写到 `ops/kill_target.json` 里。我马上要直接强杀它恢复业务。只要准确的 ID，不要多余的东西。

（不用碰其他文件，也别改已有数据，查出那个锁住 inventory 表的元凶即可。）
