我是前台 Alice，早上刚到工位就被老板骂了一顿——他说昨晚客户发来的紧急邮件我一封都没回！我明明记得昨晚收到了几封，但今早邮箱里全是乱七八糟的邮件，根本分不清哪些是客户的高优先级未读邮件。

我把原始数据都放在 `data/emails/` 里了，每个邮件一个 JSON 文件，联系人信息在 `data/contacts.json`。你帮我从里面把所有 **来自“Client”角色的联系人**、**重要性为“high”** 并且 **还没有读过** 的邮件找出来，然后按时间顺序整理成一个清单，放到 `ops/urgent_clients.json` 里。每个条目只要包含邮件的 `id` 和 `subject` 两个字段就行，别的东西我一概不需要。这事很急，赶紧处理！
