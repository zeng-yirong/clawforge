John Manager 在 IM 上给你发来一段话：

“嘿，新来的智能助手，我这有急事。我们和 ClientCorp 的续约正在关键节点，Alice Client（alice@clientcorp.com）昨天发了几封邮件，但我现在才拿到导出快照，放在 `data/` 目录下了。我需要你帮我做两件事：

1. 把所有 Alice 发来的、还没有被归档或标记已读的邮件找出来，针对每一封写一个回复草稿，把回复任务整理到一个清单里，放到 `ops/reply_tasks.json`。每条记录要能清楚看到是哪封邮件（邮件 ID）、回复给谁、原主题和你拟写的回复正文。

2. 除此之外，所有邮件里提到的待办事项（比如会议安排、需要提交的文档、需要准备的报告等）都提取出来，整理到 `ops/todo_tasks.json`。每条待办要注明来自哪封邮件（邮件 ID）、任务描述和截止日期（如果有）。

特别提醒：那些已经被归档或标记为已读的邮件就别管了；垃圾邮件也忽略，比如那些来自陌生域名、标题带中奖的。你看着 `data/` 里的 `contacts.json` 可以判断发件人是不是 Alice。

我只有这些数据，你尽快弄好，我后面要导入系统。”
