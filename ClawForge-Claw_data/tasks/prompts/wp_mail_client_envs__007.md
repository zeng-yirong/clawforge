嘿，兄弟，我快被这场重感冒搞趴下了！邮箱堆了一周的消息，客户在吼，同事在催，我实在扛不住了。

我的账户和联系人信息都在 `data/` 里面；邮件本体散在 `data/emails/` 里，每一个 `.json` 就是一封。我不需要你帮我回每一封，你按我平时那套规矩来就行：

- **需要我亲自回复的**：只处理我（John Manager）的账户、未读且重要等级为 `high` 的邮件。把每一封的草稿写好放在 `ops/replies.json` 里，每条至少包含邮件ID、收件人邮箱、主题和正文（正文里把我的默认签名带上）。
- **待办事项**：那些标签里标了 `action` 或 `todo` 的未读邮件，把里面的任务提取出来写到 `ops/todos.json`，每条至少注明来自哪邮件、任务标题。
- **归档清理**：已读且不重要（重要性 `low`）的邮件，还有所有垃圾邮件（标签包含 `spam`），把它们的ID逐行写在 `ops/archived.txt` 里。

只折腾我 John 一个人的邮件，别碰其他人的。产出扔 `ops/` 文件夹就行，原始文件一个都别改。干对了请你喝特调咖啡，错了我可要扣你工分。

哦，对了，`data/contacts.json` 里有联系人，`data/accounts.json` 里有我的配置和签名，你自己翻。
