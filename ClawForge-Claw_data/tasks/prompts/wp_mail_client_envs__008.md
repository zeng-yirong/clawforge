嘿，帮个大忙！刚收到老板 John 的紧急消息，他催着要下周一（3月20号）的 Q3 进度报告，我得准备材料走不开。你能帮我处理一下邮件吗？

收件箱在 `data/emails/` 里，乱糟糟的，我只要最新的那封关于 Q3 进度报告的未读邮件。看完后帮我做两件事：

1. 把这事情记下来，放到 `ops/todo.json` 里，写清楚任务标题和截止日期（老板说的是 March 20，别忘了年份）。
2. 给 John 回个信确认收到，草稿放在 `ops/reply_draft.json`，把收件人、主题和正文都写好，正文记得礼貌地表示会按时完成，署上我账户里默认的签名（在 `data/accounts.json` 里找）。

其他乱七八糟的垃圾邮件和归档邮件不用管。产出的两个文件放在 `ops/` 下就行。对了，我的账号信息在 `data/accounts.json`，联系人列表在 `data/contacts.json`，你看着用。谢啦！
