嘿，我是 Alice，HR 的。今天早上邮箱快被撑爆了，我把原始数据丢到了 `data/emails/` 里，你帮我理一理。

John Manager 发了一封关于项目进度更新的邮件，挺急的，得给他回一个。你就按标准格式写个草稿，就说我下周一下午有空，语气客气点。回完把草稿搁 `ops/replies/` 里，文件名就用那封邮件的 ID。

还有 HR 部门发来的那份新保险计划的邮件，这里头有个待办事项——需要我抽时间把关联的表格填了。你帮我把它转成一个待办条目，放到 `ops/todos/` 里，同样用邮件 ID 命名。

这两封处理完之后，就顺手把它们从收件箱挪到归档文件夹吧（把 `folder` 改成 `archived`）。其他邮件别动，特别是那些已读的、垃圾的，还有本来就归档的，一概不理。

完事了告诉我一声就行。
