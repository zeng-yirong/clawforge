嘿，AI助手！我是Lily，行政部的。今天早上我打开收件箱，发现一堆邮件堆在那儿，我头晕眼花的。我把这些原始邮件都放在了 `inbox/` 目录下，每个邮件一个JSON文件。另外，联系人信息在 `data/contacts.json` 里，账户配置在 `data/accounts.json`，不过你可能只需要联系人信息。麻烦你帮我处理一下：

1. 我需要你识别出哪些邮件是重要的、需要我尽快回复的——比如重要客户或者上级发来的紧急消息。请把这些邮件的ID整理成一个列表，放到 `ops/reply_list.json` 文件里，格式像这样：`{"reply_ids": ["id1","id2"]}`。
2. 那些垃圾广告、通知或者不需要回复的邮件，我打算归档。请把它们需要归档的邮件ID整理到 `ops/archive_list.json`，格式：`{"archive_ids": ["id3","id4"]}`。
3. 另外，有些邮件里提到了待办事项，比如“Action: ...”或者“TODO: ...”，帮我从这些需要回复的邮件里提取出来，存入 `ops/todo_items.json`，格式是一个数组，每个元素包含 `email_id` 和 `todo` 字段。比如 `[{"email_id":"id1","todo":"Send invite"}]`。

注意只处理 `inbox/` 下的JSON文件，排除无关文件。我相信你能搞定！搞定后告诉我一声，我直接拿去用。
