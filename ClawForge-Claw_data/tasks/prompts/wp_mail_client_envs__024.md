嘿，我是 Jane，邮件系统刚出了点乱子。自动化分类模块昨晚又抽风了，把好几封来自我们重要供应商和客户的高优先级邮件扔进了 `spam` 文件夹。老板刚才火急火燎地让我立刻处理，我可不想手动翻那一堆垃圾。

你帮我搞定两件事：

1. 去 `data/contacts.json` 里查一下，哪些联系人的角色是 `Vendor` 或 `Client`，他们都是我们的外部合作伙伴。然后遍历 `data/emails/` 下所有邮件，找到那些 **发件人属于上述角色**、**重要性为 high** 的邮件——不管它们现在躺在哪个文件夹里，这些邮件都应该归入 `work` 文件夹。把它们的 id、当前文件夹和推荐文件夹整理成一份清单，丢到 `ops/reclassify.json` 里。

2. 在这些被挑出来的邮件中，如果邮件正文里出现了 **`action required`**（不分大小写），说明对方在催我们干活。给我把这类邮件的 id 和 subject 提取出来，写到 `ops/todos.json` 里，格式你自己看着办，但要保证我一眼能看懂。

注意：别把内部同事的邮件也算进去，也不要被那些伪装成高优先级的垃圾邮件骗了。`data/contacts.json` 里有个叫“Lottery Scam”的家伙，他发的任何邮件都别碰。

所有产出文件都放在 `ops/` 目录下，我相信你的判断。
