嘿，我是经理 John，下周要休年假，手头一堆积压的紧急邮件还没处理，真头疼。我把最新的邮件快照都放在了 `data/emails/` 下，每个邮件一个单独的 JSON 文件，发件人信息在 `data/contacts.json` 里，账户设置啥的在 `data/accounts.json`。你帮我看看，那些标了 **high** 重要性而且还没读的邮件，肯定是火烧眉毛的事。

我需要你帮我做三件事，结果都扔到 `ops/` 目录下：

1. 把这些紧急未读邮件全部转成待办事项，写进 `ops/todos.json` —— 每个待办要有个标题（用邮件主题就行），再写一段简要说明（可以用邮件正文的开头部分，别太长）。
2. 给每位发件人回一封确认收到的回复草稿，存到 `ops/replies.json` —— 每封回复要写清楚收件人、主题（可以复用原主题前面加个 "Re: "），正文就统一写 "Thanks for your email. I will get back to you soon."（别改字，就这句）。
3. 最后把处理过的邮件 ID 列出来，写到 `ops/processed.json`。

文件夹 `ops/` 我已经建好了，但里面是空的，你直接往里放文件就行。再啰嗦一句：只动那些 **importance 为 high 且 has_read 为 false** 的邮件，别把其他邮件拉进来。搞定了跟我说一声，我检查一下就去休假。谢啦！
