嘿，我这边刚拿到IT导出的邮件快照，全部放在 `data/emails/` 里了，联系人表在 `data/contacts.json`。帮我快速扫一眼：找出所有来自咱们HR部门的未读邮件，重要性为 high 或 normal 的都算。已经归档的、垃圾邮件统统别管。另外，每封邮件的正文里要是提了“urgent”（大小写都算），在结果里给我标出来是紧急的。整理好放到 `ops/pending_review.json`，我等着拿这个去跟领导汇报。麻烦啦！
