嘿，助手！我是 Tom。今天早上发现收件箱被一堆紧急邮件淹没了。系统把那些重要度高而且带有“action required”标签的邮件单独标记了，但我没时间一个一个去翻。请你帮我把这些邮件的 ID 找出来，整理成一个 JSON 文件，放在工作区的 `result/urgent_ids.json` 里，文件结构就用一个 `ids` 数组装这些 ID 就行。记住：只找那些同时满足重要度最高（high）并且标签列表里明确写了“action required”的邮件，别把其他混杂的加进去。谢啦！
