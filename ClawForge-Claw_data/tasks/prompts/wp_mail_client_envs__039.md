邮件客户端系统提示音刚响了两声，我的手机就开始震了——老板在群里催：「收件箱里那些Tech Weekly的新闻邮件天天堆着，我根本没空看，你帮我统计一下还有哪些没读过的，把它们的ID列出来放到 `ops/archive_list.json` 里，我回头一键归档掉。」

我瞥了一眼后台，自动分类已经给大部分邮件打上了标签，但有些混乱。你快帮我翻翻 `data/emails/` 下的邮件文件，找到所有标签包含 **newsletter** 并且我还没读过的邮件。把它们的 `id` 按字母顺序排好，写进 `ops/archive_list.json`。格式是一个字符串列表，就像 `["email_01", "email_06"]` 这样。快点搞，老板等我回复。
