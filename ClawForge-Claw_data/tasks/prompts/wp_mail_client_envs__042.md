老兄，财务部那边炸锅了！所有跟钱有关的邮件全糊在收件箱里，系统虽然有自动分类标签，但我根本没空一个一个翻。你帮我把属于 finance 类别的邮件挑出来，把它们的邮件 ID 整理到一个文件里，放到 `ops/finance_mails.json` 吧。哦对了，有些邮件正文里写着付款截止日期，格式是 `Due:YYYY-MM-DD`，我需要从那些 finance 邮件里找出最早的日期，一块儿扔到同一个 json 里。还有，我怀疑有个叫 "Lottery Scam" 的垃圾邮件混进来了，你顺手把它揪出来，单独把它的 ID 写到 `ops/scam_ids.json`。数据都在 `emails/` 目录下，自己看着办，注意有些旧格式的邮件可能字段不全，别被它们骗了。格式用 JSON，我后面好解析。
