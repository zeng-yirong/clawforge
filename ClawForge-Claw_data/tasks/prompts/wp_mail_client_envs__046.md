小张气喘吁吁地跑过来：“哎呀，出差一周，回来发现收件箱炸了！我急需一个待办清单——就是那些主题里带‘[TODO]’的家伙，按邮件时间排个序，存到 `ops/todo_items.json` 里，每条写上主题和时间就行。还有，那些一眼就能看出是垃圾邮件的（就是系统自动标记为 spam 的那种），你直接帮我丢到 `archived/` 文件夹里，别让我再看到它们。其他邮件暂时别管。拜托了，我下午开会要用这个清单！”

（注意：所有邮件都在 `data/emails/` 目录下，每个邮件是一个 `.json` 文件。）
