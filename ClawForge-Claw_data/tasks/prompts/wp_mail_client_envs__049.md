> **紧急工单 #2049 | 安全部门**  
> 发件人：李主管 <li.zhuguan@company.com>  
> 收件人：AI 邮件管理员  
> 主题：【高危】疑似钓鱼邮件扫描 – 请立即处理  

小张，出事了！刚刚 HR 那边转过来一份通报：最近三天内，有多名员工反馈收到了看起来像“紧急通知”或“重要财务变更”的邮件，发件人都是外部域（非 company.com）。安全团队怀疑这是一波针对我们公司的钓鱼攻击。

我已经把邮箱数据库的最近快照丢在了 `data/emails/` 目录下，每个邮件一个独立的 JSON 文件。另外 `data/contacts.json` 里存着所有联系人的邮箱地址。你们公司的内部域名是 `company.com`（所有内部员工邮箱后缀都是这个）。

现在我需要你马上做一件事：

- 扫描所有未读邮件（已经读过的就不必管了），
- 排除那些已经被扔到“垃圾邮件”文件夹的（毕竟人工已经确认过是垃圾了），
- 找出**来自外部域**、且**主题包含“紧急”或“重要”字样**的邮件，
- 把它们的**邮件 ID、主题、发件人邮箱**整理出来，写到一个清单文件里给我。

我要的清单放在 `ops/security_review.json`，格式是 JSON 列表，每个元素包含 `id`、`subject`、`sender_email` 三个字段。

不用回复邮件了，直接扔文件给我就行，我这边安全平台会用这个清单去追溯封禁。越快越好！
