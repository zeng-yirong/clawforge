嘿，帮我个急事！我在开车，车载音乐系统突然卡住了。刚才正放着“速度与激情”播放列表，我按了下一首，结果没反应，屏幕还停在那首歌上。我手动把当前播放的状态记下来了，放在 `ops/current_playback.json` 里，你帮我看看这个播放列表接下来该放哪首歌。播放列表的数据在 `data/playlists/` 下，歌曲信息在 `data/songs/` 里。处理完后把结果写到 `ops/next_song.json`，格式就一个字段：`{"next_song_id": "..."}`。注意别用那些备份目录里的旧数据，只要最新的。谢了，在线等！
