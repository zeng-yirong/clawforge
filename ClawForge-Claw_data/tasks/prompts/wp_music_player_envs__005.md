小陈，车机音乐系统的蓝牙播放最近老是出怪事——明明歌单里列着歌，放两秒就自动跳到下一首。我查了一下音频日志，说找不到某些曲目的媒体资源。你把 `data/playlists/` 下的歌单文件和 `data/songs/` 里的歌曲库对一对，看有没有歌单里引用了歌曲库里根本不存在的 song_id。这种“幽灵曲目”肯定不止一条，全找出来，写到 `ops/missing_songs.json` 里，我这边好批量从所有歌单里摘掉它们。文件格式就一个简单的 JSON 数组，每个元素是一个幽灵曲目的 song_id 字符串，不要重复。搞定后告诉我一声，我重新推送配置。
