嘿，我是运维小刘。昨晚“夜驾驶”模式下一首歌曲播放直接卡死，用户骂了一整路。我从车机里扒了数据库快照和播放器日志，全丢在 `data/` 和 `logs/` 下面了。日志里有一条错误记录，明确说某首歌的时长非法导致播放器跳过。你帮我把那个罪魁祸首的歌曲 ID 揪出来，放到 `ops/problem_song.json` 里，格式就写 `{"song_id": "<那个ID>"}`，我等着把它从播放列表里踢掉。别的文件别乱动。
