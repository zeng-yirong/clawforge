嘿，哥们儿！我刚从车载系统里把数据导出来了，全在 `data/` 目录下。现在有个急活——咱们那个“速度与激情”歌单，我怀疑里面混了不少中文歌，但客户要的是纯英文驾驶氛围。你帮我数数这个歌单里到底有多少首英文歌，把结果扔到 `output/report.json` 里。格式你定，但必须有个 `count` 字段，数值要准，不准回头挨骂可别怪我。对了，数据里可能有些歌缺字段或者 ID 对不上，你自己掂量着处理，别把垃圾数进去。
