嘿，系统最近跑了一轮播放列表清洗的预检查，结果发现“夜驾驶”这个歌单里混进来好几首不对路的曲子。用户投诉说晚上开车想听中文歌，结果蹦出来英文的，还有年代对不上的。按理说咱们后台的过滤规则一直在迭代，最新版应该已经部署了，但不知道为什么这个歌单没生效。

我把相关的配置都留在 `config/` 底下了，规则文件在 `data/rules/` 里，播放列表在 `data/playlists/`，歌曲库在 `data/songs/`。你帮忙按照当前生效的那个过滤规则，把“夜驾驶”里不符合条件的歌筛掉，重新整理一份干净的歌单出来，放到 `ops/curated_playlist.json` 里。格式不用太复杂，就把歌单ID、名称还有保留的歌曲ID列表写上就行。我直接拿去覆盖线上的配置。

注意，只动“夜驾驶”这个歌单，其他的别管。谢谢！
