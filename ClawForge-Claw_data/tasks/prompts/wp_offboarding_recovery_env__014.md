你好！我们收到了HR发来的离职单，员工李雪（EMP-042）的离职申请已经批准了。不过IT系统里的数据有点乱，我需要你帮忙处理一下。

数据都在 `offboarding/` 目录下的几个JSON文件里：

- `exit_requests.json` 记录了离职请求的状态。
- `system_access.json` 记录了员工在系统里的权限。
- `equipment_assignments.json` 记录了设备分配情况。

你看一下李雪的记录：

- 她的系统权限还是 active 状态（Admin Portal 和 CRM），需要全部撤销。
- 她的设备分配里，BG-8821 和 LT-2041 显示 assigned，但 BG-8821 好像有条旧的已归还记录混在里面，你要判断一下哪些是当前实际占用的设备，然后回收它们。
- 另外，交接时还需要手动删除她的邮箱账户，这个也记下来。

最后，请把你的处理结果汇总成一个交接清单，放到 `ops/handover_checklist.json` 里。格式可以参考下面（当然你可以自己组织，但关键信息不能少）：

- 员工ID和姓名
- 已撤销的系统权限列表（每个系统名称和状态 revoked）
- 已回收的设备列表（每个资产标签和状态 reclaimed）
- 需要手动处理的事项列表

完成后告诉我一声就行，谢谢！
