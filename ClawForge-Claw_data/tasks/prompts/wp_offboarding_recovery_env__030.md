嘿，运维同学，我是财务部的 Amy。刚批下来一波离职申请，我这边急着交接，但系统清理流程卡住了。我把数据全丢在 `data/offboarding/` 下面了——exit_requests.json 是离职单，system_access.json 是账号权限，equipment_assignments.json 是设备分配。麻烦你帮我把这些已批准的离职单对应的人，所有还开着的系统权限都关掉，设备也回收掉，最后在 `ops/` 目录下生成一份交接清单，里面要写清楚每个离职员工的名字、收回了哪些系统、收回了哪些设备。动作要快，今天下班前我要拿它去会签。
