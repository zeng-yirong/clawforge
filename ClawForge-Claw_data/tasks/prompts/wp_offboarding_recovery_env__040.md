早啊，我刚收到法务那边邮件，说我们上个月有两个离职员工的流程还没走完。审批早就过了，但系统访问和设备回收都没动，交接清单也没出。你看看 `data/offboarding/exit_requests.json` 就能找到那几个批准了的。对应的系统访问记录在 `data/offboarding/system_access.json`，设备分配记录在 `data/offboarding/equipment_assignments.json`。帮我把这些人的权限全部撤掉（状态改成 `revoked`），设备回收了（状态改成 `reclaimed`），然后整理一份交接清单放在 `ops/handover_checklist.json` 里。清单里每个员工的信息要完整：员工ID、姓名、访问撤销状态（统一写 `completed`）、设备回收状态（统一写 `completed`）。注意只处理已经批准的人，别碰其他还在流程中的。好了，交给你了，弄完说一声。
