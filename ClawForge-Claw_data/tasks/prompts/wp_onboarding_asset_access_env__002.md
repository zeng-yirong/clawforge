嘿，我是人事部的李经理。新同事张三今天正式入职，他的合同我已经确认签了。你能帮我处理一下他的入职配置吗？所有原始资料都在工作区的 `data/onboarding/` 目录下。

- 合同文件 `contracts.json` 里有他的信息，不过注意里面有一些老合同或者已撤销的，别搞混了，挑出那个状态是 `signed` 的。
- 设备库存 `equipment_inventory.json` 里找一台可用的笔记本电脑，别给我分配那些已分配出去或者坏掉的。
- 权限包 `permission_packs.json` 里根据他的部门匹配对应的权限包。

最后，把整理好的结果放到 `output/` 目录下，文件名叫 `onboarding_summary.json`。里面要包含他的员工ID、姓名、邮箱、分配的设备信息（标签和类型）以及权限包详情（包ID和系统列表）。动作快一点，我要立刻用这个去开通账号。
