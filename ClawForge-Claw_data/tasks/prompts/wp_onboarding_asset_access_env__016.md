嗨，新员工 Jane Doe 的入职流程终于到了配置环节。她的合同已经签署完毕，你可以在工作目录的 `contracts/` 下找到相关记录。Jane 会加入 Engineering 部门担任高级工程师，下周一开始。

我们需要为她准备好完整的一套入职配置：

- 先给他创建好公司邮箱（标准格式是名.姓@company.com）
- 然后根据 Engineering 部门的权限模板，开通必要的系统访问权限。权限模板放在 `permissions/` 目录中，你对照一下部门信息就能找到对应的那个。
- 另外，还需要从 `equipment/` 库存中调拨一台空闲的笔记本电脑给她，注意别选到已经分配出去的设备。
- 最后，把所有这些配置结果整理成一个 JSON 文件，放到 `ops/` 目录下，命名为 `onboarding_result.json`。这个文件我会用来导入 Slack 和内部系统，所以字段务必齐全：员工ID、姓名、邮箱、系统访问列表、设备资产标签，以及一句简短的欢迎消息（你自己写一句就行，她叫 Jane Doe）。

其他员工的状态不需要你处理，我只关心 Jane 的。东西都在工作区里，你看着办吧，搞定了喊我一声。
