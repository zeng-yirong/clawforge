Hey Team,

I just got the green light – Alice Wang (employee ID: E123) is officially signed and starting Monday. I've dropped all the raw data in the `data/` folder. You'll find her signed contract mixed in with a bunch of outdated ones in `data/onboarding/contracts.json`. The engineering team's permission packs are in `data/onboarding/permission_packs.json`, and the equipment inventory is in `data/onboarding/equipment_inventory.json`. There's also some legacy `data/accounts.json` and `data/contacts.json` you can ignore.

Could you prepare the full onboarding package? Here's what I need:
1. Her email profile – use the standard pattern firstname.lastname@company.com.
2. The systems her role needs – grab the matching permission pack from engineering.
3. A laptop from the inventory – anything available that's a laptop. Update the inventory so it shows as allocated.
4. A welcome message for the #general Slack channel – keep it friendly, mention her email.

Put the final package into `ops/onboarding_package.json` so HR can double-check everything before the weekend. Don't touch anything else – just the new files and the inventory update.

Thanks!
