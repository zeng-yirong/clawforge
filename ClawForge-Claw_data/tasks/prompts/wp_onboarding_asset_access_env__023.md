**收件人**: 新入职 IT 支持  
**发件人**: 运维主管 David  
**主题**: 新一批实习生入职，马上处理！

Hi，

刚收到 HR 的通知，本期实习生合同已经全部签完了。我这边已经把他们的合同信息、权限模板和设备清单放在了 `data/onboarding/` 下面，还有一张空白的 Slack 消息缓存表在 `data/slack_cache/`。

你帮我跑一下标准的入职流程：
1. 邮箱配好 —— 每个已签合同的实习生要有一个专属的邮件 profile。
2. 系统权限配上 —— 根据他们所属部门，从权限包模板里挑对应的系统列表，激活访问。
3. 设备分到位 —— 从库存里找出可用的笔记本（laptop），每人一台，别重复分配。
4. 最后在团队 Slack 频道发一条欢迎消息，格式统一：“Welcome {姓名} to the team!"。

整理结果时按老规矩来：在 `output/` 目录下，为每个已签约的实习生创建一个以 `employee_id` 命名的子文件夹，里面放四个文件：
- `email_profile.json`
- `sys_access.json`
- `equipment.json`
- `welcome.json`

内容怎么填你应该清楚，别漏掉谁，也别把没签合同的拉进来。弄完了告诉我一声，我跑一遍批量检查脚本。

多谢！
David
