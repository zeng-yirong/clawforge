Hey Alice刚刚签了合同，明天就要入职了。HR催着我把她的设备、系统权限和邮箱都配好，但我手头还有三个工单要处理，实在忙不过来。

她把原始数据都丢在 `data/` 里了：合同文件在 `data/onboarding/contracts.json`，员工账户信息在 `data/accounts.json`，设备库存和权限包在 `data/onboarding/` 下面。你帮我跑一趟，根据这些材料给她配齐：

- 挑一台**笔记本电脑**（从库存里找一台还没分配出去的）
- 根据她的**部门**选一个对口的权限包
- 最后把所有关键信息（员工ID、邮箱、分配的设备标签、权限列表，外加一句简短的欢迎语）塞到一个配置文件里，放到 `ops/onboarding_result.json`。我后续脚本直接读这个文件一键执行。

记住，别搞错人——她合同上是 `signed` 状态，账户里也有她的记录。拜托了！
