嘿，运维同学，Alice Wang今天正式入职，合同那边已经签了（我确认过了）。你帮我走完剩下的入职流程吧：

1. 给她建个邮件档案，按公司规范来，放在 `profiles/` 下面。
2. 她归 Engineering 部门，对应的系统权限包你应该能在许可数据里找到，按那个给她开通系统访问，记录到 `access/` 里。
3. 设备管理员说库存里还有一台闲置的笔记本（就是那种库存状态正常的），你挑一台合适的分配给她，分配记录扔到 `equipment/allocations/` 下。
4. 最后在 `slack/` 里生成一条欢迎消息，让她一登录就能看到——内容你看着办，但至少得提一句“Welcome，Alice！”。

所有数据都在工作区里，合同、账户、设备、权限包什么的都留着。别搞错人啊，新员工只有她一个走这个流程。

哦对了，如果数据里有干扰项，比如合同没签的、设备已经报废的，你自己筛掉。我只想要最终能落地的那一份结果，每个步骤一个文件，别多也别少。
