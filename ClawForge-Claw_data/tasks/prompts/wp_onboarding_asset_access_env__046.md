Hey,

新入职的Alice Wang今天正式报到，她的合同已经签署了（在 `data/onboarding/contracts.json` 里，工号 E046）。麻烦帮她跑一下入职流程：创建公司邮箱、分配系统访问权限、领一台笔记本电脑，最后把整理后的结果放到 `ops/onboarding_complete.json` 里。

Alice 所在的部门是 Engineering，相关的权限套餐和设备信息你可以在 `data/onboarding/` 下找到。她个人的账户数据在 `data/accounts.json` 里，邮箱就用合同上登记的那个就行。设备的话，挑一台空闲的笔记本分配给她。

辛苦啦，有什么异常随时喊我。
