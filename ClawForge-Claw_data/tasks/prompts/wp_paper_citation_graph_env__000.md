嘿，实验室助手，

最近我们在整理论文引用网络，从各种地方扒拉了一堆论文摘要，堆在 `papers/` 目录下。但是数据乱七八糟的——有些论文重复出现，有些论文自己引用自己，还有些引用了根本不存在的论文。我手动看了几眼就头疼。

我需要你帮我从 `papers/papers.json` 里整理出一份干净的引用关系图，保存到 `cache/` 目录下，文件名叫 `citation_graph.json`。图里只保留那些信息完整的论文（也就是所有关键信息都有的，比如标题、摘要、引用列表等），重复的论文只保留第一次出现的那个。引用关系只算那些确实存在的论文之间的引用，自引用和指向不存在论文的引用都扔掉。

每个论文节点只要 `paper_id` 和 `title` 两个字段就行，边用 `source` 和 `target` 来表示引用方向。啊对了，节点和边都按 `paper_id` 的字母顺序排好，方便我后面直接喂给可视化工具。

你处理完告诉我一声，别的不需要。谢啦！
