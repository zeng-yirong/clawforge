Hey运维组，昨晚跑论文耦合分析时发现好几条幽灵引用——指向根本不存在的论文ID，搞得可视化全是乱连的假边。原始论文数据我扔在 `data/papers/papers.json` 里了，你帮我把那些无效的引用过滤掉，只保留真实存在的论文之间的引用关系，然后按源论文ID升序、同一源下按目标ID升序排好，写到 `cache/citation_graph.json` 里。我只要一个干净的列表，每条记录给 source 和 target 字段就行，别加其他花哨东西。弄好了喊我一声，我直接拿过来跑图算法。
