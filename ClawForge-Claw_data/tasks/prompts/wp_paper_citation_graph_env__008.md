哎，刚跑了一轮论文引用分析，结果图里全是幽灵节点！data/papers/papers.json 里那些论文的 citation_ids 字段，有些引用的论文根本不存在于这个数据库里。我需要一个干净的引用关系图，只保留那些源和目标都在当前论文集合里的边，别把那些挂空引用的垃圾带进来。

麻烦你把所有有效的引用边提取出来，按我常用的格式存到 cache/citation_graph.json。格式是 {"edges": [{"source": "论文ID", "target": "被引ID"}, ...]}。我晚上要对着这个图做主题聚类，千万别给错。
