我是论文引用图维护团队的运维。刚刚我们导入了新一批论文摘要数据，但我发现数据库里的 `citation_ids` 字段居然全是空的——估计是导入脚本出 bug 了。可是摘要文本里明明有很多引用格式 `[paper_id]` 标记，比如 `[p002]` 这种。我需要你帮我把真正的引用关系手工捞出来，生成一份干净的引用图快照存在 `cache/` 目录下，文件名就叫 `citation_graph.json`。我只信任那些确实存在于 `data/papers/papers.json` 中的论文 ID，摘要里如果出现大写或拼写的假引用（比如 `[P001]` 或引用一个不存在的 ID）请直接忽略。最终文件里每条引用边记录一次就行，不要重复。数据验证组等着要，尽快搞定！
