Hey,

我们数据清理组的同事临时被调去处理日志了，留下一堆混乱的论文元数据。我刚才翻了翻工作区里的 `papers/papers.json`，发现引用列表简直是一团糟——自引的、指向根本不存在的论文、还有大量重复的引用。这样我们后续的可视化和影响力分析根本没法做。

我们需要一个干净的引用图快照，你帮忙整一下：把 `papers/papers.json` 里的数据捞出来，生成一个 `citation_graph.json`，放在工作区根目录就行。具体要求：

- 节点就是所有出现在 `papers/papers.json` 里的论文（按 `paper_id` 升序排列），每个节点带上它的 `title`。
- 边就是有效的引用关系：去掉自环（引用自身）、去掉那些引用了不存在的论文的边、去掉完全相同的重复边（只保留一条）。
- 所有边按 `source` 升序，相同 `source` 再按 `target` 升序排列。

我没时间手把手交代细节，你看看数据自然知道该怎么做。弄好了尽快通知我，我好拿给老板看。

谢了！
