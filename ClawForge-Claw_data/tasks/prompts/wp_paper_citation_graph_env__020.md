嘿，我是实验室的数据管理员小张。咱们的论文库最近有点乱，旧版本还没清理干净，有些论文的引用字段也写错了。我刚刚把最新的快照放在了 `papers/papers.json` 里，但里面混进了一些无效条目——比如年份不对的、ID格式乱掉的、还有字段不全的。我需要你帮我从里面把真正有效的论文挑出来，然后根据它们之间的引用关系生成一个完整的引用图，存到 `cache/citation_graph.json` 里。我后面要用这个图做可视化和影响力分析，所以结构要清晰：每个论文节点要有唯一的 ID 和标题，边要表示引用关系。拜托了！
