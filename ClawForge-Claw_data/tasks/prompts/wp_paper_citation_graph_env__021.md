嘿，我刚把论文数据库的 dump 倒出来了，放在 `papers/papers.json` 里。这些数据是从几个不同来源拼的，里面混了不少问题——有的缺引用列表，有的引用了根本不存在的论文，还有年代久远的老记录。我打算做一个可视化展示，只需要干净的引用关系：只保留那些引用双方都是真实存在且记录完整的论文。杂乱的东西你直接过滤掉就行。

你整理完后，把结果扔到根目录的 `citation_graph.json` 里。格式你来定，但得让我一眼看出哪些论文被引用了、具体谁引谁。节点信息至少要有论文 ID 和标题，边的信息标明谁引谁。我只认这个文件，里面数据质量你负责。

对了，别动我原来的 `papers/` 目录，我只读不写。

拜托了，越快越好。
