老张，实验室那套论文数据你熟悉吧？最近隔壁组要画引用网络图，我翻了翻 `data/papers/` 下的记录，发现好几个问题：有的论文跟别人互相引用，但引用的那篇压根没收录；还有个老版本的 `papers_old.json` 格式都变了，别管它。你帮我把真正有效的论文引用关系整理出来，只要引用链能闭环的那些——也就是说，只有引用了本库里存在的论文才算有效。结果放到 `cache/citation_graph.json`，格式我不管，但一定要能清楚知道每篇论文引用了哪些别的论文（用 paper_id 标识）。对了，库里有几篇论文的 `citation_ids` 是空的，这种也算节点，只是没有出边。弄完后跟我说一声就行，别拖太久。
