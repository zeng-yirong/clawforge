嘿，我是实验室的小陈。刚跑完一批论文入库，现在想快速看一下引用关系网，方便下周组会汇报。最新的论文目录在你工作区的 `data/papers/papers.json` 里，注意别用旁边那个 `old_papers.json`，那是上周的旧版，有些引用已经修正了。

我需要你帮我从最新的论文库里把所有有效引用关系拎出来，做成一个图快照。规则很简单：每篇论文的 `citation_ids` 里可能包含一些不存在的论文 ID（比如已经撤稿或编号写错了），也可能包含论文自己引用自己——这两种情况都给我扔掉，只保留那些被引论文确实在库里的引用。另外，引用关系可能重复，但图里只需要一条边表示一次引用就行。

结果放到 `cache/citation_graph.json`，格式你看着办，只要人能看懂、程序也能解析就行。我等着用，谢啦！
