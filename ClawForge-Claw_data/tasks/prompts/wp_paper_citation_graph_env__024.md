嘿，运维同事，急事！刚发现论文引用图谱缓存彻底坏了，前端展示全部乱套。原始数据还在 `papers/papers.json` 里，但我瞄了一眼，里面有些引用指向了根本不存在的论文ID，还有些引用记录重复了。我需要你帮我把正确的引用关系整理出来——只保留实际存在的论文之间的有效引用，重复的只留一条，然后按引用源论文ID排好序，输出到 `ops/citation_graph.json`。格式你自己看着办，反正我只要干净的边列表，别给我塞无效的脏数据。拜托了，我等着用这个去修复缓存。
