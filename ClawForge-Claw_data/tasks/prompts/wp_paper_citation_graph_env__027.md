嘿，实验室的伙伴们！最近我在整理咱们组历年发表的论文引用关系，想画个引文网络图方便写综述。我已经把所有论文的元数据从数据库里导出来了，就放在工作区里的 `papers/` 目录下，每个文件是一篇论文的 JSON 快照。但是这些文件有点乱——有些是直接从接口 dump 的纯文本，有些是二手整理的，可能混了不是 JSON 的文件进去，还有一两篇的摘要里写得稀里糊涂的。

我需要你帮我把这个混乱的数据清洗一下，然后把所有有效的论文之间的引用关系提取出来，生成一个标准的引文图。图里每个节点代表一篇论文，要有论文 ID 和标题；每条边代表一次引用，从引用方指向被引用方。注意，那些引用了不存在的论文（即不在任何有效论文中的 ID）的边就不要画进去了，免得污染图。最后把这个图存到 `graph/citation_graph.json` 里就行。

我急着要，麻烦你快一点！对了，输出格式你看着办，只要我下游程序能读就行（JSON 对象，包含 nodes 和 edges 两个列表）。拜托啦！🙏
