嗨，我是实验室的小王。老板让我把最近收集的一批论文整理一下，生成一个引用网络图，方便组会展示。我把原始数据丢在 `papers/` 文件夹里了，每个论文是一个 JSON 文件。但你知道的，总有新人把草稿也塞进去，有些字段没写完，还有一些引用了根本不存在的论文。我只需要那些结构完整、引用也能追上的论文关系。麻烦你帮我扫一遍，把最终成果写成一个 `citation_graph.json` 放到工作目录根下就行。里面得包含每篇论文的 ID 和标题，还有它们之间的引用关系，注意只保留有效的引用（即目标论文也存在的情况）。谢谢啦！
