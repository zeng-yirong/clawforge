嘿，小王，你来得正好！我们组论文引用数据库简直乱成一锅粥了。我刚刚手动整理了一份最新的论文清单，放在 `papers/papers.json` 里，但里面的 `citation_ids` 字段一团糟——有些引用了根本不存在的论文，还有重复的、自指的，看得我头大。

下周组会我要用这个引用关系图做展示，你帮我把里面的脏数据清理干净，只保留那些**确实存在于论文列表里**的引用，每个引用只出现一次（别重复），然后按照标准格式输出到 `cache/citation_graph.json`。

格式很简单：一个对象，包含 `"nodes"` 数组（每个节点有 `"id"` 字段，就是论文的 `paper_id`）和 `"edges"` 数组（每条边有 `"source"` 和 `"target"`，表示谁引用了谁）。记住，只把真正存在的论文之间的引用放进去，那些指向不存在的论文的都得扔掉。快去搞，我下午就要用！
