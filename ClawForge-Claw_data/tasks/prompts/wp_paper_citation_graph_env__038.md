嘿，我是研究组的助理小刘。昨天组会老板发飙了，说论文引用网络图一直没更新，旧版本还混在里面，自引用的垃圾也往里塞。我把原始论文数据 dump 在 `papers/` 目录下了，但情况有点糟：有的文件格式不对打不开，有的缺了引用列表，还有个文件里的 paper_id 竟然引用了自己（简直胡闹）。我需要你帮我整理出一份干净的引用关系图，只保留有效且合理的边（排除自引），然后输出到 `output/graph.json` 里。格式就用简单的数组，每个元素是 `{"source": "paper_id_A", "target": "paper_id_B"}` 就行。另外记得别把那些无法解析的、残缺的文件算进去。谢了！
