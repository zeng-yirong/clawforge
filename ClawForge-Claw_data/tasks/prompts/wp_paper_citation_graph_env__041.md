主题：论文引用图整理，拜托了

晓东，

我刚刚把实验室论文摘要库的导出文件丢到工作区的 `papers/` 目录下了。你懂的，那个库一直没人维护，里面混了不少过期草稿和格式损坏的垃圾文件。我现在要赶一个会议投稿，实在没时间清理。

麻烦你帮我把真正有用的论文挑出来——那些年份在2015年之后、字段齐全（ID、标题、年份、摘要、引用列表）的才算有效。然后根据每篇论文的 `citation_ids` 字段，把它们之间的引用关系整理成一张有向图，保存到 `analysis/citation_graph.json` 文件里。

图的结构我简单说一下：节点要包含论文ID、标题和出版年份；边就是引用的指向，从引用方指向被引用方。你处理的时候直接把那些无效的、格式不对的、过时的统统扔掉，我只要干净的核心数据。

谢了！

——老周
