嘿，我是运营部的小张。HR那边刚催我提交上个月的绩效档案，说系统里有个员工的数据一直没更新。我手头杂事太多，你能帮我跑一下吗？

事情是这样的：我们的绩效规则文件在 `data/rules/` 下面，但那个文件夹里混了几个旧版本的备份，你只认最新的那个 `scoring_rules.json` 就行，里面按角色代码分了权重。员工信息在 `data/employees/employees.json` 里面，每个人的角色代码、部门、名字都有。每个月的产出记录在 `data/ledgers/monthly_outputs.json`，每个员工有 feature_delivery、quality_score、collaboration_score 三个指标。

我需要你找到那个编号是 **E001** 的员工（就叫 Alice 吧），然后用她的角色对应的评分规则，算出她的月度总分（加权平均，结果保留到整数），最后在 `profiles/` 目录下生成一个叫 `E001_profile.json` 的文件，里面至少包含她的员工ID、名字、部门、角色代码、当月总分和计算明细（各项分数和权重）。哦对了，那个 `profiles/` 目录可能还不存在，先建一下。

数据里可能有些乱七八糟的东西（比如过时副本、无效记录），别管它们，就用我说的那几个文件里最新的记录就行。你搞定后发我一声，我直接传给HR。
