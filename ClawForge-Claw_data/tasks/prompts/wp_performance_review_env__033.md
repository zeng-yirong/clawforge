**发件人**: 林敏 (HR绩效经理)  
**收件人**: AI助手 <ai@workplace>  
**主题**: 紧急：本月绩效评分汇总，周五前必须完成！

Hi 助手，

又要到月度绩效汇总的时候了。我已经把基础材料放在了工作区的 `data/` 下面：

- 员工花名册在 `data/employees/employees.json`
- 每个员工本月的产出明细在 `data/ledgers/monthly_outputs.json`
- 不同岗位的评分规则在 `data/rules/scoring_rules.json`

这些文件都是标准格式，但你也知道，HR这边数据一直有点脏——有的员工可能只录入了部分数据，还有重复记录或者不在名单里的人冒出来。另外，规则里每个角色的权重是不同的，你计算总分时需要把这些权重用上。

我需要的最终成果是一份干净、可直接导入奖金系统的档案，放在 `ops/` 文件夹里，文件名叫 `performance_profile.json`。格式你看着办，但每条记录必须包含：员工工号、姓名、所属部门，以及最终的加权总分。请确保只有你找到的有效员工才写入，千万别把那些异常或无效的带进去。

周五就要截止了，拜托你了！

林敏
