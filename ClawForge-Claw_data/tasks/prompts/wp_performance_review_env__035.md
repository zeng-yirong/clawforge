嗨，我是HR的Linda。月底绩效汇总又来了，我把所有材料都放在工作区里了：

- `data/employees/` 下面有员工花名册
- `data/ledgers/` 里是每个人这个月的产出台账
- `data/rules/` 里有我们用的评分权重规则

这些文档里可能混了一些冗余或不完整的记录，麻烦你帮我理一下。请你只挑出那些既在花名册里、又有台账、而且角色能在规则里找到对应权重的人，算出他们的加权总分，最后整理成一份清晰的绩效评分文件。文件就放在 `performance/scores_202501.json`，格式你定，但要一目了然，我后续直接导入系统。拜托了！
