嘿，我是财务部的Mira。我们怀疑最近有重复的报销提交，我需要你帮忙核对一下。我把所有邮件原始数据丢在了 `data/emails/` 目录下，每个邮件里可能带附件，附件文件在 `data/attachments/` 里。请帮我找出所有来自财务部（发件人ID是 `finance`）并且邮件已被标记为已审批（`labels` 包含 `"approved"`）的邮件。每个这样的邮件会对应一个发票附件，附件里会有一个 `approved` 字段表示是否有效，以及一个 `amount` 字段表示报销金额（金额必须大于0）。把你找到的每一笔有效报销记录下来，汇总到 `ops/finance_summary.json` 里，格式是一个对象，包含 `items` 数组（每个元素有 `email_id` 和 `amount`）和一个 `total_amount`（所有金额之和）。我只要准确的数字，别把那些草稿、拒绝或无关的邮件算进去。谢谢！
