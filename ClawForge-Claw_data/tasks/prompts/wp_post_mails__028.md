我是市场部的 Kim。凌晨三点被叫起来开电话会议——Nova-7 的发射时间提前了，所有宣发物料必须今天就位。IT 那边把邮箱倒腾成了文件，全堆在 `data/emails/` 下面，抄送了一堆人，我根本分不清哪封才是最终批准的 Brief。你帮我整理一下。

我只需要一个文件：`ops/launch_payload.json`，里面放的是我们即将在 X 和 Reddit 上发布的官方消息。格式你可以自己定，但必须包含我下面提到的所有关键信息（别问我具体字段名，你自己看附件内容应该知道哪些东西是必须的）。

记住，我只认 **最新** 且 **已经过审批** 的版本。邮件太多，别拿草稿或者旧版糊弄我。附件在 `data/attachments/` 里，文件名对应邮件里的 `attachments` 字段。搞定了直接放 `ops/` 下，我让同事直接 pickup 去发。
