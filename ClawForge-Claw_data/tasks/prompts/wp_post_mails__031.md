嘿，我是 Owen。明天就是轨道发射的正式窗口了，Mira 昨晚把最终批准的简报发了过来，但我邮箱里塞满了各种 CC 和垃圾邮件。那封简报的附件就在 `attachments/orbital_brief_v3` 里，你应该能在 `emails/` 下找到对应的消息。

帮我把附件里最重要的信息拎出来，写到 `output/launch_info.json` 里。特别注意一下版本号——如果 build 版本太低，我们得标记一个风险标志。别搞混了版本，我只需要那个最终批准的。
