嘿，AI 助手！我是市场部的 Lena。我们刚拿到 AuraSync 产品发布的正式批准，但我的收件箱乱得像战场——里面有各个版本的简报草稿、旧版、甚至还有几个过期的“已批准”版本。老板十分钟后就要看最终摘要，我实在没时间从那一堆邮件里挑。

我需要你钻进 `data/emails/` 这个收件箱，找出那封**最新**的、**带附件**的、并且标注了 **approved** 的邮件。附件里写着产品名、发布日期和版本号。把它们摘出来，整理成一个简洁的 JSON 文件，放到 `ops/brief_summary.json`。老板只看这三个值，别多放别的。

对了，附件文件都在 `data/attachments/` 里，邮件里的 `attachments` 字段会告诉你该读哪个。我只要准确的数据，拿不准的别猜。
