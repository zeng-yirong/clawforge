嘿，安全值班的同事，我是数据平台小张，刚被on-call吵醒。事情是这样的：凌晨我巡检发现批处理队列里卡了好几条“全量导出”的请求，全是冲着架构备忘录和客户主账本去的。这几个账号平时根本不该碰那些敏感资产。我手边没有环境，就把账户清单、资产注册表和待办请求都扔在 `data/` 下面了，你帮我查查哪些请求是越权操作，必须立刻拦截。把我该标记为“拒绝”的请求 ID 写到 `ops/deny_requests.json` 里，我等着复制到工单系统。别搞错，我只要真正越权的那些，正常的请求不动。谢了！
