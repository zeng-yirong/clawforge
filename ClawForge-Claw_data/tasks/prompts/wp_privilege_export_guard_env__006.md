📧 紧急：批量导出权限审查

安全团队今早发现一批可疑的批量导出请求，怀疑有人试图越权获取敏感数据。我们的账户 ID 是 `acc_001`，权限在 `data/accounts.json` 中。资产分类信息在 `data/assets/assets.json`，所有待处理的导出请求在 `data/requests/requests.json`。那些已经处理过的（状态不是 pending）就别管了，但还在排队的一律要过一遍。

我需要你把那些**不应该被允许**的请求 ID 单独拎出来，整理成一个 JSON 数组，放到 `ops/deny_target.json` 里。记住，只有当前账户权限范围内的资产才能导出，其他的一律拦下。别被附件里的闲话干扰，也别把已经处理的请求再翻出来。

动作快，期望下班前看到结果。
