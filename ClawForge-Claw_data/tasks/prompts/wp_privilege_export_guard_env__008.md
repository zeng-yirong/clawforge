合规小姐姐上周找我哭了一场，说有人在半夜批量拉架构库和客户清单，差点把主库搞崩。咱们账号的权限只到客户数据层，架构数据层不该碰。我把最近挂在那里的所有导出请求和基础信息都拖到工作区了，你帮我捋一捋。哪些批量导出请求是踩着红线走的？把它们挑出来，记到 `ops/deny_list.json` 里，就记一个数组，里头是那些越权请求的 ID。我等着拿去跟 Security 开会拍桌子。哦对了，别把单次导出的、或者自己权限范围内能碰的批量导出也塞进去，那可就闹笑话了。
