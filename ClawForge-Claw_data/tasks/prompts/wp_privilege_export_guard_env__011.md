嘿，安全队友，有个急事得你出手。

刚才系统告警说有人批量导出数据，我简单翻了翻 `data/` 下的记录，发现有几个请求不对劲。我们公司的权限模型很清楚：每个账户只能读取自己权限范围内的资产类型（`read_policy`）。但我注意到有些“批量导出”请求的目标资产，跟发起人的权限完全不匹配。这要么是误操作，要么就是内部越权。

我把相关文件都留在了工作区：
- `data/accounts.json`：所有账户信息，里面标明了每个人的 `permissions`。
- `data/assets/assets.json`：资产清单，每个资产有 `read_policy` 字段，说明需要什么权限才能读。
- `data/requests/requests.json`：所有的导出请求，包括状态、风险标记等。

我需要你帮我整理一份“必须立即拒绝”的请求清单。规则很简单：如果一个请求的 `target_asset_id` 对应的资产，其 `read_policy` 不在发起人的 `permissions` 里，那这个请求就该被拦下。请把结果写到 `ops/deny_list.json`，结构可以像这样：一个列表，每个元素包含 `request_id` 和简短的 `reason`。

拜托了，这直接关系到我们的数据安全，别漏掉任何一个。
