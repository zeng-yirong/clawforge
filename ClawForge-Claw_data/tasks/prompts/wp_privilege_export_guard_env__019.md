嘿，我是安全运维阿杰。刚收到审计告警，说有人批量提交了导出请求，但我怀疑好多根本就没权限。我把核心数据扔在工作区里了：

- `accounts/accounts.json` 里是所有员工账号和他们的数据访问权限。
- `assets/assets.json` 里是受保护的数据资产，每个资产都标明了需要什么权限才能读。
- `requests/requests.json` 是这次提交的所有批量导出请求，每条都指向一个资产，还记录了提交人。

我现在没空一个个对表，你帮我逐个检查：哪些请求的提交人根本没有对应资产要求的权限？把那些确实越权的请求 ID 整理出来，写到一个叫 `ops/blocked_exports.json` 的文件里。我只要列表，别加注释、别改格式——后面流水线直接吃这个文件。搞快点，这帮人还在催着要数据。
