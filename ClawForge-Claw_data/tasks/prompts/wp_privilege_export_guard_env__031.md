我这边登录的是 `acc_001` 账号，权限只到客户数据层。但刚才安全网关告警说有人提交了一批批量导出请求，可能包含架构仓库的资产。我把工作区里的 `data/` 和 `session/` 都拉下来了，你帮我过一眼。那些明显越权的请求，把它们的 ID 记下来，放到 `ops/guard_action.json` 里，格式就是 `{"deny_ids": ["req_..."]}`。我只想看到一个干净的动作清单，别给我多余的东西。
