早点把我叫起来！凌晨刚收到 SOC 告警，说有一个批量导出请求正在排队，审计日志显示发起人的账户权限明显不够，但不知道为什么系统还没拦下来。我不想等明天开评审会，现在就截住它。

我已经把最新的账号资料、资产清单和所有待处理的请求打包放在 `data/` 下了。导出的目标资产在请求的消息正文里写得清清楚楚，但是账号的 permissions 列表少了一种关键数据类型的访问权。你帮我看看到底是哪个请求，然后直接出一个拒绝指令，放在 `ops/` 下面，我拿去喂给审批网关。格式你看着办，但要让我一眼知道拒绝的是哪笔请求、为什么拒。

别问我具体怎么做，这种越权拦截你应该比我熟。动作快点，趁业务还没开始大规模跑。
