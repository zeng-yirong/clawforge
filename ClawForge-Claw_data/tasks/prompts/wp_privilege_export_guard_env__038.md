Hi team,

凌晨审计系统狂跳警报，说最近那批数据导出请求里有越权嫌疑。我把所有原始数据都铺在 `data/` 里了——账户权限、资产定义、请求列表，还有几个附件也扔在 `attachments/` 下。得赶紧筛一遍，把那些发起人权限不够却要拿高敏数据的请求揪出来。

判断依据很简单：每个账户能访问什么数据，看 `data/accounts.json` 里的权限列表；每个资产属于什么等级，看 `data/assets/assets.json` 里的类型和阅读策略。请求文件 `data/requests/requests.json` 里列了所有待处理的批量导出请求，注意只要还没处理的（状态是 pending 的），已经拒过的别管。

请你整理出所有越权请求的 ID，放到一个 JSON 列表里，写进 `ops/deny_list.json`。我只想看到准确的 ID，一个不多一个不少。弄完直接告诉我，我这边一键拒单。

别磨蹭，越快越好！
