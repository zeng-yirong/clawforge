嘿，我是安全审计组的Lena。凌晨的批量导出日志扫描报警了——有个请求试图拉取架构知识库的完整快照，但发起账户的权限分明只开放了客户数据层。我把最近的导出请求清单、账户权限配置和资产分类表都放在了工作区里，路径分别是：

- `data/requests/requests.json`
- `data/accounts.json`
- `data/assets/assets.json`

另外还有些附件说明和联系方式，需要的话也可以翻翻 `data/attachments.json` 和 `data/contacts.json`。

我需要你帮我把所有**超出账户数据访问权限的批量导出请求**揪出来，整理成一个阻止列表。每个违规条目要写明请求ID和简要理由（比如“账户无权访问该资产的数据类型”）。把结果写到 `ops/violations.json`，格式是数组，每个元素包含 `request_id` 和 `reason` 字段。

别漏了那些伪装成低风险但实际越权的请求。完成直接放在工作区内就行，我回头批量执行拒绝操作。
