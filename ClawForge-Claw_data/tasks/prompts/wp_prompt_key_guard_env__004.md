嗨，我是安全运营中心的Jane。昨天下午有个自称是数据库管理员的人提交了三个紧急访问工单，说要修复主库的锁死问题。但我觉得不对劲——他要求访问的是API Key Vault，跟数据库半毛钱关系没有。我把他提交的工单信息都放在 `data/requests/` 下面了，还有他上传的附件在 `data/attachments/` 里。你帮我仔细核对一下，找出那个试图骗我们开放密钥保险库的家伙。按照我们公司安全事件的标准模板，把结果写到 `ops/blocked_attempt.json` 里——里面要记录请求ID、拒绝原因代码（用 `credential_exfiltration_block`）以及你的工号。快，别让我的老板追问我为什么还没处理。
