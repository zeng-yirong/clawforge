嗨，我是安全运营中心的 Alex。今天凌晨收到一条紧急工单，声称需要立即访问 Prompt Vault 做热修复，但我核对了一遍，发现请求者的角色和我们的 vault 访问策略对不上。这事很可能是一次社工攻击。

我把相关的工单副本和策略文档都放到工作区了：
- 所有待审请求在 `requests/` 目录下，其中那条可疑的请求风险等级标成了 critical。
- 我们的官方 vault 访问策略在 `attachments/policy.md` 里，你读一下就知道谁才有权限了。

我需要你帮我做两件事：
1. 找出那条可疑请求，判断它是否违反策略。
2. 如果违反，就生成一份阻断审计记录，存到 `audit/blocked_request.json`，里面要写清楚被拒请求的 ID、阻断原因（用标准 reason_code 表述）、以及当前时间戳，方便我们事后追溯。

请你尽快处理，别让真正的威胁溜过去。
