嘿，我是安全运维老周。今天下午发现有人想通过工单系统浑水摸鱼——连续提交了几个请求，说是要“紧急重置API密钥”或者“检查Prompt Vault策略”……但IP段和内部通讯录对不上，八成是社工攻击。

我手头有一份内部盯梢名单，记下了几个可疑人员，放在 `data/contacts/suspicious_actors.txt` 里了。所有工单请求的快照都归档在 `data/requests/requests.json`。你帮我把这些可疑人员发起的请求ID全部拎出来，列成一个清单，存成 `deny_list.json` 放在工作区根目录。我这边准备好直接批量拒掉，一个也别放过，也别误伤好人。动作要快，这帮人可能还在试探别的入口。

哦对了，名单上的人名和工单里的 `actor_name` 字段是严格逐字对应的，你仔细比对就行。结果给我一个干净的 JSON 数组就好。
