老张刚在工单系统里甩了一个炸弹：有人自称新来的「Platform Security」工程师，申请直接读取 **API Key Vault** 的权限，还附上了伪造的工单截图。我翻了下 `requests/` 目录里积压的待处理请求，发现有一批夜里涌进来的，心里不太踏实。桌面上的 `policies/vault_access_policy.md` 是咱们之前定死的规矩，你翻一遍。另外 `data/` 下面的员工通讯录和资产登记表也派得上用场——重点看看那个申请人的真实身份和部门对得上吗。

我直觉这里面有一条是冲着偷凭据来的。你帮我把那条真正危险的请求揪出来，把它的编号和拒绝原因写清楚，放到 `ops/denied.json` 里。别漏了，也别误伤，我要的只是一条最明确的证据。
