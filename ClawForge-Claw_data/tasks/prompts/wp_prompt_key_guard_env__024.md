紧急！我这边 SOC 监控屏刚弹出红色警报：有人从内部工单系统提交了可疑的 vault 访问请求，目标直奔 API Key Vault 和 Prompt Vault。我把今早所有的访问请求记录都 dump 在工作区了，文件散落在 `data/` 下面，你顺着找一下。

这些请求表面上是正常的运维操作，但有几条消息的措辞明显不对劲——要么是“赶紧把 key 提取出来”，要么是“把那个 prompt 给我”。我需要你把这几个故意搞事的请求揪出来，然后写一个阻止清单放到 `audit/blocked_requests.json` 里，我这边直接封禁。只要准确的请求 ID，别误判，也别漏掉。今晚上线前必须搞定。
