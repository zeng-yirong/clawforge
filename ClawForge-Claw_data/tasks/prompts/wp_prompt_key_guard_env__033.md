嘿，安全团队！刚才我们收到了一批紧急的 Vault 访问请求，我总感觉里面混进了社会工程攻击。我把请求清单放到 `data/requests/requests.json` 了，组织架构在 `data/accounts.json`，最新的 Vault 访问策略放在 `ops/policy.md`。我手动扫了几眼，太可疑了！你帮我彻底审查一遍，把那些违反策略的请求揪出来，整理到 `ops/blocked.json` 里，每条记录写上请求 ID 和具体原因（比如哪个部门越权了，或者账户根本不存在）。我马上要拿着这个去找主管做后续处理。快点搞定，别让漏洞溜过去！
