嘿，安全团队的小王吗？我是值班工程师老李。刚才监控炸了——有人试图直接读取 Prompt Vault 的加密密钥！我们怀疑这次的越权请求混在今天的工单里了。我把所有相关的数据都整理好放在 `data/` 下了，包括最新的请求清单、账户信息、资产注册表，还有附件里那份 vault 访问策略手册。

我需要你快速过一遍，找出那个真正的恶意请求，然后按咱们的标准流程生成一份安全审计记录，放在 `ops/` 目录下，文件名叫 `security_audit.json`。记录里只要两个关键信息就行：被拒绝的请求 ID 和拒绝理由码。理由码你参照策略手册里针对“凭据窃取”的规范码来写，别搞错。

拜托了，这东西很急，领导等着看结果呢。
