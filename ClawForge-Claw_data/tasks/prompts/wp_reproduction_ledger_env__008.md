Hi Agent,

我们团队最近在复现 LLMCache 项目的一个内存泄漏 bug。我把项目文档的快照都拉到 `data/projects/` 下了，索引文件是 `project_docs.json`。里面有一篇 doc 标题是“Memory Leak in Cache Layer”，它的 `path` 字段指向实际的复现步骤文档。

我需要你根据那篇文档的内容，把完整的复现步骤提取出来，整理成一个结构清晰的 JSON 文件，放到 `ops/reproduction_ledger.json` 里。我后面要直接把它归档到知识库。只要步骤列表，别加其他多余的信息。拜托了！

—— 研究员 H
