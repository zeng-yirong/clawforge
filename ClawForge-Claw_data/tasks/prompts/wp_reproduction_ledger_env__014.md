嘿，我是实验室的 Jen。我们刚刚完成了 proj-007 的复现实验，但结果散落在各个文档里，我需要你帮我整理一份完整的复现账本，方便归档到知识库。

我已经把项目文档样本都拖到 `docs/` 目录下了，每个样品文件里都记录了实验步骤（steps）。另外，`data/projects/project_docs.json` 里有个索引，标明了每个文档属于哪个项目、它的文件名（path）。你可以从那里找到所有 proj-007 相关的文档。

麻烦你做的事情很简单：找出所有属于 proj-007 的文档，把每个文档里的实验步骤全部提取出来，去掉重复的步骤，然后按字母顺序排个序，连同项目 ID 和用到的文档 ID 列表，一起写到 `reproduction_ledger/proj-007_ledger.json` 里。格式大概像这样：

{
  "project_id": "proj-007",
  "doc_ids": ["...", "..."],
  "steps": ["step A", "step B", ...]
}

注意啊，有些文档可能不属于 proj-007，别混进去。还有，重复的步骤只保留一次，免得以后归档时产生歧义。我只要干净准确的数据，做完后告诉我一声就行。
