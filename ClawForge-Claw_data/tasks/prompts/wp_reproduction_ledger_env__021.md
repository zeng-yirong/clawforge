嘿，我是实验室的运维。最近我们在归档开源项目复现记录时发现数据乱成一锅粥了——有人把草稿和正式记录混在一起，还有重复提交和缺字段的无效条目，再这样下去知识库根本没法用。

我把所有零散的项目文档都丢在 `data/` 下面了：主文件 `project_docs.json` 里有一些记录，但混杂了无效项；几个草稿子目录（比如 `drafts/`）里也有其他人随手塞的 JSON 文件，其中可能包含有用的复现数据，但也可能混着过时的标记或重复内容。你帮我把这些乱七八糟的记录整理成一份干净的存档：**只保留那些真正有效、不重复、且信息完整的项目文档条目**，按文档编号排好序，统一存入 `ledger/reproduction_ledger.json`。

我只要最终能直接拿去归档的干净列表，别给我中间步骤。有效记录需要包含所有关键信息：文档 ID、项目 ID、标题、文件路径，缺一不可。重复的只留最新版本（看时间戳，没有时间戳的视为旧版本直接扔掉）。那些明确标记为过时（status 为 obsolete）的条目也直接忽略。`data/` 下其他的无关文件（比如账号、联系人）就别管了。

搞定了告诉我在哪，我直接拿去做归档。
