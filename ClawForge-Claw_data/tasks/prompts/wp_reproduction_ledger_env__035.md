张三研究员在团队群里发了一条消息：

> 兄弟们，我这边在整理 `data/projects/project_docs.json` 里列的那些复现记录，发现里面很多文档路径已经失效了，或者元数据格式乱七八糟。我现在急需一个准确的工作量预估——把所有**实际存在、且元数据中明确标记为可复现**的文档的步骤数量加起来，看看总共有多少步，涉及多少个文档。这样我才能跟老板说我们到底完成了多少可再现的测试。
> 
> 我已经把文档的元数据放在了每个 `.md` 文件的开头（用 `---` 包裹的那个区块），里面有 `reproducible` 和 `steps` 字段。但请务必先检查文件本身是否存在，以及元数据字段是否完整、合法。那些路径指向不存在的文件、或者 `reproducible` 不是 `true`（大小写敏感）、或者 `steps` 不是正整数的，统统不要纳入统计。
> 
> 最终结果统一放到 `ops/reproducibility_score.json` 里，我就只认这个文件。格式我不管，但必须包含总步骤数和贡献文档数两个信息。辛苦大家帮忙跑一下，今晚之前要给老板看。
