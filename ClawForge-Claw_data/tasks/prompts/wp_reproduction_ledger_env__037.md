嘿，昨晚我们团队在复现 open source 项目 alpha 的一个 bug 时折腾到半夜。最终有一个复现跑通了，其他几个都挂了。我现在需要把那个成功的复现记录整理归档，方便以后回溯。

你是新来的实习生，手边应该有我们工作区的数据：

- `data/accounts.json` – 团队成员账号，包括谁在哪个组
- `data/contacts.json` – 项目相关的外部联系人，比如文档作者
- `data/projects/project_docs.json` – 项目文档清单，里面有文档的路径
- `data/reproduction_logs/` – 昨晚每人跑复现时留下的日志文件，文件名是 `{姓名}_{日期}.log`，里面会记录复现状态和引用的文档 ID

我已经把日志文件丢那了，但还没整理。你帮我从里面捞出那个状态为“verified”的唯一一条复现记录，然后结合账号和联系人信息，把完整档案生成出来。

我的要求很简单：

1. 档案要放在工作区根目录，文件名叫 `ledger_archive.json`。
2. 档案里必须包含：这次复现对应的文档 ID、项目 ID、复现结果状态，还有负责这个复现的团队成员显示名字（display_name），以及这个文档对应的联系人名字（name）。
3. 其他无关的日志、无关的账号、无关联系人统统不要混进来。

我不需要你写报告、发邮件，只要一个干净利落的 JSON 文件就行。快搞，今早例会要汇报。
