嘿，小张，整理复现账本这事儿又得麻烦你。我们团队刚刚从几个项目里收集了一堆 scenario 记录，都扔在 `scenarios/` 目录下了。但你知道的，里面有不少半成品、草稿，甚至有人把没写完的步骤也塞进去了。我现在需要一个干净可用的复现账本归档，直接存档到 `archive/` 目录下，文件名就叫 `reproduction_ledger.json`。

具体要求嘛 —— 只要那个已经被验证为真正可复现的 scenario 的完整信息：包括它的 ID、对应的项目名称、详细的复现步骤列表，以及它的验证状态。别把那些还在 draft 或者步骤不完整的混进来，我只要一个，清清楚楚的。格式就用 JSON 好了，字段名你按我们平时归档的习惯来就行。弄好了我直接拿去入库。
