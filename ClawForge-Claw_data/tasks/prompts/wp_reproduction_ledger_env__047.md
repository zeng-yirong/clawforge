嘿，实验室的复现记录又乱糟糟的了。我前两天在跑那个 #447 的 bug，前后试了好几种环境，记了三四个版本的复现笔记，现在完全分不清哪个是最终定稿。项目文档目录下应该有一个叫 `data/projects/project_docs.json` 的索引，里面列了所有的文档记录，对应的实际文件放在 `docs/` 下面。你帮我把那个真正完整的、被标记为“最终版”的复现 ledger 找出来。我需要你把那份复现报告里的 **Reproduction ID** 和 **Result** 字段揪出来，单独归档到 `ops/reproduction_ledger.json` 里，格式你自己看着办，但得让我一眼就能拿到那两个值。其他那些半成品或者干扰项就别管了，我要的是准确的、唯一的那一份。快点搞完，测试那边还在等这个归档才能继续推。
