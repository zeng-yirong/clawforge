你是一名资深招聘专员，正在处理季度招聘高峰。我把最新的候选人库和职位池都整理好了，分别放在 `data/candidates/candidates.json` 和 `data/jobs/jobs.json` 里。但我实在没时间手工一个一个匹配——以往总因为漏掉最佳人选，被业务部门投诉。

现在我需要你帮我做两件事：
1. 为每个开放的职位（`status` 为 `open`）找到**唯一最匹配**的候选人。匹配规则很简单：看候选人技能集与职位要求技能集的**重叠技能数量**，重叠最多的胜出。如果出现并列，就选候选人名字字母序（按 `candidate_name` 字典序）最小的那个。
2. 把匹配结果整理成一份面试安排表，面试时间统一设为 **2025-04-15 10:00**（格式用 ISO 8601），并为每条面试记录添加一条提醒，提醒时间设在面试时间前 **30 分钟**。把这份安排表放到 `ops/interview_schedule.json` 里，结构清楚、字段完整即可。

注意：候选人库里可能有一些格式不完整的记录（比如技能字段为空），这些候选人直接忽略，不能参与匹配。职位池里已关闭的职位也不要匹配。另外，有些候选人可能出现多次（相同 `candidate_id` 重复），去重后只保留第一次出现的记录。

简单说——帮我摆脱手工烦恼，输出一份可信、可直接用的面试排期。
