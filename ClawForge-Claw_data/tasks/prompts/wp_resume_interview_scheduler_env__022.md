👋 你好！我是人力资源部的 Olivia。我们遇到了个大麻烦——最近的招聘季简历堆成山，我手动匹配职位和候选人已经连续加班三天了，而且老是被业务部门催着安排面试。更糟的是，上周因为漏掉一个候选人的技能，面试官白跑了一趟，大家都闹得很不愉快。

我需要你帮我把这件事彻底自动化，而且必须一次搞定，不能再出错了。

**背景**：我们公司目前在招三个岗位，所有职位信息都在 `data/jobs/jobs.json` 里，应聘者的简历资料我整理成了 `data/candidates/candidates.json`。另外，面试官的联系方式在 `data/contacts/contacts.json` 里（公司内部）。各个部门约定俗成的面试时间配置，我放在了 `ops/config.json` 里，你跟着那个来就行。还有，之前手动安排过的面试记录（包括无效的、取消的）都在 `ops/past_interviews.json` 里，那里面的事你别碰，我不要重复安排。

**你的任务**：
1. **精准匹配**：把每个活跃职位（状态是 `active` 的）和候选人做技能匹配。注意，候选人必须掌握该职位要求的**全部**核心技能（一个都不能少），大小写必须完全一致。那些技能列表为空的、或者明显有拼写错误的简历（比如“Python”写成“python”）都不要考虑。
2. **安排面试**：每个匹配成功的组合，都生成一条面试记录。面试官统一用 `data/contacts/contacts.json` 里角色是 `interviewer` 的那位。面试时间就用 `ops/config.json` 里默认的时间点。另外，每个面试都需要设置一个提醒，提醒时间要比面试时间早30分钟。
3. **输出结果**：把最终的面试计划写到一个文件里，就叫 `ops/interview_plan.json`。格式我不管，但必须包含每个面试的职位 ID、候选人 ID、面试官 ID、面试时间、提醒时间和状态（状态用 `"scheduled"`）。为了后续系统好处理，请按照职位 ID 的字母顺序排列记录。

**注意**：
- 我这边的文件都是用 UTF-8 编码的标准 JSON，但你也留个心眼，可能会有一些历史遗留的非 JSON 文件混在目录里（比如 .txt 的草稿），那些直接忽略就行。
- 别去动 `ops/past_interviews.json`，那里面的记录已经过时了，只读，别写入。
- 完成后在 `ops/interview_plan.json` 里给个干净的、可直接导入调度系统的结果。

拜托了，别再让我熬夜了！😭
