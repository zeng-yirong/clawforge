嘿，我是HR组的Linda，急！昨天实习生把候选人匹配流程跑错了，现在所有面试安排都作废。我刚把最新的职位列表和候选人库整理好，分别放在 `data/jobs/jobs.json` 和 `data/candidates/candidates.json` 里。另外，之前误操作占用了明天（2025-03-10）上午9:00到9:30的一个临时面试，记录在 `data/schedule.json`，但那个不算数，我们重新来。

请帮我做两件事：
1. 每个职位选出最匹配的候选人——技能要最大程度覆盖职位要求，如果两个人覆盖数一样，就选候选人ID小的那个。
2. 从明天（2025-03-10）上午9:00开始，按职位ID升序，为这些候选人安排30分钟的面试，跳过已经被占用的时段，把结果写到 `ops/interviews.json`。

格式就一个JSON数组，每个元素是一个对象，包含三个字段：`job_id`（职位ID）、`candidate_id`（候选人ID）、`interview_time`（面试开始时间，ISO 8601格式）。我只要这个文件，别的不需要。多谢啦！
