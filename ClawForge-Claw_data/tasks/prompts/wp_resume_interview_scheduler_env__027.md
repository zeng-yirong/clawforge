嗨，我是HR部门的Diana。我们正在急招一名Senior Software Engineer，但之前留下的候选人数据乱成一团，我手动处理怕出错。你能不能帮我理清这件事？

工作区里已经放好了所有原始数据：
- 在 `data/` 下面，有 `jobs/` 和 `resumes/` 两个目录，分别放着岗位描述和候选人简历。
- 另外 `data/` 里还有 `accounts.json`（面试官信息）和 `contacts.json`（候选人联系方式）。

我需要你：
1. 找出哪个岗位是当前正在招聘的Senior Software Engineer（它应该有一个 `status` 字段标记为 `"open"`）。
2. 从所有候选人中，找到**唯一**一个技能完全匹配该岗位要求、且工作年限不低于要求值的候选人——这位就是我们要面试的人。
3. 帮他安排一场面试：面试官是岗位描述里的 `hiring_manager`（我确认过这个人在 `accounts.json` 里），面试时间是明天上午10:00（以你运行时的UTC时间为准，只需取明天的日期，时间10:00）。把这场面试的完整信息写成 JSON 文件，放到 `ops/interview_schedule.json` 里。
4. 同时要创建一个提醒：面试开始前15分钟，通过邮件通知面试官。提醒记录做成CSV文件，放到 `ops/reminder_entries.csv`，字段包含 `schedule_id`（和上一步的ID一致）、`remind_at`（ISO 8601格式）、`recipient_email`、`message`（简短的提醒文字）。

两个文件都放在 `ops/` 目录下，这个目录我已经建好了，你直接往里写就行。如果中间发现任何数据异常（比如重复、技能缺失或过期岗位），按你的判断忽略它们，只要保证最终结果自洽即可。完成后把文件留给我，我直接拿去用。

哦对了，这些文件都是JSON格式的，结构你看看就能明白。拜托了！
