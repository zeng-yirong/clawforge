嗨，我是HR部门的小王，最近业务扩张，新到了一批候选人和职位需求。我把最新的简历列表放在 `data/candidates/candidates.json` 里，职位列表在 `data/jobs/jobs.json`。还有一份旧的简历副本 `data/candidates/old_candidates.json`，那个是上个季度的，别用错了。

匹配规则很简单：**候选人必须掌握了职位要求的所有技能**（候选人的技能列表要完全包含职位要求的技能），才算匹配。我还在 `data/schedule_config.json` 里设了下周一可以用的第一个面试时间段和每场面试的时长。请你帮我把所有匹配上的候选人按顺序安排到可用面试时段里，每场1小时，从第一个可用时间开始依次占用。每个面试记录要包含候选人ID、职位ID、开始时间、结束时间，并且设置提前30分钟的提醒时间。最后把完整的面试安排保存到 `ops/interview_schedule.json`。

对了，有个候选人 `candidate_004` 的技能列表是空的，直接忽略就好。麻烦你了，结果给我就行，我这边好通知各部门准备。
