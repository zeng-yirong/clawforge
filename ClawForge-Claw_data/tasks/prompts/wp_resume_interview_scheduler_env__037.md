你好，我是招聘经理Alice。我们团队急需为 **Senior Data Engineer** 职位安排面试，但后台数据有点乱，我一时理不清。职位详情放在 `jobs/jobs.json` 里，候选人的投递记录在 `candidates/candidates.json`。另外，面试官联系方式在 `contacts.json` 中。

我需要你帮我做一件事：从候选人中挑出**技能完全匹配**这个职位要求的，然后按候选人姓名从 A 到 Z 排好序，为每位候选人指定一位面试官（就用联系人里的那位就行）。最后把这份面试计划整理成 JSON 文件，放到 `output/interview_plan.json` 里。文件内容要清晰，让我能直接拿去排日程。

之前系统卡顿过，可能有些旧数据混进去了，你看着处理，别让那些无效或过时的记录干扰最终结果。我只想要一份干净、准确的计划。
