嘿，团队：

刚把最新的职位需求整理到 `data/jobs/jobs.json` 了，候选人的技能档案在 `data/candidates/candidates.json`。我们需要自动安排面试，规则很简单——候选人必须**完全掌握**职位列出的所有必备技能（一个都不能少）才算匹配。请为每一对匹配生成一条面试记录，面试日期统一设为 **2025-06-02**，状态标记为 `scheduled`。另外，每条记录加上一个唯一标识 `interview_id`（用候选人ID和下划线加职位ID拼接，例如 `C001_J001`）。

最终结果输出到 `interview_schedule.json`，放在工作区根目录。请确保没有遗漏任何匹配，也别混入不匹配的。辛苦了！
