嘿，我是招聘团队的小王。刚刚从业务那边接到一个紧急需求——我们后端基础架构组有个 Senior DevOps 工程师的坑位，今天必须安排面试，不然候选人就要被别家抢走了。我手忙脚乱的，你帮我处理一下吧。

相关文件都放在工作区里了：职位描述在 `data/jobs/` 目录下的 `jobs.json` 里，候选人的简历摘要和技能列表在 `data/candidates/candidates.json`。另外，我们之前已经排过一些面试，记录在 `data/schedules/` 目录下，里面可能有些已经不用的草稿或者过期记录，别管它们。

你要做的是：
1. 找到那个标题叫 "Senior DevOps Engineer" 的职位，看看它要求的技能有哪些。
2. 从候选人里找出技能 **完全匹配** 的那个——注意，匹配时忽略技能字符串里额外的空格和大小写，比如 "Kubernetes " 和 "Kubernetes" 算一样。
3. 然后，为这位候选人创建一个新的面试邀请记录，存到 `data/schedules/new_interview.json`。面试时间就定在明天（系统日期写在 `data/today.txt` 里，你读一下就知道今天几号了）。邀请状态写成 `"scheduled"`，另外记得带上候选人 ID、职位 ID 和日期字段。

我只要准确的结果，别给我搞出多余的文件或者乱七八糟的记录。弄完我直接拿去发给 interviewer 了，谢啦！
