主题：紧急 - 自动空调控制

嘿，AI助手，

今晚我要远程开启一些空调，但家里传感器有点乱。我把设备清单放在 `data/devices.json` 了，最新的传感器数据在 `data/sensors.csv`。麻烦你帮我算算哪些房间的温度超过了空调的目标温度，需要开启制冷。注意传感器文件可能有脏数据（空行、注释、缺失值），你处理时小心。把结果写到 `ops/action.json`，用JSON格式，包含设备ID和操作指令。我只想看到需要开启的设备，其他的别管。

谢了！
