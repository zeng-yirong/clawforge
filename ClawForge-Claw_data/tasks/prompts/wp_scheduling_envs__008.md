Hey，我是小明，能源监控那边。昨晚系统报警，显示卧室和客厅的调度有重叠，特别是加湿器和空调同时开着，能耗飙升。我把系统导出数据放在了 `data/` 目录下，`accounts.json` 里有每个家庭的账户和它们的调度安排，`devices/devices.json` 里是所有设备的详细资料。你帮我分析一下，所有有效设备（忽略那些出错的或已删除的）之间的调度重叠情况，找出重叠总时长最严重的那个设备对，把这两个设备的 ID 和总重叠时长（分钟）写到 `ops/peak_conflict.json` 里。我等着拿去跟住户沟通。拜托了！
