小王，麻烦帮我看一下卧室的加湿器调度！我前天设了个自动加湿方案，想让卧室加湿器从早上7点一直开到晚上，结果今天湿度还是低得可怜。我猜八成是哪个调度在中间把加湿器给关了。所有的设备清单和调度记录都在 `config/` 目录下，设备叫 `devices.json`，调度叫 `schedules.json`。你帮我从里面揪出那个捣乱的调度 ID，存到 `ops/humidifier_fix.json` 里，格式就写 `{"bad_schedule_ids": ["..."]}` 就行，我等着拿它去删掉。别整别的，我只要 ID 啊！
