嗨，我是这家的主人。最近家里的智能设备调度总出岔子，昨晚客厅空调应该10点自动关，结果一直开着到今早。

我把设备清单和调度记录放在 `data/devices.json` 和 `data/schedules.json` 里了。调度记录里每个任务都有状态（已执行、待执行、错过等），还有计划的执行时间。

现在是 **2025年4月5日晚上10点整**（2025-04-05T22:00）。麻烦你帮我找出所有“应该在这个时间点之前已经执行、但实际状态却是未执行”的调度任务（包括状态为 `pending` 且计划时间已过，或者状态为 `missed` 的）。把这些任务整理成一个列表，写到 `ops/pending_tasks.json` 里，每个任务至少包含调度ID和设备ID，方便我快速定位处理。

已经执行过的就不用管了，谢谢！
