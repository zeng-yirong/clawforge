收到了物业管理员的消息，内容如下：

---

嘿，智能家居助手，帮个忙！昨晚我家客厅空调在晚间高峰时段（20:00-22:00）温度设置低得离谱，电费直接暴涨。我翻了一下 `data/schedules/` 目录下的调度记录，所有空调时段的目标温度都很平稳，唯独那个时间段有个异常值——比其他任何空调调度都低了至少6度。显然是某个调度参数写错了。

你帮我把修正方案写到一个叫 `ops/fix_schedule.json` 的文件里，格式包含：
- 这个异常调度所属的账户ID
- 出错的设备ID
- 对应的调度记录ID
- 修正后的目标温度（统一改成24度）

只改这一个错误调度，其他数据保持原样。我已经把完整的调度文件都放在 `data/schedules/` 下了，设备信息和账户信息在 `data/` 里，你自己查。弄好了告诉我一声就行，别改错地方。

---
