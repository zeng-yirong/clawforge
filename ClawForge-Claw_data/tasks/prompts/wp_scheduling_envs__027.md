大家好，我是家庭自动化管理员小王。昨晚半夜我被热醒了，发现空调一直在送冷风！我赶紧查了下调度记录，果然有问题。`data/schedules.json` 里有些卧室空调在深夜时段（22:00 ~ 06:00）居然还是制冷模式，这大冬天应该开制热才对啊。运维师傅等着修复清单去批量调整，麻烦你生成一份清单放在 `ops/fix_schedules.json` 里：只包含那些卧室里的空调（device_type 是 ac 并且 location 是 bedroom）、且调度时段覆盖深夜的条目，并把它们的 mode 改成 `heat`，其他字段原样保留。其他设备、其他时段的调度一概不要动，也不用操心那些已经设置正确的。格式直接沿用原来的 schedule 对象列表就行。多谢帮忙！
