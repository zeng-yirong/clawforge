嘿，家里最近出了件怪事——卧室那个加湿器，每天晚上10点都会自己打开，明明我只设过一次定时。我翻了翻数据库，发现`data/`下面有账户和设备清单，`schedules/`里存了所有的定时任务。我怀疑是有人不小心多加了一条一模一样的调度，导致重复执行。你帮我把调度表理一遍，找出那个不该存在的重复调度，把它的ID记下来，放到`ops/fix_target.json`里。格式用数组包一下，方便我直接丢给清理脚本。注意看仔细，别把其他正常的调度也算错了。
