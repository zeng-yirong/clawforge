**主题：紧急调度优化请求**  
**发件人：家庭自动化管理员 <admin@smart-home.local>**  
**收件人：自动化助手**  

助手，家里最近电费账单涨了40%！我翻了一下 `rules.conf` 里我们之前定的节能规则，又对比了 `schedules/` 目录下最近的调度记录和 `logs/energy_usage.csv` 的能耗数据，发现有些调度明显在“反着规则跑”。

具体来说，有两个问题让我很头疼：  
1. 工作日下午的高峰电价时段，空调还在呼呼地开。  
2. 同一个卧室里，空调和加湿器同时运行，这不是对冲吗？  

我不需要你把所有调度都列出来，也不需要你改规则——只要找出那些**当前正在违反规则**的调度，把它们的 ID 和应该怎么修正（比如改时间或者直接关掉）写到 `ops/overrides.json` 里就行。格式你看着办，要让我能直接喂给调度系统。  

注意：只处理那些**明确违规**的，其他调度不要动。文件已经在工作区放好了，你先看看再说。
