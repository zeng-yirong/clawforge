嘿，我的客厅空调最近下午老是闹脾气！我在 `data/schedules/` 里看了一眼，好像有个老掉牙的调度在捣乱，温度设得怪怪的。你帮我顺一下设备信息都在 `data/devices/` 里，把那个空调下午该有的正常调度整出来。直接在 `ops/` 下面创建一个修复文件，就叫 `fix_afternoon_ac.json`，让它在下午时段自动打开到24度就行。我下午回家可不想蒸桑拿！  
——着急的房主
