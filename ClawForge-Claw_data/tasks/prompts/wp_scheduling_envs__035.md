嘿，我是这栋楼的智能家居管理员。昨晚真是折腾死了！凌晨2点到4点之间，客厅和卧室的灯、空调、加湿器像是抽风一样同时开，搞得电费飙升，还吵醒了住户。我怀疑是调度文件里有冲突——同一时间、同一房间扎堆安排了多个设备。

我把所有记录都塞到工作区了：`data/schedules.json` 是整个月的调度表，里面有些条目是测试用的过期垃圾，还有些设备ID已经停用了，你可别把它们算进去。另外 `logs/` 目录下有设备日志，里面记录了每次调度的实际执行时间，不过有几份日志是旧版本的备份，带 `_bak` 后缀的别碰。

我需要你帮我找出所有[同一小时、同一房间]内至少有两台设备被调度的冲突事件。把结果整理成一份简洁的报告，放到 `ops/conflicts.json` 里。格式随便，但每个冲突必须写明：具体时间（精确到小时）、房间名、冲突的设备ID列表。我只看这个文件，别漏也别多。

对了，如果调度里某个设备ID在 `data/devices.json` 里找不到，那它就是无效设备，忽略它。另外，调度里的 `valid` 字段如果是 `false` 也别管。拜托了，今天必须搞定！
