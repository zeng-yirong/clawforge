主题：家里智能灯下午抽风了！

我是屋主小王。我家那盏卧室灯（Bedroom Light）今天下午老是忽明忽暗，明明没人在家，它自己开开关关。我怀疑是调度系统里有冲突的指令——同一个时间点，一个让它开，一个让它关。

我把今天的设备清单和调度记录导出来了，放在 `data/devices/devices.json` 和 `data/schedules.json` 里。你帮我把今天下午3点到5点之间（也就是15:00到17:00）的那些调度翻一翻，找出那些在**同一时刻、对同一个设备**下达了**相反动作**（一个开、一个关）的调度，把它们的 ID 列出来，放在 `ops/conflict_schedules.json` 里给我。我只要那些闯祸的调度编号就行，剩下的我来处理。

注意：有些调度可能时间不在那个区间，或者控制的是房间里根本不存在的设备，那些就别管了。我只要准确的、真正冲突的那几个 ID。
