嗨，我是家里管智能设备的。昨晚邻居又在群里抱怨，说我家客厅空调半夜轰隆隆的。我查了一下系统日志，确实发现客厅空调凌晨还在运转，明明家里的自动化规则应该22:00自动关掉的。我怀疑有些设备的调度没生效，或者有人手动开了没关。

我把目前最新的设备清单和调度规则都整理出来了，放在了工作区的 `data/` 目录下。另外我还随手记了一个当前时间戳在 `data/current_time.txt` 里，方便你对照。调度规则里面有些是旧的备份，注意别搞混了。

我需要你帮我把**所有应该已经关掉但还亮着/运行着的设备**找出来，然后生成一个明确的关机指令清单，放到 `ops/auto_off.json` 里。格式很简单：一个数组，每个元素写上设备的 ID 和要执行的动作（比如 `turn_off`）。只列那些确实需要关的，别多关也别漏关。拜托了，我可不想再被邻居找上门！
