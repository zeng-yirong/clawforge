你好！我是这栋房子的主人。最近电费账单高得离谱，我注意到客厅的空调和加湿器总是在半夜同时开启，可能是调度冲突。我把所有设备的配置和调度信息都放在了工作区的 `data/` 目录下。

- `data/accounts.json` 包含了我家（Home）和另一个办公室的账户信息，每个账户下面有设备列表和相应的调度安排。
- `data/devices/devices.json` 包含所有设备的详细参数。

请你帮我分析一下，找出所有**在同一时间段内运行**的调度对（特别是空调和加湿器），然后把这些**导致冲突的调度ID**以 JSON 数组的形式保存到工作区的 `ops/conflict_schedule.json` 文件中。

注意：有些调度是跨天的（例如晚上10点到早上6点），请正确判断重叠。另外，办公室的调度应该不会影响到我家，但你还是需要检查全部数据。

完成后回复我即可。
