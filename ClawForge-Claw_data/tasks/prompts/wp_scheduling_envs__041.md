**主题：紧急 – 客厅空调调度冲突**

Hi 运维同学，

我家客厅的空调（设备 ID: dev_ac_livingroom）最近一到晚上就频繁自动开关，搞得全家都睡不好。我怀疑是之前设的自动调度表里有冲突——两个调度的执行时间重叠，导致空调一会儿开一会儿关。

我把当前的调度记录放在了工作区的 `data/schedules.json`，设备清单在 `data/devices/devices.json`，麻烦你帮我查一下：

- 哪些调度在**同一时间段**控制了**同一台设备**？
- 找出导致冲突的调度 ID，把它们放到 `ops/fix_list.json` 里，我准备手动停掉这批冲突项。

注意：有些调度记录可能有缺失字段或格式问题，请自行跳过无效条目。我只想要准确的、冲突的调度 ID 列表，不要无关的。

辛苦了，今晚之前搞定。
