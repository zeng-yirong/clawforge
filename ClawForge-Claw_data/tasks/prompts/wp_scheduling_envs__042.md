嘿，家里客厅的空调和加湿器昨晚又搞事情了！我查了仪表盘，这两个家伙同时从22点开始运行，搞得湿度飙升然后空调猛吹，电费都快炸了。记得我们之前定过规矩：同一个房间里 AC 和 humidifier 不能同时开，会互相干扰还跳闸。

我把所有设备的配置和调度数据都丢在 `data/` 里面了，你帮我翻一翻，找出哪些调度冲突了，然后出一个修正版放到 `ops/fixed_schedules.json`。我只想让你调整那个该后运行的设备，其他调度保持原样，每个调度的 ID 也别改。拜托了！搞定后跟我说一声。
