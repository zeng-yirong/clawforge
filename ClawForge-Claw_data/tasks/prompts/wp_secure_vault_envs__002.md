嘿，运维团队的老哥，刚收到安全审计的加急邮件。他们指出我们 vault 里那些低优先级的凭据居然一直没有专门的清单，万一泄露了都没法快速溯源。我这边月底要交整改报告，需要你帮忙把“低风险”（priority=low）类别下的所有凭据整理出来。

我手头有一部分数据：`vault.json` 里记录了所有凭据，每个凭据都带有 `category_id`；`categories.json` 里定义了类别和对应的优先级。其他一些散落的日志、旧配置什么的不用管，都是历史遗留垃圾。你只需要捞出低优先级的凭据，把每个凭据的 `name` 和 `username` 提取出来，按 `name` 字典序排好，写到 `ops/low_risk_credentials.json` 里。格式就用标准的 JSON 数组，每一项一个对象，只保留 `name` 和 `username` 两个字段就行。

对了，注意剔除那些 category_id 在 `categories.json` 里找不到对应定义的记录，还有任何明显重复或无效的数据也别漏进去——但不用管密码强度或过期时间，只按 priority 和 category 过滤。我明天开会前要拿到文件，麻烦尽快搞定！
