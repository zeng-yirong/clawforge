哎，运维那边又炸了——几个同事的密码被撞库，还好发现得早。现在上面压着要我们立刻把所有遗留凭据翻出来清理一遍，不能再有弱口令、过期账号满天飞的情况了。

我这边有两份东西：`ops/legacy_credentials.csv` 是之前手动整理的清单，但里面混了好多早该报废的账号，有些甚至密码明文就写在表格里。另外 `ops/scattered_notes.txt` 是几个老员工随手记的备忘录，什么“测试账号”“临时密码”全是杂乱的文字，得自己想办法提炼出来。对了，我还从安全组拿了个 `data/known_breaches.txt`，里面是那些已经在暗网曝光的密码——这些密码绝对不能再用了。别问我为什么没整合，当初大家各干各的就成了这德行。

我需要你帮我把所有**还能用的**有效凭据找出来，按业务类别（工作邮箱、电商平台、社交媒体、银行账户）分好组，整理成一个干净的汇总报告。报告名字就叫 `vault_report.json`，格式你看着办，只要让我一眼能看出哪个类别下有哪些凭证就行，每个凭证要有平台、用户名、密码（既然是内部管理，明文留着没问题）以及它本身的强度分数。

另外，既然要彻底上正轨，顺便帮我把每个有效凭据的自动填充规则也生成一下。我们内网应用大多是用域名匹配的，你根据平台名推断一个合理的 url 模式（比如 `*.platform.com` 之类的），都写到 `autofill_rules.json` 里。

做完这两样东西就成。不用给我发总结，文件到位就好。时间紧，拜托了。```python
