嘿，运维团队在整理公司凭证库，发现一团糟。我们手头有一堆从旧系统导出的 CSV，放在 `vault/` 目录下，还有一些备份文件混在里面。我按你的要求把策略文件 `policy.json` 和分类规则 `category_rules.json` 也留在根目录了。

现在需要你干件事：把这些乱七八糟的凭证归拢一下，弄出一份干净的清单。具体要求我简单说下：

- 把 `vault/` 下所有真正的凭证文件合并起来（那些备份和无关文件别管）。
- 同一用户名和站点的记录可能有多条，留最新的那条就行，旧的丢掉。
- 有些凭证已经过期了（看看策略里怎么定义过期），过期的就别要了，免得被用错。
- 密码强度也得查一遍，不符合策略的换个新的上去，新密码用策略里给的默认密码。
- 每条凭证需要分个类，根据站点域名来，分类规则在 `category_rules.json` 里。
- 最后，按类别生成自动填充规则，方便以后浏览器一键登录。规则文件也标准化一下。

输出两个文件到工作目录根下：
- `cleaned_vault.json`  —— 清洗后的凭证列表
- `autofill_rules.json` —— 自动填充规则

格式就是 JSON，字段说明策略文件里都有。搞快点，老板等着下周上线。
