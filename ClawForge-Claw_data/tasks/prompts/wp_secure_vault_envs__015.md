**From:** Admin <admin@corp.com>  
**Subject:** 紧急：凭证库安全审计与自动填充配置  

Hi AI，  

最近合规检查发现我们的密码策略执行相当松懈。我把当前完整的凭证库放在 `vault/credentials/` 目录下了，分类规则在 `vault/categories/` 里。不过说实话，那个文件夹有点乱——前几天有人顺手丢了几份垃圾文件进去，甚至还有重复的条目，你处理的时候得睁大眼睛，只认那些格式规范、字段齐全的真凭证。  

我的需求很简单：  

1. 对所有有效凭证，按公司的密码强度标准过一遍。标准你知道的：**密码长度至少 8 位，并且必须包含至少一个数字和一个特殊符号**（像 @ # $ % ^ & 这种都算）。把那些不达标、欠揍的弱密码全部揪出来。  

2. 对每一个弱密码，记录它的 ID、它原本的密码、以及它属于哪个类别（别用分类 ID，用分类的名字，`vault/categories/` 下每个文件里写得很清楚）。  

3. 把结果整理成一个 JSON 数组，放到 `ops/audit_weak_passwords.json` 里。结构大概是每个元素包含 `id`、`original_password`、`category` 三个字段。  

**注意**：这次只是审计，**千万不要修改任何凭证文件**，也不要在里面加东西。我只想看看报告，后续要不要改密码再说。  

文件产出之后回我一句，我去看看结果。谢了！  

—— 老张
