Hey, 我是安全运维Alice。今天凌晨安全审计发现我们的凭据库存在严重隐患——大量密码强度不足，而且分类完全混乱。我已经把最新的导出文件扔在 `data/vault_dump.json` 里了，旁边还有一份 `data/category_definitions.json` 定义了公司目前的凭据类别和安全策略。

帮我把这几件事搞定：
1. 从 `vault_dump.json` 里挑出真正有效的凭据——那些字段残缺或密码为空的直接扔掉，重复ID只留第一条就行。
2. 根据银行账户类别的密码策略（长度≥12、含数字和特殊字符）判定每条有效记录的密码强度：满足的算强，不满足的算弱。
3. 把强的归到“银行账户”类别，弱的归到“工作邮箱”类别，最终生成一份分类后的凭据清单，放到 `ops/vault_classified.json` 里。
4. 另外要设自动填充规则：强密码的平台启用自动填充，弱密码的平台不启用，结果存成 `ops/autofill_rules.json`。

注意，旧备份目录 `data/backup/` 下的东西别管，那些是历史归档。干完告诉我一声，我直接拿去上线。谢了！
