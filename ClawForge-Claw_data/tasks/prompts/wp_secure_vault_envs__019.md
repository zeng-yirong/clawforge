嘿，我是安全运维小张。我们组遗留了一堆散乱的凭据，全扔在 `credentials/` 目录下了，文件很杂。旁边 `config/site_categories.json` 里有一份站点分类映射表，你得帮我把这些凭据按站点类型归好类，结果写到 `classified/` 下面，我后面审计要用。

另外，财务那边反馈银行账户类的凭据密码太弱了，安全组已经预生成了一批新密码放在 `pool/strong_passwords.json`里，你顺手把银行账户类的弱密码替换成对应的新密码，更新后的凭据单独存到 `vault/upgraded_bank_credentials.json`。

还有，电商平台类里那些密码强度够硬的（强度≥70），我需要给它们配好自动填充规则，方便用户一键登录。规则写到 `autofill/autofill_rules.json`，每条规则带上站点、用户名和当前密码就行。

注意：站点不在映射表里的凭据就别管了，省得添乱。分类归档用一个文件搞定，结构清晰点。搞快点，别交叉污染了。
