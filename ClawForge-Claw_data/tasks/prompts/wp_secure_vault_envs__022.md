**主题：紧急密码审计！**

老张，刚接到安全组通报，有人尝试撞库我们几个关键系统。我怀疑内部密码保险库有大量弱口令。我把 `vault/` 目录下最新的凭证导出 `vault_entries.csv` 放那了，你赶紧过一遍。

重点盯着 **工作邮箱** 和 **银行账户** 这两类 —— 它们优先级最高，万一泄露我们全完。凡是密码强度撑不住的，把记录摘出来，放到 `ops/weak_creds.json`。另外，工作邮箱下所有平台都要配上自动填充规则，规则文件扔 `ops/autofill_rules.json`，格式你按行业标准来就行。

我十分钟后回来，希望看到这两样东西。搞快点！
