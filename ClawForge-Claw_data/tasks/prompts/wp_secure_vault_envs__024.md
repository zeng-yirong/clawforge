嘿，运维老兄，刚收到安全审计的紧急邮件，说我们内网密码库管理一塌糊涂。我把那个凑合用的 `credentials.csv` 从旧服务器拷过来了，里面重复项多如牛毛，有几个甚至没写分类，密码也弱得跟纸糊的似的。

公司的安全策略你清楚吧？弱密码必须标出来，没分类的统统归到“未分类”里面，重复的凭证只留一条最新的（后面覆盖前面的）。我已经把公司官方的分类规则放在 `data/vault_schema.json` 里了，你参考一下。

花点时间跑一遍数据审计，把整理好的结果写成 `ops/vault_audit.json`，我直接拿这个去跟安全组汇报。注意，要列清楚每条凭证的原始 ID、名称、分类、密码强度（“强”或“弱”）以及建议操作（“保留”、“移除重复”或“升级密码”）。

别拖了，明早开会就要这玩意儿。文件都扔在工作区了，你搞吧。
