Mark，安全团队上周做了一次内部审计，发现 vault 仓库里不少凭据的密码强度堪忧，有些连基本要求都没达到。我把最新的凭据导出文件放你工位上了，就在 `vault_export.csv` 里，字段包括 `id`、`name`、`password`、`email` 和 `last_updated`。

我们实行的密码策略非常明确：密码长度至少 12 位，且必须包含大写字母、小写字母、数字和特殊符号（例如 `!@#$%^&*` 中的任意一个）。凡是不满足这三条的，都得标记出来。

我需要你快速把不符合策略的凭据整理成一个清单，放到 `ops/weak_credentials.json` 里。清单里每条记录只保留 `id` 和 `name` 两个字段，方便修复团队直接导入系统处理。注意：老的备份文件 `vault_backup_old.csv` 是历史数据，别弄混了。

这活儿挺急的，你看着办。
