嘿，安全团队的小张，紧急情况！昨天我们搞了一次全公司的密码导出，用来做合规审计。但你看看这堆数据——简直是一团乱麻。有些密码弱得像纸糊的，有些账户早就过期了，还有各种乱七八糟的分类命名。我可不想把这些脏数据直接交给审计组。

我这边已经把原始导出扔到 `credentials_dump.csv` 里了，另外我们之前定过的密码策略和分类标准也分别存在 `vault_policy.json` 和 `category_mapping.json` 里，你自己翻翻看。

听好了，我需要你干一件干净利落的事：**从这堆垃圾里把所有真正安全、没过期的凭据捞出来，按咱们统一的分类规整好，放进 `ops/secure_vault.json`**。审计组只认标准分类，别给我整什么“个人邮箱”之类的奇怪标签。另外，那些不合规的密码和过期账户直接扔掉，一个不留。

弄完告诉我一声就行，我这边等着拿去交差。
