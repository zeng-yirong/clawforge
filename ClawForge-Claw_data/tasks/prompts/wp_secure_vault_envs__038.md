嘿，安全团队刚跑完一轮内部审计，结果不太妙。我手头有几个散落在 `creds/` 目录下的凭据快照，来自不同平台——Google、Twitter、银行、电商，还有一个古老的备份。问题一堆：有些密码简直就是摆设（明眼人一看就知道弱），有些凭据连分类都没填，还有几个自动填充规则是空的。审计要求我们出一份标准化的**修复汇总报告**，放在 `ops/` 底下，文件名就叫 `audit_summary.json`。

我需要你帮我把这些散乱的凭据串起来，逐一检查：哪些密码该换、哪些分类需要补上、哪些自动填充规则缺失。具体的修复参考放在 `config/` 目录里了——密码策略、弱密码列表、替换映射、还有按平台分分类的建议。你根据这些规则，把要改的地方清清楚楚列出来就行，不用动原始文件。最终报告里告诉我：
- 哪些凭据密码太弱，以及我给它们准备了什么新密码；
- 哪些凭据缺少分类，以及我建议归到哪类；
- 哪些平台没有自动填充规则，以及我建议的默认规则是什么；
- 最后加一句“都处理完了”。

别漏掉那个老备份，它虽然旧，但一样要过一遍。报告格式要规整，每条记录都带凭据 ID。拜托了，这份报告我要直接提交给合规办。
