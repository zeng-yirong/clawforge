嘿，我是运维老张。咱们安全审计刚出来，发现一批社交媒体账号的密码强度太低了，必须立刻处理。

我把 vault/ 目录下的凭证快照拖下来了，主要就是那个 credentials.csv 文件，里面记录了所有凭证。其他几个旧备份别管，容易搞乱。

你现在帮我做两件事：

第一，把所有 **未过期**（expired 列是 no）且 **类别是社交媒体**，同时 **强度评分低于 50** 的凭证找出来。把这些凭证对应的平台名称（platform 列）去重后，按字母顺序整理好，存到 `ops/platforms.json` 里，格式为 JSON 数组。

第二，从刚才那一堆符合条件的凭证里，挑出 **密码长度最长** 的两个（如果长度相同，就选平台名字母靠前的那个）。把这两个凭证的 `username:password`（中间用冒号）按密码长度从长到短排列，然后用换行符 `\n` 连接成一个字符串，对整个字符串算一个 SHA256 哈希值，结果存到 `ops/credential_hash.json` 里，格式为 `{"hash": "..."}`。

注意，结果目录 `ops/` 目前还不存在，你创建一下。弄完跟我说一声，我要拿去跟安全部门对接。别搞错文件，也不要动原始数据。
