紧急通知：安全合规审查发现我们的密码库存在漏洞！刚刚导出的 vault 副本就在 `vault_backup/` 下面，里面有一个完整的凭据清单。公司新的统一安全策略要求：每个密码长度必须至少 12 位，并且同时包含大写字母、小写字母、数字和特殊字符。我需要你帮我把那些不合规的凭据 ID 全部揪出来，按升序整理好，放到 `ops/weak_passwords.json`。另外，老板想直观看到每个类别的安全状况——请按类别统计一下每个类别有多少条凭据，其中又有多少条不合规，整理成 CSV 文件放到 `ops/category_report.csv`，表头写 “category,total,non_compliant”。动作要快，我等着拿这些数据去跟安全组对线。
