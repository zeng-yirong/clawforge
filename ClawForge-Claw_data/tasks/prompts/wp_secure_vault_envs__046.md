**主题：紧急：密码库安全报告**

老李：

昨晚安全扫描让人揪心——我们的密码库一团乱麻，弱密码、重复记录、还有一堆不知道什么时候塞进去的僵尸凭证。我把原始数据全部拖到工作区的 `data/` 下面了：

- `vault_schema.json` —— 各个类别的密码策略和中文名称
- `credentials.json` —— 完整的凭证历史库（包括重复和无效记录）
- `audit_log.csv` —— 最近30天的访问记录，只处理这里面出现的活跃凭证

`backups/` 里有旧版本备份，别动它们，也别参考。

我需要你整理出一份安全报告，放在 `output/safety_report.json`。报告要包含：

1. **所有活跃且密码强度达标的凭证清单**（去重：同一平台+用户名只保留最新一条；类别名称用 schema 中的中文名；每个条目给出 id、username、platform、category_name、strength）
2. **所有活跃但密码强度不足的凭证 ID 列表**
3. **汇总统计**：活跃总数、合格数、弱密码数、因各种原因被忽略的记录数

注意：
- 密码策略写在 schema 里，每个类别要求不同，请仔细对照。
- 空密码、未知类别、不在 audit_log 中的记录直接忽略，不要放进报告。
- 结果用 UTF-8 编码，字段名严格按英文我上面列的。

辛苦了，今天下班前要结果。

——安全运维 老李
