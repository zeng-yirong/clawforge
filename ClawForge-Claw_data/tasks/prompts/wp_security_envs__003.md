嘿，Mark 找我帮忙处理昨晚的入侵警报。系统又报了一大堆，但 IT 说有些账户已经注销了，不能通知。我刚把最新的区域快照和账户信息扔到了工作区的 `data/` 下面——`data/zones/` 里有个 `zones.json`，里面每个区域的 `intrusion_detected` 标记了是否真的触发了入侵。`data/accounts/` 下的 `accounts.json` 有账户信息，注意看 `active` 字段，只处理那些还活着的账户。每个活跃账户的 `emergency_contacts` 列表里是联系人 ID，去 `data/contacts/` 找 `contacts.json`，把对应的姓名和电话捞出来。最后整理成一份干净的报告，放到 `ops/confirmed_alerts.json`。每条记录都要包含 `zone_id`、`zone_name`、`account_name`、`contact_name` 和 `phone`，按区域和联系人排好序。我只想看到真正需要通知的人，别把已注销账户下的联系人塞进来。搞定了直接告诉我就行。
