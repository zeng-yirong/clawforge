嘿，我是值班安全主管。凌晨3点，主楼传感器触发了一次入侵告警，但监控台上一片混乱——旧日志、备份数据、还有几份过期的报告全堆在工作区里了。我忙着联系安保，没空梳理这些文件。

我需要的很简单：找到那个真正被入侵的防护区（zone），然后告诉我必须立刻锁死的那扇门（door）的ID。别管冗余的备份文件，也别被过时的记录误导。把结果写进 `ops/lock_target.json` 里，格式就一条 `{"door_id": "xxx"}`。我只要准确的ID，错一个门今晚就有人闯进来了。

数据都在这儿了：`zones.json`、`doors.json`、`accounts.json`、`contacts.json`，还有 `logs/` 下面的传感器时间序列和警报 CSV。全在 workspace 里，你看着办。别问我怎么分析，你是专家，自己判断。
