哎呀，我是你们安全运营中心的值班经理老王。昨晚后半夜后台告警系统疯狂弹窗，我年纪大了没扛住，睡过去了几分钟……现在复盘发现有个特别奇怪的漏洞：**主楼大厅（Main Lobby）的入侵传感器在凌晨3:14触发过一次，但当时值班员居然没做确认**。系统日志里只保留了最后一次状态快照，就在 `data/zones/zones.json` 里。我得赶紧把这个遗漏的告警补上确认记录，不然明天审计又要扣分。

我已经让实习生把最新的账户和联系人清单放到 `data/` 下面了。你知道的，规则是这样的：  
- 每个区域（zone）归属到特定的账户（account），账户信息在 `data/accounts.json`。  
- 每个账户有预设的紧急联系人列表（`emergency_contacts`），联系人详情在 `data/contacts/contacts.json`。  
- 报警确认时，必须记录：触发告警的 `zone_id`、归属的 `account_id`、以及该账户第一个 **角色为 'Monitoring Service'** 的联系人（`contact_id`），再加上当前确认时间（ISO 格式，精确到秒，UTC 时区，用 `Z` 后缀）。  

我现在手头乱糟糟的，你能不能帮我把这份确认记录生成出来？就放在工作区根目录，叫 `ops/acknowledgement.json`。字段结构你看着合理组织就行，我只要确认信息准确，别搞错联系人。

哦对了，别管其他房间的传感器，它们都没报警，只有主楼大厅那个是真的。还有，联系人清单里有些号码已经过期了（我让人在 `data/contacts/expired_contacts.json` 里标了），千万别用那些旧的。
