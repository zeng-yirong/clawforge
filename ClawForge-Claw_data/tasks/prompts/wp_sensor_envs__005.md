# 紧急：排查活跃报警传感器

Hey，值班系统刚弹了一条告警，说 Server Room 的温度传感器在下午出现过载，现在可能还在活跃报警。我把所有传感器的历史记录丢在 `data/sensors/` 里了，但这里头文件有点乱，有备份、有草稿，还有好几个版本。

我只要一个东西：**当前还在活跃报警的传感器 ID 列表**。也就是那些状态标记为“active”的传感器。请把这些 ID 整理成一个 JSON 文件，放到 `ops/active_sensors.json` 里，格式你自己定，但让我一眼能看出哪些传感器需要处理。

注意，别把那些已经静默的（比如状态是 inactive 或 error 的）给我混进来。我马上要去跑机房排查，拜托快点搞定。

—— 老王，23:45
