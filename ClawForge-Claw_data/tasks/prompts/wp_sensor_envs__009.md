**主题：机房温度告警——紧急处理**

发件人：运维经理 <ops@example.com>  
收件人：值班工程师 <engineer@example.com>  
时间：今日 03:12

工程师你好，

刚才 Zabbix 连续三次推送了 **Server Room** 的温度告警，但我手头正在处理另一个紧急工单，没空仔细翻数据。

我把最新的传感器快照丢在 `data/sensors/sensors.json` 里了，里面记录了所有传感器当前的状态和读数。另外 `data/locations/locations.json` 里有站点信息，`data/accounts/accounts.json` 是设备归属，不过这些你大概用不上。

现在需要你帮我做一件事：**把所有当前处于活跃状态（active）且读数已经超过各自高限阈值的传感器 ID 提取出来**，写成 JSON 列表，放到 `ops/alarm_sensors.json` 里。文件结构大概是：

["sensor_id_1", "sensor_id_2"]
我这边会拿着这个列表直接去后台做降温和切断操作。注意只要那些 **真的在活跃且读数超标** 的传感器，别把已经停用或正常的也拉进来，否则误操作就麻烦了。

数据比较乱，有些传感器可能缺字段或者格式不对，你识别一下，只处理符合规范的行。辛苦了，等你的文件。

—— 运维经理
