凌晨3点，主库CPU冲到100%，IO等待80%！我得赶紧处理。我把事故池扔在 `data/incidents/incident_pool.json` 里了，你帮我翻一翻，里面应该有一个跟主库IO飙升相关的工单，描述里提到了应该参考哪个运行手册。找到那个工单，然后按手册的步骤来。

手册里会告诉你需要从哪个日志文件里捞出那些长时间运行的活跃事务。把它们的ID整理出来，写到 `ops/kill_target.json`，我准备直接强杀。另外，把这次处理记录写到 `ops/resolution.json`，注明处理了哪个工单、做了啥动作、杀了哪些ID，方便后续审计。

我只要准确的数据，别搞混了。
