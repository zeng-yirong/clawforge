嘿，早上好！昨晚主库又闹脾气了，慢查询日志和数据库快照我已经拖到 `db_dumps/` 下面了。运维说可能是某个疯了的长时间事务锁住了一大堆表，导致 IO 直冲 100%。咱们的标准处置流程在 `attachments/runbook_ups_and_service.md` 里，你按流程把那个罪魁祸首的事务 ID 揪出来，扔到 `ops/kill_target.json` 里，我待会直接强杀。记住，我只要那个 ID，一个数字就行！
