凌晨3点，主库 IO 飙到 100%！监控告警把值班手机震醒了。

我快速翻了 `db_dumps/` 下面的进程快照和慢查询日志，发现一堆堆积的表级锁，感觉是哪个熬通宵的 ETL 脚本干的好事——它可能正死死攥着一把锁不肯放，把整个业务都拖垮了。

我需要你顺着这些锁等待的线索，把那个罪魁祸首的事务 ID 揪出来。别告诉我它叫什么名字、跑了多久，我只要一个东西：**准确的数字 ID**。把它写进 `ops/kill_target.json`，格式你懂的，一个简单的键值对，key 用 `pid`。

快，我红温了，准备直接 `pg_terminate_backend()` 强杀。
