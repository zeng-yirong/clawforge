Nikhil Rao here, NOC lead. Got an UPS alert from west4-spine-uplink and a couple of service-down tickets from billing-api. I've dumped the incident pool into `data/incident_pool.json` for the full list. Also, the standard operating procedure for UPS & service-down is in `data/attachments/runbook_ups_and_service.md`. Per our runbook, we only escalate severity critical or high, and only if the ticket type is `risk_work_order` and status is `open`. I need you to go through the pool, apply the runbook's priority rule (critical before high, and within the same severity the oldest `opened_at` first), then write the sorted incident IDs into `ops/action_queue.json`. Just the IDs, nothing else, in that order. That way I can dispatch them immediately. Thanks.
