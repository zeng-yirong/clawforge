Hey, we've got multiple outages right now. I dropped the latest incident snapshot in `data/incidents/incident_pool.json` and the runbook is in `data/attachments/runbook_ups_and_service.md`. We need to identify all critical outages that are still open — the ones that are either UPS failure or service down. Please read the runbook, apply the appropriate remediation actions for each identified incident, and then write a JSON report to `ops/remediation_actions.json`. The report should list each incident ID along with the action we need to take. Keep it clean, I'll forward it to the team for execution. Thanks.
