嘿，值班小伙伴，半夜被NOC电话吵醒了吧？昨晚西区机房UPS突发断电，虽然ATS切换了，但一堆服务还是抖了几下。工单系统里炸了锅——我简单看了一眼，`data/incidents/incident_pool.json`里混了太多历史遗留的噪音，什么网络抖动、DB延迟爬坡都塞进来了。我现在只关心两类：要么是UPS掉电类（ups_outage），要么是直接服务停摆（service_down）。而且必须是还没处理过的（状态是open或triaged的才算活口），其他关掉的、搁置的都不管。

我把那个池子丢在工作区了，你帮我扫一遍，把符合条件的工单挑出来。挑完之后，在`ops/`目录下建一个`screened_incidents.json`，每条记录就保留 `incident_id` 和 `severity` 两个字段就行，我要拿这个列表去跑批量修复脚本。别给我塞多余的东西，解析出错的话明天晨会你自己解释。动作快点，黄金抢救窗口就剩半小时了。
