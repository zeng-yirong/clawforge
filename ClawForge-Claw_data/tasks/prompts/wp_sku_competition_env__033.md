王姐刚才丢给我一句话：“Q2价格册生效了，DermVeil那边有几款老款停产了，别把废料混进来。你把在售的SKU按价格从低到高拉一份清单，放到reports文件夹里，我晚上开会要用。”  

数据都在 `data/` 下面，品牌、SKU、价格册各归各的夹子。注意别拿上个季度的归档册子充数，我只要 Q2当前生效的那份。里面可能夹着些脏数据，你帮我筛干净。我要每个SKU的ID、名字、单价和币种，按单价从小到大排好。  

弄完放在 `reports/dermveil_sku_pricing.json` 里就行。
