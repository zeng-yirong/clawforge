凌晨三点，我被John Smith的呼吸警报吵醒了——他最近呼吸道一直不太好。我赶紧查了下家里的设备状态，感觉卧室空调可能调得太低了。但家里设备太多，我一时也理不清。  

我把今天下午的快照都扔在工作区了：设备清单在 `data/devices/devices.json`，用户健康档案在 `data/health/health.json`，还有每台设备当前的实时设置放在 `ops/device_status.json`。另外房间分配表在 `data/assignments.json` 里，谁住哪个房间都写清楚了。  

你帮我逐一核对一下：每台空调的温度、加湿器的湿度，是不是超出了对应用户健康档案里标注的舒适范围？如果发现哪个设备设置跟健康要求冲突，就把出问题的设备ID、冲突类型（温度还是湿度）、当前值，以及一个合理的建议值，按条目写到 `ops/violations.json` 里。我只想快速知道哪里要改，别给我分析报告，只输出那个文件就行。  
