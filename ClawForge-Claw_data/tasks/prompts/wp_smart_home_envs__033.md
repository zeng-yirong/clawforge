**紧急工单 #033 – 老爷子又喘了！**

Ops 值班同志：

今下午我刚接到老爷子（John Smith）房间的警报，说呼吸又不顺了。我调了监控，发现卧室空调和加湿器现在的读数跟他的健康档案对不上。你赶紧把 `data/` 下面那些配置文件扒拉一遍，看看所有 **正在运行** 的空调和加湿器，有没有哪个设置超出了对应房间用户的健康建议范围。现在刚好下午3点，电价正贵，顺便把该调的参数都列出来，别让电费白烧。

具体要你干的：
- 找出所有该调整的气候设备（空调 or 加湿器，别管智能插头）。
- 把结果扔到 `ops/override_settings.json` 里，格式随你，但得让我一眼看清 **设备ID**、**当前值** 和 **推荐值**。我要直接拿去改。

对了，别翻旧备份，只看 `data/devices/`、`data/electricity/`、`data/health/`、`data/weather/` 这几个目录。动作快点，老爷子等不起。

—— 智能家居运维中心
