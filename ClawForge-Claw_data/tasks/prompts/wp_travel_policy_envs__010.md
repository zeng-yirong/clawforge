嘿，我是旅行协调员小张。凌晨刚收到VP出差申请——下周一（6月15号）从JFK飞LHR，要商务舱。公司最近新出了商务旅行政策，我手机快没电了，帮我把把关。

我把平台报价、政策文件和历史预订都扔到 `data/` 和 `bookings/` 里了。平台目录下每个文件对应一个供应商，里面有他们的手续费和服务费；报价表在 `data/price_comparison.json`，是各平台同一航线的底价。政策文件有两份，但咱们只认最新的商务旅行政策（文件名里有business那个）。之前已经订过几趟，花掉的预算记录在 `bookings/` 里，每个文件都是一次预订的金额。

你帮我干几件事：
1. 找到最便宜且符合政策的平台（记得把手续费和服务费加上，折扣别漏了，但非活跃的平台别管它）。
2. 看看这个预订是否符合政策（成本上限、舱位、提前天数这些），要不要审批（超过1500刀才需要）。
3. 算一下公司总预算（在 `data/accounts.json` 里）减去历史已花费的，再减去这次预订，告诉我还剩多少。

最后把结果写成 `ops/booking_recommendation.json`，里面写清楚你选的是哪个平台、总费用（含各种杂费）、政策ID、是否合规、是否需要审批，以及这次预订后预算的剩余。尽量精确，我直接拿去发邮件。
