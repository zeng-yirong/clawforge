Hey，我是Emma，财务部负责差旅审批的。  
刚刚Alice提交了一个从JFK到LHR的紧急出差申请，说是下周一就要走，我已经把所有相关材料都拖到 `raw_data/` 文件夹下了——  
- 报价快照在 `raw_data/quotes/` 里面，每张票的裸价、手续费、促销都写得很清楚。  
- 公司差旅政策在 `raw_data/policies/`，最新版本的才是当前有效的，别用错了。  
- 还有她的申请单 `raw_data/booking_request.json`，上面写了出发日期、舱位和首选平台。  

她指定要用SkyBook订票。按公司最新政策，我们需要算一下 **实际每张票要花多少钱**（裸价加上平台手续费和交易费，再减掉有效优惠）。  
我这边急着回复审批，你算出来后，把最终成本数字写到工作区根目录的 `result.json` 里，格式像 `{"final_cost": <数字>}` 这样，我程序直接读。  
别整复杂了，我就要这个数字。谢谢！
