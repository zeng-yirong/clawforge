主题：紧急差旅政策合规检查

Hi 伙伴，

公司刚刚签发了新一版的差旅政策（2026年版，v2.0），CEO 要求我们立即排查所有已确认或待处理的预订，确保完全合规。我把相关的政策文件、平台数据和历史预订记录都放在 `data/` 下了：

- `data/policies/` 里有新旧版本的政策定义，注意用最新的那份。
- `data/platforms/` 是各平台的费用与规则。
- `data/accounts.json` 是公司账户和审批人信息。
- `data/bookings/` 里有很多预订记录，有些已经取消了，别管它们。

请帮我找出所有**违背新政策**的活跃预订（状态为 `confirmed` 或 `pending`），并说明每条违反了哪几条具体规定。把结果整理成一个 JSON 文件，放在 `ops/approval_required.json` 里，格式大致是：

[
  {
    "booking_id": "...",
    "violations": ["...", "..."]
  }
]
这样我直接提交给法务和财务去处理。拜托了！

—— Lisa（差旅运营经理）
