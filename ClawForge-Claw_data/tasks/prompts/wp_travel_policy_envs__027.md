Subject: 紧急！批量导入的预订记录需要审批筛选

Hi 小助手，

上周五财务把一批从各平台同步过来的预订记录扔进了 `data/bookings/`，现在我得在下班前把所有需要走正式审批的订单挑出来。你也知道，不同公司用的政策不同，每个账户还有自己的审批人列表。

几个要点：
- 政策文件在 `data/policies/` 里，按政策 ID 命名，版本最新的那个才是生效的（版本号最大的就对了）。
- 账户信息在 `data/accounts.json`，每个账户都有对应的 approvers 列表，排第一位的审批人就行。
- 预订记录里有些已经取消了（status = cancelled），这些别管它。

我现在最关心的是：**哪些预订的实际总费用超过了它对应政策规定的 require_approval_above 阈值，而且还没被取消。** 对于每一条这样的预订，我需要你整理出一个清单，包含预订编号（booking_id）、实际费用（total_cost）、用到的政策编号（policy_id）以及该账户的第一位审批人（approver）。把这些信息写到一个 JSON 文件里，放在 `ops/approval_required.json`。

我只需要精确的结果，不要多也不要少。麻烦你了，今天必须搞定！

—— 差旅管理员 Jack
