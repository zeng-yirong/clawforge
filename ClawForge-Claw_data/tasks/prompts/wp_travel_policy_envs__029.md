嘿，出差部的同事们，情况有点紧急。

昨晚我们系统升级时把一堆预订记录搞乱了，现在财务那边催着要本月需要特批的出差单列表。我把所有原始数据都扔到了当前工作目录里，包括账户信息（accounts.json）、过去三年各版本的公司差旅政策（policies/）、平台数据（platforms/）、以及所有未归档的预订（bookings/）。注意里面混了很多旧版本政策、作废记录和测试数据，你帮我筛一筛。

公司目前执行的最新差旅政策是版本号最高的那个，里面有个“需要手动审批”的金额门槛，你应该能找到。我需要你找出所有**有效预订**（状态是 confirmed 或 pending，且未被删除标记）中，**单次费用**超过了那个门槛的那些，把它们的 booking_id 整理成一个 JSON 数组，放进 `ops/pending_approvals.json`。文件里只要纯数组就行，我这边脚本直接读。

哦对了，预订文件里有几个字段是冗余的（比如 cost 和 total_amount），以 cost 为准。别漏了，也别把垃圾数据放进去。搞定了告诉我一声，我直接拿去审批流。

谢了！
