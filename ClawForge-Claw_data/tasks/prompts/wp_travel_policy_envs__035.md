Subject: 紧急预订审核 – 请尽快处理

我刚收到一封来自 Acme Corp 差旅部的邮件，他们有一个紧急的商务出行需求：

- 出发地：JFK
- 目的地：LHR
- 日期：2026-06-15
- 舱位：商务舱（Business）
- 人数：1人
- 账户：acme001

预订请求的详细文件我已经放在 `ops/booking_request.json` 里了。

你知道的，公司最近更新了差旅政策，新版本就在 `data/policies/` 目录下，旧版本也留着做参考。各平台（SkyBook、AeroCheap、FlightPro）的最新报价和服务费明细都在 `data/platforms/` 里，注意有些平台可能处于停用状态，别拿到无效的。

我需要你帮我做三件事：

1. 在所有 **当前可用** 的平台中，计算每个平台的实际总成本（包含票价、手续费、服务费，别忘了他们可能有折扣），然后选出 **总成本最低** 的那个。
2. 从政策目录里找出 **Acme Corp 商务出行政策** 的 **最新版本**（注意版本号），用它来审核这个最低成本方案是否合规。关键检查两点：
   - 总成本是否超过政策允许的单次预订上限？
   - 如果没超上限，是否超过了需要报批的金额阈值？
3. 最后把审核结果写到 `ops/booking_decision.json` 里，结构我大概想了一下：包含选中的平台、总成本、所用的政策编号、是否需要报批（`requires_approval`）、以及是否可以直接自动批准（`auto_approved`）。

数据都给你了，请务必准确，别漏掉任何费用或折扣。这件事很急，拜托了。
