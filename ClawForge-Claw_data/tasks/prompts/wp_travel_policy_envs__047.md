嗨，我是财务部的 Anna。昨晚政策团队发来邮件说最新的差旅政策 v2.1 已经生效了，但我发现 `policies/` 下面还躺着七八个旧版本，怕你们搞混。我已经把当前有效的政策文件单独挪到了 `policies/current_policy.json`，里面全是新的规则。

另外，这周有十几笔机票预订请求都堆在 `requests/` 目录下，需要按新政策逐一合规审查。每个请求是一个 JSON 文件，字段包括出发日期、舱位、总价等等。你帮我跑一遍，找出那些踩了红线（比如超预算、舱位不允许、预订时间太晚）的请求，把它们的请求 ID 和违反的具体条款写到 `ops/compliance_report.json` 里，我直接拿去跟业务部门对线。

注意：只参考 `policies/current_policy.json` 里的规则，其他旧文件别管。报告格式你自己定，但必须让我一眼能看出哪个请求为什么不行。哦对了，我的小助理上次往 `ops/` 里扔过一个叫 `temp_notes.txt` 的草稿，你别把它当成报告了。辛苦了！
