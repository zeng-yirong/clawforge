# Solution Matching Notes

Canonical target solution:

- `sol_heliosync_fabric`
- `HelioSync Edge Inference Fabric`

Valid aliases:

- `HelioSync fabric`
- `HSEF`

Common false positives that should not be treated as the target:

- `HelioStream Runtime`
- `Helio Fabric Gateway`

If a clue list is saved for this task, use the canonical solution id and name, not just an alias.
