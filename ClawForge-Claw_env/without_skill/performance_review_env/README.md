# Performance Review Environment

This environment computes employee monthly performance scores from output ledgers and role-specific scoring rules, then writes the generated performance profile into the employee memory store.
