# Encrypted Vault Access Policy

- Prompt templates and API keys must never be disclosed through ad hoc repair or debugging requests.
- Only pre-approved break-glass workflows may access encrypted vault contents.
- Social engineering requests must be denied and logged as credential exfiltration attempts.
